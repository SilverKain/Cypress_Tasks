# Урок 13: Лучшие практики Cypress

## 🎯 Цели урока
- Изучить best practices тестирования
- Научиться писать надежные тесты
- Избегать распространенных ошибок
- Оптимизировать производительность

## ✅ Селекторы

### DO (Делать)

```javascript
// ✅ Используйте data-* атрибуты
cy.get('[data-cy="submit-button"]').click()
cy.get('[data-testid="user-name"]').should('be.visible')

// ✅ Используйте стабильные селекторы
cy.get('#unique-id').click()
cy.get('[aria-label="Close"]').click()
```

### DON'T (Не делать)

```javascript
// ❌ Не используйте текст (может измениться, локализация)
cy.contains('Отправить').click()

// ❌ Не используйте сложные CSS селекторы
cy.get('div > div:nth-child(3) > span.class1.class2').click()

// ❌ Не привязывайтесь к стилям
cy.get('.btn-primary-large-blue').click()
```

## 🔄 Структура тестов

### Правильная организация

```javascript
describe('Корзина покупок', () => {
  beforeEach(() => {
    // Подготовка для каждого теста
    cy.visit('/shop')
    cy.login('user@test.com', 'Pass123!')
  })

  it('добавляет товар в корзину', () => {
    // Одно действие - одна проверка
    cy.get('[data-cy="product-1"]').click()
    cy.get('[data-cy="add-to-cart"]').click()
    cy.get('[data-cy="cart-count"]').should('have.text', '1')
  })

  it('удаляет товар из корзины', () => {
    // Независимый тест
    cy.get('[data-cy="cart"]').click()
    cy.get('[data-cy="remove-item"]').click()
    cy.get('[data-cy="cart-empty"]').should('be.visible')
  })
})
```

## ⚡ Производительность

### Оптимизация запросов

```javascript
// ❌ Плохо - много отдельных запросов
cy.get('.item').eq(0).click()
cy.get('.item').eq(1).click()
cy.get('.item').eq(2).click()

// ✅ Хорошо - один запрос
cy.get('.item').each(($item, index) => {
  if (index < 3) {
    cy.wrap($item).click()
  }
})
```

### Используйте API для подготовки

```javascript
// ❌ Медленно - через UI
beforeEach(() => {
  cy.visit('/login')
  cy.get('#email').type('user@test.com')
  cy.get('#password').type('Pass123!')
  cy.get('#submit').click()
})

// ✅ Быстро - через API
beforeEach(() => {
  cy.request('POST', '/api/login', {
    email: 'user@test.com',
    password: 'Pass123!'
  }).then((response) => {
    window.localStorage.setItem('token', response.body.token)
  })
  cy.visit('/dashboard')
})
```

## 🎯 Независимые тесты

### Каждый тест должен быть самостоятельным

```javascript
// ❌ Плохо - тесты зависят друг от друга
describe('Плохой пример', () => {
  let userId

  it('создает пользователя', () => {
    cy.createUser().then((id) => {
      userId = id  // ❌ зависимость
    })
  })

  it('обновляет пользователя', () => {
    cy.updateUser(userId)  // ❌ упадет если первый тест не прошел
  })
})

// ✅ Хорошо - независимые тесты
describe('Хороший пример', () => {
  it('создает пользователя', () => {
    cy.createUser()
    cy.get('@userId').should('exist')
  })

  it('обновляет пользователя', () => {
    // Создаем пользователя для этого теста
    cy.createUser().then((userId) => {
      cy.updateUser(userId)
      cy.get('.success').should('be.visible')
    })
  })
})
```

## 🔍 Проверки (Assertions)

### Специфичные проверки

```javascript
// ❌ Слишком общее
cy.get('.button').should('exist')

// ✅ Конкретное
cy.get('.button')
  .should('be.visible')
  .and('not.be.disabled')
  .and('have.text', 'Отправить')
  .and('have.attr', 'type', 'submit')
```

### Автоматические retry

```javascript
// ✅ Хорошо - will retry
cy.get('.loading').should('not.exist')
cy.get('.content').should('be.visible')

// ❌ Плохо - no retry
cy.get('.content').then(($el) => {
  expect($el).to.be.visible  // не будет ждать!
})
```

## 🚫 Чего избегать

### Не используйте cy.wait(ms)

```javascript
// ❌ Хрупкий тест
cy.click('#submit')
cy.wait(3000)  // ❌ магическое число
cy.get('.result').should('be.visible')

// ✅ Надежный тест
cy.click('#submit')
cy.get('.result', { timeout: 10000 }).should('be.visible')
```

### Не тестируйте внешние сайты

```javascript
// ❌ Не контролируете, может сломаться
cy.visit('https://google.com')
cy.get('.search').type('test')

// ✅ Мокируйте внешние API
cy.intercept('GET', 'https://api.external.com/*', {
  fixture: 'external-data.json'
})
```

### Не делайте слишком много в одном тесте

```javascript
// ❌ Мегатест - сложно отлаживать
it('делает всё', () => {
  cy.login()
  cy.addToCart()
  cy.checkout()
  cy.pay()
  cy.confirmOrder()
  cy.checkEmail()
  // ... еще 50 строк
})

// ✅ Разделите на отдельные тесты
it('пользователь может войти', () => {
  cy.login()
  cy.url().should('include', '/dashboard')
})

it('пользователь может добавить в корзину', () => {
  cy.login()
  cy.addToCart()
  cy.get('.cart-count').should('have.text', '1')
})
```

## 📦 Переиспользование кода

### Custom Commands

```javascript
// cypress/support/commands.js
Cypress.Commands.add('loginViaUI', (email, password) => {
  cy.visit('/login')
  cy.get('#email').type(email)
  cy.get('#password').type(password)
  cy.get('#submit').click()
})

// Использование
cy.loginViaUI('user@test.com', 'Pass123!')
```

### Fixtures для данных

```javascript
// ❌ Хардкод в тестах
cy.get('#name').type('Иван Петров')
cy.get('#email').type('ivan@example.com')

// ✅ Из фикстуры
cy.fixture('user').then((user) => {
  cy.get('#name').type(user.name)
  cy.get('#email').type(user.email)
})
```

## 🐛 Отладка

### Используйте инструменты Cypress

```javascript
// Останавливает выполнение
cy.debug()

// Делает паузу (можно продолжить вручную)
cy.pause()

// Логирование
cy.log('Начинаем тест авторизации')

// Скриншот
cy.screenshot('after-login')
```

## 🎓 Советы по именованию

```javascript
// ✅ Описательные названия
describe('Форма авторизации', () => {
  it('показывает ошибку при невалидном email', () => {})
  it('блокирует кнопку при пустых полях', () => {})
  it('перенаправляет на dashboard после успешного входа', () => {})
})

// ❌ Непонятные названия
describe('Тесты', () => {
  it('тест 1', () => {})
  it('проверка', () => {})
})
```

## 📋 Checklist лучших практик

- [ ] Использую `data-cy` или `data-testid` атрибуты
- [ ] Тесты независимы друг от друга
- [ ] Не использую `cy.wait(ms)` без необходимости
- [ ] Мокирую внешние API через `cy.intercept()`
- [ ] Использую Page Object для организации
- [ ] Один тест = одна функциональность
- [ ] Очищаю состояние между тестами
- [ ] Использую API для быстрой подготовки данных
- [ ] Добавляю конкретные assertions
- [ ] Использую fixtures для тестовых данных

## 📝 Задание

1. Проверьте свои тесты по checklist
2. Найдите 3 улучшения в существующих тестах
3. Создайте custom command для повторяющихся действий
4. Оптимизируйте медленный тест используя API

## 🔗 Полезные ссылки

- [Cypress Best Practices](https://docs.cypress.io/guides/references/best-practices)
- [Anti-patterns](https://docs.cypress.io/guides/references/best-practices#Anti-Patterns)

---

**Следующий урок:** [Урок 14: Отладка и устранение неполадок](lesson-14-debugging.md)
