# Урок 3: Первый тест - разбираем по частям

## 🎯 Цели урока
- Понять структуру теста в Cypress
- Изучить основные команды
- Научиться писать простые проверки
- Создать свой первый осмысленный тест

## 📖 Анатомия теста

### Базовая структура

```javascript
describe('Название группы тестов', () => {
  it('Описание конкретного теста', () => {
    // Тело теста - что делаем
    cy.visit('/page')        // Действие
    cy.get('#button').click() // Действие
    cy.url().should('contain', '/success') // Проверка
  })
})
```

### Разберем каждую часть:

#### 1. `describe()` - группа тестов
```javascript
describe('Регистрация пользователя', () => {
  // Здесь будут все тесты связанные с регистрацией
})
```

Это как папка, в которой собраны похожие тесты.

#### 2. `it()` - отдельный тест
```javascript
it('Пользователь может зарегистрироваться с валидными данными', () => {
  // Конкретные шаги теста
})
```

Название должно четко описывать, что проверяется.

#### 3. Команды Cypress - действия
```javascript
cy.visit()    // Открыть страницу
cy.get()      // Найти элемент
cy.click()    // Кликнуть
cy.type()     // Ввести текст
cy.should()   // Проверить
```

## 🔍 Основные команды

### Навигация

```javascript
// Открыть страницу
cy.visit('/login')
cy.visit('https://example.com/page')

// Перейти назад/вперед
cy.go('back')
cy.go('forward')

// Обновить страницу  
cy.reload()
```

### Поиск элементов

```javascript
// По ID
cy.get('#submit-button')

// По классу
cy.get('.error-message')

// По атрибуту
cy.get('[data-testid="username"]')

// По тегу
cy.get('button')

// Комбинированные селекторы
cy.get('input[type="email"]')
cy.get('.form button.primary')
```

### Действия с элементами

```javascript
// Клик
cy.get('#login-btn').click()

// Ввод текста
cy.get('#email').type('user@test.com')

// Очистить поле
cy.get('#email').clear()

// Выбор в dropdown
cy.get('#country').select('Russia')

// Чекбокс/радиобаттон
cy.get('#agree').check()
cy.get('#agree').uncheck()
```

### Проверки (Assertions)

```javascript
// Проверить текст
cy.get('h1').should('contain', 'Добро пожаловать')

// Проверить наличие элемента
cy.get('#success-message').should('be.visible')
cy.get('#error-message').should('not.exist')

// Проверить URL
cy.url().should('include', '/dashboard')
cy.url().should('eq', 'https://example.com/home')

// Проверить атрибуты
cy.get('#submit').should('be.disabled')
cy.get('#email').should('have.value', 'test@example.com')
```

## 💡 Практический пример

Давайте создадим тест для простой формы регистрации:

### HTML страницы (для понимания):
```html
<form id="registration-form">
  <h2>Регистрация</h2>
  
  <input 
    type="text" 
    id="firstName" 
    placeholder="Имя"
    required
  />
  
  <input 
    type="email" 
    id="email" 
    placeholder="Email"
    required
  />
  
  <input 
    type="password" 
    id="password" 
    placeholder="Пароль"
    required
  />
  
  <input 
    type="checkbox" 
    id="agreeTerms"
  />
  <label for="agreeTerms">Согласен с условиями</label>
  
  <button type="submit" id="registerBtn">
    Зарегистрироваться
  </button>
  
  <div id="successMessage" style="display:none;">
    Регистрация успешна!
  </div>
</form>
```

### Тест для этой формы:

```javascript
describe('Форма регистрации', () => {
  
  beforeEach(() => {
    // Выполняется перед каждым тестом
    cy.visit('/register')
  })

  it('Отображает все необходимые поля', () => {
    // Проверяем наличие заголовка
    cy.get('h2').should('contain', 'Регистрация')
    
    // Проверяем наличие полей
    cy.get('#firstName').should('be.visible')
    cy.get('#email').should('be.visible')
    cy.get('#password').should('be.visible')
    cy.get('#agreeTerms').should('be.visible')
    cy.get('#registerBtn').should('be.visible')
  })

  it('Успешная регистрация с валидными данными', () => {
    // Заполняем форму
    cy.get('#firstName').type('Иван')
    cy.get('#email').type('ivan@test.com')
    cy.get('#password').type('SecurePassword123')
    cy.get('#agreeTerms').check()
    
    // Отправляем форму
    cy.get('#registerBtn').click()
    
    // Проверяем результат
    cy.get('#successMessage').should('be.visible')
    cy.get('#successMessage').should('contain', 'Регистрация успешна')
  })

  it('Показывает ошибку при пустом email', () => {
    // Заполняем только имя
    cy.get('#firstName').type('Иван')
    cy.get('#password').type('SecurePassword123')
    cy.get('#agreeTerms').check()
    
    // Пытаемся отправить
    cy.get('#registerBtn').click()
    
    // Email поле должно показать ошибку валидации браузера
    cy.get('#email:invalid').should('exist')
  })

  it('Кнопка неактивна без согласия с условиями', () => {
    // Заполняем все кроме чекбокса
    cy.get('#firstName').type('Иван')
    cy.get('#email').type('ivan@test.com')
    cy.get('#password').type('SecurePassword123')
    
    // Проверяем, что кнопка неактивна
    // (предполагается, что она блокируется JS)
    cy.get('#registerBtn').should('be.disabled')
  })
})
```

## 🔧 Полезные хуки (hooks)

```javascript
describe('Тесты с подготовкой', () => {
  
  before(() => {
    // Выполняется ОДИН раз перед ВСЕМИ тестами
    console.log('Настройка перед всеми тестами')
  })
  
  beforeEach(() => {
    // Выполняется перед КАЖДЫМ тестом
    cy.visit('/login')
    cy.clearCookies()
  })
  
  afterEach(() => {
    // Выполняется после КАЖДОГО теста
    cy.clearLocalStorage()
  })
  
  after(() => {
    // Выполняется ОДИН раз после ВСЕХ тестов
    console.log('Очистка после всех тестов')
  })

  it('тест 1', () => {
    // beforeEach -> тест 1 -> afterEach
  })

  it('тест 2', () => {
    // beforeEach -> тест 2 -> afterEach
  })
})
```

## 🎯 Лучшие практики именования

### ❌ Плохо:
```javascript
describe('Тест', () => {
  it('работает', () => {
    // Что работает? Как понять?
  })
})
```

### ✅ Хорошо:
```javascript
describe('Страница логина', () => {
  it('Пользователь может войти с валидными данными', () => {
    // Понятно, что тестируется
  })
  
  it('Показывает ошибку при неверном пароле', () => {
    // Понятно, что проверяется
  })
})
```

### Паттерн именования:
```
describe('Что тестируем (компонент/страница)', () => {
  it('Кто может сделать что в каком сценарии', () => {
    // Действия и проверки
  })
})
```

## 📝 Практическое задание

Создайте тест для простого калькулятора с такой HTML структурой:

```html
<div id="calculator">
  <input type="number" id="num1" placeholder="Первое число">
  <select id="operation">
    <option value="+">Сложение</option>
    <option value="-">Вычитание</option>
    <option value="*">Умножение</option>
    <option value="/">Деление</option>
  </select>
  <input type="number" id="num2" placeholder="Второе число">
  <button id="calculate">Вычислить</button>
  <div id="result">Результат: <span id="answer"></span></div>
</div>
```

### Напишите тесты для:

1. **Сложение**: 5 + 3 = 8
2. **Вычитание**: 10 - 4 = 6
3. **Умножение**: 6 * 7 = 42
4. **Деление**: 15 / 3 = 5

### Шаблон для начала:

```javascript
describe('Калькулятор', () => {
  beforeEach(() => {
    cy.visit('/calculator') // замените на ваш URL
  })

  it('Выполняет сложение чисел', () => {
    // Ваш код здесь
  })

  // Добавьте остальные тесты
})
```

## 🚀 Что дальше?

В следующем уроке мы изучим селекторы подробнее и научимся находить элементы в сложных сценариях.

---

💡 **Совет**: Пишите понятные названия тестов - через месяц вы забудете, что именно проверяли!