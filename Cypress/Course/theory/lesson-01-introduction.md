# Урок 1: Что такое Cypress?

## 🎯 Цели урока
- Понять, что такое Cypress и зачем он нужен
- Изучить преимущества Cypress перед другими инструментами
- Понять архитектуру и принципы работы

## 📖 Теория

### Что такое Cypress?

**Cypress** - это современный инструмент для автоматизации тестирования веб-приложений. Он позволяет писать тесты, которые взаимодействуют с вашим приложением так же, как это делал бы обычный пользователь.

### Зачем нужны автотесты?

Представьте, что вы разрабатываете интернет-магазин. Каждый раз, когда добавляется новая функция, нужно проверить:
- ✅ Регистрация работает
- ✅ Авторизация работает
- ✅ Добавление товара в корзину работает
- ✅ Оформление заказа работает
- ✅ И еще десятки функций...

Делать это вручную каждый раз - очень долго и скучно. Автотесты делают это за вас!

### Преимущества Cypress

#### 🚀 **Скорость разработки**
```javascript
// Простой тест на Cypress
describe('Магазин', () => {
  it('Пользователь может добавить товар в корзину', () => {
    cy.visit('/shop')
    cy.get('[data-testid="product-1"]').click()
    cy.get('[data-testid="add-to-cart"]').click()
    cy.get('[data-testid="cart-count"]').should('contain', '1')
  })
})
```

#### 🎥 **Визуальность**
Cypress записывает видео ваших тестов и делает скриншоты при ошибках.

#### ⚡ **Быстрое выполнение**
Тесты выполняются прямо в браузере, что делает их очень быстрыми.

#### 🔍 **Отличная отладка**
Можно остановить тест в любой момент и посмотреть, что происходит.

### Архитектура Cypress

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Ваши тесты    │ -> │  Cypress Runner  │ -> │   Браузер       │
│   (JavaScript)  │    │                  │    │   (Chrome,      │
│                 │    │                  │    │    Firefox...)  │
└─────────────────┘    └──────────────────┘    └─────────────────┘
```

**Особенность**: Cypress выполняется ВНУТРИ браузера, а не снаружи (как Selenium).

### Что можно тестировать?

1. **Функциональность** - кнопки работают, формы отправляются
2. **UI** - элементы отображаются правильно
3. **API** - бэкенд отвечает корректно
4. **Интеграции** - разные части приложения работают вместе

### Простой пример

Допустим, у нас есть простая страница логина:

```html
<form>
  <input type="email" id="email" placeholder="Email">
  <input type="password" id="password" placeholder="Пароль">
  <button type="submit">Войти</button>
</form>
```

Тест на Cypress будет выглядеть так:

```javascript
describe('Страница логина', () => {
  it('Пользователь может войти в систему', () => {
    // Открываем страницу
    cy.visit('/login')
    
    // Вводим email
    cy.get('#email').type('user@example.com')
    
    // Вводим пароль
    cy.get('#password').type('mypassword')
    
    // Нажимаем кнопку
    cy.get('button[type="submit"]').click()
    
    // Проверяем, что попали на главную страницу
    cy.url().should('include', '/dashboard')
  })
})
```

## 📝 Ключевые понятия

- **Test Suite (Набор тестов)** - группа связанных тестов
- **Test Case (Тест-кейс)** - один конкретный сценарий
- **Assertion (Утверждение)** - проверка того, что что-то работает как ожидается
- **Element (Элемент)** - кнопка, поле ввода, текст на странице

## 🎯 Проверь себя

### Вопросы для самопроверки:

1. Что такое Cypress?
2. В чем главное отличие Cypress от других инструментов тестирования?
3. Что можно тестировать с помощью Cypress?
4. Какой язык используется для написания тестов?

### Ответы:
1. Современный инструмент для автоматизации тестирования веб-приложений
2. Выполняется внутри браузера, что делает его быстрым и удобным для отладки
3. Функциональность, UI, API, интеграции
4. JavaScript

## 🚀 Что дальше?

В следующем уроке мы установим Cypress и настроим рабочее окружение.

---

💡 **Совет**: Не переживайте, если пока не все понятно. Мы будем разбирать каждую тему подробно с практическими примерами!