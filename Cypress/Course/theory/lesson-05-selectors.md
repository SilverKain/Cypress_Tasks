# Урок 5: Селекторы и поиск элементов

## 🎯 Цели урока
- Изучить все типы селекторов в Cypress
- Научиться находить элементы в сложных случаях
- Понять иерархию и специфичность селекторов
- Освоить продвинутые техники поиска

## 📖 Теория селекторов

### Что такое селекторы?

**Селектор** - это способ указать Cypress, какой именно элемент на странице нас интересует. Это как адрес дома - чем точнее адрес, тем быстрее найдем нужное место.

```javascript
// Аналогия с адресом:
cy.get('#main')           // Дом №1 на главной улице
cy.get('.button')         // Все синие дома в городе
cy.get('[data-cy="send"]') // Дом с табличкой "Отправить"
```

## 🔍 Основные типы селекторов

### 1. Селекторы по ID

```javascript
// HTML: <button id="submit-btn">Отправить</button>
cy.get('#submit-btn').click()

// ✅ Плюсы: быстро, однозначно
// ❌ Минусы: ID может измениться
```

**Когда использовать:** Когда есть уникальный ID и он стабилен.

### 2. Селекторы по классу

```javascript
// HTML: <div class="error-message">Ошибка!</div>
cy.get('.error-message').should('be.visible')

// Множественные классы
// HTML: <button class="btn btn-primary btn-large">
cy.get('.btn.btn-primary')  // элемент с обоими классами
cy.get('.btn-large')        // элемент с классом btn-large
```

**Когда использовать:** Для групп похожих элементов.

### 3. Селекторы по тегу

```javascript
// Все кнопки на странице
cy.get('button').should('have.length.greaterThan', 0)

// Все ссылки
cy.get('a').each(($link) => {
  expect($link).to.have.attr('href')
})
```

**Когда использовать:** Для проверки групп элементов.

### 4. Селекторы по атрибутам

```javascript
// HTML: <input type="email" placeholder="Введите email">
cy.get('[type="email"]').type('test@example.com')
cy.get('[placeholder="Введите email"]').should('be.empty')

// Частичное совпадение атрибута
cy.get('[class*="button"]')     // содержит "button"
cy.get('[href^="/admin"]')      // начинается с "/admin"
cy.get('[src$=".jpg"]')         // заканчивается на ".jpg"
```

### 5. Data-атрибуты (РЕКОМЕНДУЕТСЯ)

```javascript
// HTML: <button data-testid="login-btn">Войти</button>
cy.get('[data-testid="login-btn"]').click()

// Короткая форма для data-cy
// HTML: <div data-cy="user-profile">
cy.get('[data-cy="user-profile"]')
```

**Почему лучше:** Специально для тестов, не изменяются разработчиками.

## 🔧 Комбинированные селекторы

### Пространственные отношения

```javascript
// Дочерние элементы
cy.get('#sidebar > .menu-item')     // прямые дети
cy.get('#sidebar .menu-item')       // все потомки

// Соседние элементы
cy.get('.label + input')            // следующий элемент
cy.get('.label ~ input')            // все следующие

// Вложенность
cy.get('.form').find('.input-field') // поиск внутри
```

### Практический пример:

```javascript
describe('Поиск элементов в форме', () => {
  beforeEach(() => {
    cy.visit('/registration')
  })

  it('Находит элементы формы разными способами', () => {
    // По структуре HTML
    cy.get('form#registration')
      .within(() => {
        // Ищем только внутри формы
        cy.get('input[type="text"]').first().type('Иван')
        cy.get('input[type="email"]').type('ivan@test.com')
        cy.get('select[name="country"]').select('Russia')
      })

    // По позиции
    cy.get('.form-group').eq(2)       // третья группа
    cy.get('.input-field').first()    // первое поле
    cy.get('.input-field').last()     // последнее поле
  })
})
```

## 🎨 Продвинутые техники

### Поиск по тексту

```javascript
// Точное совпадение
cy.contains('Войти в систему').click()

// Частичное совпадение
cy.contains('Войти').click()

// С селектором
cy.get('.buttons').contains('Отправить').click()

// По тексту в конкретном элементе
cy.get('button').contains('Удалить').click()
```

### Фильтрация элементов

```javascript
// Видимые элементы
cy.get('.menu-item').filter(':visible')

// По атрибуту
cy.get('input').filter('[required]')

// По содержимому
cy.get('.card').filter(':contains("Специальное предложение")')

// Исключение элементов
cy.get('button').not('.disabled')
cy.get('.list-item').not(':first')
```

### Поиск по позиции

```javascript
// По индексу
cy.get('.item').eq(0)        // первый (индекс 0)
cy.get('.item').eq(2)        // третий (индекс 2)
cy.get('.item').eq(-1)       // последний

// Первый и последний
cy.get('.item').first()
cy.get('.item').last()

// Срезы
cy.get('.item').slice(1, 3)  // элементы с индексами 1-2
```

## 🛡️ Стратегии стабильных селекторов

### ❌ Ненадежные селекторы

```javascript
// Плохо - зависят от стилей
cy.get('.btn-primary.large.rounded')

// Плохо - хрупкие пути
cy.get('body > div:nth-child(3) > section > div:nth-child(2)')

// Плохо - зависят от контента
cy.contains('Кнопка 1').click() // что если текст изменится?
```

### ✅ Надежные селекторы

```javascript
// Хорошо - data-атрибуты для тестов
cy.get('[data-testid="submit-form"]')
cy.get('[data-cy="user-menu"]')

// Хорошо - стабильные ID
cy.get('#main-navigation')

// Хорошо - семантические атрибуты
cy.get('[role="button"]')
cy.get('[aria-label="Закрыть модальное окно"]')
```

### Договоренности с разработчиками

```javascript
// Попросите разработчиков добавлять data-атрибуты:

// HTML от разработчика:
<button 
  class="btn btn-primary" 
  data-testid="login-button"
  onClick={handleLogin}
>
  Войти
</button>

// Ваш тест:
cy.get('[data-testid="login-button"]').click()
```

## 🔍 Отладка селекторов

### Проверка в браузере

```javascript
// В консоли браузера проверьте селектор:
document.querySelector('#my-selector')
document.querySelectorAll('.my-class')

// В Cypress Test Runner:
cy.get('.my-selector').debug()  // остановка для изучения
cy.get('.my-selector').pause()  // пауза выполнения
```

### Логирование

```javascript
cy.get('.items')
  .should('have.length.greaterThan', 0)
  .then(($elements) => {
    console.log('Найдено элементов:', $elements.length)
    $elements.each((index, element) => {
      console.log(`Элемент ${index}:`, element)
    })
  })
```

## 💡 Практические примеры

### Сложная форма

```javascript
describe('Сложная форма регистрации', () => {
  it('Заполняет многоступенчатую форму', () => {
    cy.visit('/complex-form')

    // Шаг 1: Личные данные
    cy.get('[data-step="personal"]').within(() => {
      cy.get('[name="firstName"]').type('Иван')
      cy.get('[name="lastName"]').type('Петров')
      cy.get('[type="date"]').type('1990-01-01')
    })

    // Переход к следующему шагу
    cy.get('.step-navigation').contains('Далее').click()

    // Шаг 2: Контакты
    cy.get('[data-step="contacts"]').within(() => {
      cy.get('input[type="email"]').type('ivan@example.com')
      cy.get('input[type="tel"]').type('+7 900 123 45 67')
      
      // Сложный селектор для выпадающего списка
      cy.get('.custom-select[data-field="country"]')
        .click()
        .get('.select-option[data-value="RU"]')
        .click()
    })
  })
})
```

### Динамический контент

```javascript
describe('Работа с динамическим контентом', () => {
  it('Находит элементы, которые появляются после загрузки', () => {
    cy.visit('/dynamic-page')

    // Ждем появления контента
    cy.get('[data-testid="loading"]').should('not.exist')
    
    // Ищем в загруженном контенте
    cy.get('.dynamic-list')
      .find('.list-item')
      .should('have.length.greaterThan', 0)
      .each(($item) => {
        // Проверяем каждый элемент
        cy.wrap($item)
          .find('.item-title')
          .should('not.be.empty')
        
        cy.wrap($item)
          .find('.item-price')
          .should('match', /^\d+\.\d{2}$/) // регулярное выражение для цены
      })
  })
})
```

## 🎯 Практические советы

### 1. Приоритет селекторов (от лучшего к худшему):

1. `[data-testid="element"]` - специально для тестов
2. `[data-cy="element"]` - Cypress конвенция  
3. `#unique-id` - уникальный ID
4. `[role="button"]` - семантические атрибуты
5. `.stable-class-name` - стабильные классы
6. `button[type="submit"]` - комбинация тег + атрибут

### 2. Используйте `.within()` для области поиска:

```javascript
cy.get('.modal').within(() => {
  cy.get('.close-button').click() // ищем только в модальном окне
})
```

### 3. Создавайте алиасы для переиспользования:

```javascript
cy.get('[data-testid="product-list"]').as('productList')
cy.get('@productList').find('.product-item').should('have.length', 5)
cy.get('@productList').find('.load-more').click()
```

## 📝 Практическое задание

Попробуйте найти селекторы для этой HTML структуры:

```html
<div class="shopping-cart" data-testid="cart">
  <div class="cart-header">
    <h2>Корзина покупок</h2>
    <span class="item-count">3 товара</span>
  </div>
  
  <div class="cart-items">
    <div class="cart-item" data-product-id="123">
      <img src="/product1.jpg" alt="Ноутбук">
      <div class="item-details">
        <h3>Ноутбук Dell</h3>
        <span class="price">45000 ₽</span>
        <div class="quantity-controls">
          <button class="btn-decrease">-</button>
          <input type="number" value="1" min="1">
          <button class="btn-increase">+</button>
        </div>
      </div>
      <button class="remove-item" data-action="remove">×</button>
    </div>
  </div>
  
  <div class="cart-footer">
    <div class="total">Итого: 45000 ₽</div>
    <button class="checkout-btn" disabled>Оформить заказ</button>
  </div>
</div>
```

**Задачи:**
1. Найти корзину
2. Найти количество товаров  
3. Найти первый товар
4. Найти кнопку увеличения количества
5. Найти кнопку удаления товара
6. Найти общую сумму

## 🚀 Что дальше?

В следующем уроке мы изучим различные способы взаимодействия с найденными элементами: клики, ввод текста, drag&drop и многое другое.

---

💡 **Совет**: Лучший селектор - тот, который понятен через полгода и не сломается при изменении дизайна!