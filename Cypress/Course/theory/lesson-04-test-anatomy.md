# Урок 4: Анатомия теста в Cypress

## 🎯 Цели урока
- Понять жизненный цикл теста
- Изучить хуки (hooks) и их применение
- Научиться группировать тесты
- Понять асинхронность в Cypress

## 📖 Структура теста

### Полная анатомия теста

```javascript
// 1. Импорты (если нужны)
/// <reference types="cypress" />

// 2. Глобальные переменные
let userData

// 3. Основная группа тестов
describe('Функциональность логина', () => {
  
  // 4. Хуки жизненного цикла
  before(() => {
    // Выполняется ОДИН раз перед ВСЕМИ тестами в describe
    cy.fixture('users').then((data) => {
      userData = data
    })
  })

  beforeEach(() => {
    // Выполняется перед КАЖДЫМ тестом
    cy.visit('/login')
    cy.clearCookies()
  })

  afterEach(() => {
    // Выполняется после КАЖДОГО теста
    cy.clearLocalStorage()
  })

  after(() => {
    // Выполняется ОДИН раз после ВСЕХ тестов в describe
    cy.task('cleanDatabase')
  })

  // 5. Вложенная группа тестов
  describe('Валидация формы', () => {
    it('Показывает ошибку при пустом email', () => {
      // Тело теста
    })
  })

  // 6. Отдельные тесты
  it('Успешный логин с валидными данными', () => {
    // Arrange - подготовка
    const { email, password } = userData.validUser
    
    // Act - действие
    cy.get('#email').type(email)
    cy.get('#password').type(password)
    cy.get('#login-btn').click()
    
    // Assert - проверка
    cy.url().should('include', '/dashboard')
    cy.get('.welcome-message').should('contain', 'Добро пожаловать')
  })
})
```

## 🔄 Жизненный цикл теста

### Порядок выполнения:

```
describe('Группа тестов', () => {
  before()       // ← 1. Один раз в начале
  
  beforeEach()   // ← 2. Перед каждым тестом
  it('тест 1')   // ← 3. Первый тест
  afterEach()    // ← 4. После каждого теста
  
  beforeEach()   // ← 5. Перед каждым тестом
  it('тест 2')   // ← 6. Второй тест
  afterEach()    // ← 7. После каждого теста
  
  after()        // ← 8. Один раз в конце
})
```

### Практический пример:

```javascript
describe('Интернет-магазин', () => {
  let testProduct

  before(() => {
    // Загружаем тестовые данные один раз
    cy.fixture('products').then((data) => {
      testProduct = data.electronics.laptop
    })
  })

  beforeEach(() => {
    // Очищаем состояние перед каждым тестом
    cy.clearCookies()
    cy.clearLocalStorage()
    cy.visit('/')
    
    // Логинимся (если нужно для всех тестов)
    cy.login('customer@example.com', 'password123')
  })

  afterEach(() => {
    // Очищаем корзину после каждого теста
    cy.clearCart()
  })

  after(() => {
    // Очищаем базу данных после всех тестов
    cy.task('resetDatabase')
  })

  it('Добавление товара в корзину', () => {
    cy.searchProduct(testProduct.name)
    cy.addToCart()
    cy.verifyCartCount(1)
  })

  it('Удаление товара из корзины', () => {
    cy.searchProduct(testProduct.name)
    cy.addToCart()
    cy.removeFromCart()
    cy.verifyCartCount(0)
  })
})
```

## 📊 Группировка тестов

### Вложенные describe блоки:

```javascript
describe('Система аутентификации', () => {
  describe('Регистрация', () => {
    describe('Валидация email', () => {
      it('Принимает валидный email', () => {})
      it('Отклоняет невалидный email', () => {})
    })

    describe('Валидация пароля', () => {
      it('Требует минимум 8 символов', () => {})
      it('Требует специальные символы', () => {})
    })
  })

  describe('Авторизация', () => {
    it('Входит с валидными данными', () => {})
    it('Блокирует невалидные данные', () => {})
  })
})
```

### Организация по функциональности:

```javascript
// Файл: cypress/e2e/auth/registration.cy.js
describe('Регистрация пользователей', () => {
  // Тесты регистрации
})

// Файл: cypress/e2e/auth/login.cy.js  
describe('Авторизация пользователей', () => {
  // Тесты авторизации
})

// Файл: cypress/e2e/shop/cart.cy.js
describe('Корзина покупок', () => {
  // Тесты корзины
})
```

## ⏱️ Асинхронность в Cypress

### Автоматическое ожидание:

```javascript
describe('Асинхронные операции', () => {
  it('Ожидает загрузки данных', () => {
    cy.visit('/users')
    
    // Cypress автоматически ждет появления элемента
    cy.get('[data-testid="user-list"]').should('be.visible')
    
    // Ждет, пока список загрузится
    cy.get('[data-testid="user-item"]')
      .should('have.length.greaterThan', 0)
  })

  it('Работает с AJAX запросами', () => {
    // Перехватываем запрос
    cy.intercept('GET', '/api/users*').as('getUsers')
    
    cy.visit('/users')
    cy.get('[data-testid="load-users"]').click()
    
    // Ждем завершения запроса
    cy.wait('@getUsers').then((interception) => {
      expect(interception.response.statusCode).to.eq(200)
    })
    
    // Проверяем результат
    cy.get('[data-testid="user-list"]').should('be.visible')
  })
})
```

### Кастомные ожидания:

```javascript
describe('Кастомные ожидания', () => {
  it('Ожидает выполнения условия', () => {
    cy.visit('/dashboard')
    
    // Ждем, пока счетчик достигнет определенного значения
    cy.get('#counter').should(($counter) => {
      const count = parseInt($counter.text())
      expect(count).to.be.greaterThan(10)
    })
    
    // Ждем изменения URL
    cy.url().should('not.include', '/loading')
  })

  it('Использует retry-until паттерн', () => {
    const checkStatus = () => {
      return cy.request('/api/status')
        .then((response) => {
          if (response.body.status !== 'ready') {
            throw new Error('Not ready yet')
          }
          return response
        })
    }

    // Повторяем запрос пока статус не станет 'ready'
    cy.wrap(null).then(() => checkStatus())
  })
})
```

## 🏗️ Паттерн AAA (Arrange-Act-Assert)

### Структурируйте тесты по паттерну AAA:

```javascript
describe('Корзина покупок', () => {
  it('Добавляет товар в корзину', () => {
    // ARRANGE - Подготовка
    cy.fixture('products').then((products) => {
      const product = products.laptop
      
      cy.visit('/')
      cy.login('user@example.com', 'password')
      cy.visit(`/product/${product.id}`)
    })

    // ACT - Действие
    cy.get('[data-testid="add-to-cart"]').click()

    // ASSERT - Проверка
    cy.get('[data-testid="cart-badge"]')
      .should('contain', '1')
    
    cy.get('[data-testid="success-message"]')
      .should('contain', 'Товар добавлен в корзину')
  })
})
```

## 🎯 Лучшие практики

### 1. Понятные названия:

```javascript
// ❌ Плохо
describe('Tests', () => {
  it('works', () => {})
})

// ✅ Хорошо
describe('Форма обратной связи', () => {
  it('Отправляет сообщение с валидными данными', () => {})
  it('Показывает ошибку при пустом поле email', () => {})
})
```

### 2. Независимые тесты:

```javascript
// ❌ Плохо - тесты зависят друг от друга
describe('Пользователь', () => {
  it('Регистрируется', () => {
    // создает пользователя
  })
  
  it('Входит в систему', () => {
    // предполагает, что пользователь уже создан
  })
})

// ✅ Хорошо - тесты независимы
describe('Пользователь', () => {
  beforeEach(() => {
    cy.createUser() // создаем пользователя для каждого теста
  })
  
  it('Может войти в систему', () => {
    // тест полностью самостоятельный
  })
})
```

### 3. Правильное использование хуков:

```javascript
describe('Админ панель', () => {
  before(() => {
    // Тяжелые операции, которые не меняются между тестами
    cy.task('seedDatabase')
  })

  beforeEach(() => {
    // Быстрые операции для каждого теста
    cy.visit('/admin')
    cy.loginAsAdmin()
  })

  afterEach(() => {
    // Очистка после каждого теста
    cy.clearCookies()
  })
})
```

## 📝 Практические задания

### Задание 1: Организация тестов
Создайте файл с правильной структурой для тестирования интернет-магазина:

```javascript
describe('Интернет-магазин ShopCypress', () => {
  // TODO: Добавить хуки для подготовки данных
  // TODO: Создать группу тестов для каталога товаров
  // TODO: Создать группу тестов для корзины
  // TODO: Создать группу тестов для оформления заказа
})
```

### Задание 2: Асинхронные операции
Создайте тест, который:
1. Загружает страницу с долгой загрузкой
2. Ожидает появления контента
3. Проверяет результат

## 🎓 Ключевые выводы

- ✅ Тесты должны быть независимыми
- ✅ Используйте хуки для подготовки данных
- ✅ Группируйте связанные тесты
- ✅ Следуйте паттерну AAA
- ✅ Давайте понятные названия
- ✅ Cypress автоматически ждет элементы

## 🚀 Что дальше?

В следующем уроке мы изучим селекторы подробнее и научимся находить элементы в сложных случаях.

---

💡 **Совет**: Хорошая структура тестов экономит время на поддержку и делает тесты понятными для команды!