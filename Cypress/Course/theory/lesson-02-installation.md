# Урок 2: Установка и настройка Cypress

## 🎯 Цели урока
- Установить Node.js и npm
- Создать новый проект с Cypress
- Настроить базовую конфигурацию
- Запустить первый тест

## 📋 Требования

### Что нужно установить:
1. **Node.js** (версия 18 или выше)
2. **npm** (устанавливается с Node.js)
3. **Редактор кода** (VS Code рекомендуется)

## 🔧 Пошаговая установка

### Шаг 1: Установка Node.js

1. Перейдите на https://nodejs.org/
2. Скачайте LTS версию (рекомендуемая)
3. Установите с настройками по умолчанию
4. Проверьте установку в терминале:

```bash
node --version
npm --version
```

Должны увидеть что-то вроде:
```
v18.19.0
9.2.0
```

### Шаг 2: Создание проекта

1. Создайте папку для проекта:
```bash
mkdir my-cypress-project
cd my-cypress-project
```

2. Инициализируйте npm проект:
```bash
npm init -y
```

Это создаст файл `package.json`:
```json
{
  "name": "my-cypress-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
```

### Шаг 3: Установка Cypress

Установите Cypress как зависимость для разработки:

```bash
npm install --save-dev cypress
```

Это может занять несколько минут, так как Cypress скачивает браузер.

### Шаг 4: Первый запуск

Откройте Cypress:
```bash
npx cypress open
```

При первом запуске Cypress:
1. Создаст папку `cypress` с примерами
2. Покажет мастер настройки
3. Предложит выбрать тип тестирования (выберите "E2E Testing")

## 📁 Структура проекта

После инициализации у вас будет такая структура:

```
my-cypress-project/
├── cypress/
│   ├── e2e/           # Ваши тесты
│   ├── fixtures/      # Тестовые данные
│   ├── support/       # Вспомогательные файлы
│   └── downloads/     # Скачанные файлы
├── cypress.config.js  # Настройки Cypress
├── package.json       # Зависимости проекта
└── node_modules/      # Установленные пакеты
```

## ⚙️ Базовая конфигурация

### cypress.config.js

Это главный файл конфигурации. Базовая настройка:

```javascript
const { defineConfig } = require('cypress')

module.exports = defineConfig({
  e2e: {
    // Базовый URL вашего приложения
    baseUrl: 'http://localhost:3000',
    
    // Размер окна браузера
    viewportWidth: 1280,
    viewportHeight: 720,
    
    // Таймауты
    defaultCommandTimeout: 10000,
    requestTimeout: 10000,
    responseTimeout: 10000,
    
    // Видео и скриншоты
    video: true,
    screenshotOnRunFailure: true,
    
    // Папка с тестами
    specPattern: 'cypress/e2e/**/*.cy.{js,jsx,ts,tsx}',
    
    setupNodeEvents(on, config) {
      // здесь можно добавить плагины
    },
  },
})
```

### package.json скрипты

Добавьте удобные команды в `package.json`:

```json
{
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox"
  }
}
```

Теперь можно запускать:
```bash
npm run cy:open    # Открыть Cypress UI
npm run cy:run     # Запустить все тесты в headless режиме
```

## 🎮 Cypress Test Runner

### Режимы работы:

1. **Interactive Mode (Интерактивный)**
   ```bash
   npx cypress open
   ```
   - Открывает GUI
   - Можно видеть выполнение тестов
   - Удобно для разработки и отладки

2. **Headless Mode (Без GUI)**
   ```bash
   npx cypress run
   ```
   - Запускает все тесты автоматически
   - Генерирует отчеты
   - Используется в CI/CD

### Интерфейс Test Runner

Когда откроете `cypress open`, увидите:
- **Список тестов** - все ваши .cy.js файлы
- **Браузеры** - Chrome, Firefox, Edge, Electron
- **Настройки** - конфигурация проекта

## 📝 Первый тест

Создайте файл `cypress/e2e/first-test.cy.js`:

```javascript
describe('Мой первый тест', () => {
  it('Проверяет, что Google доступен', () => {
    // Открываем сайт
    cy.visit('https://www.google.com')
    
    // Проверяем, что заголовок содержит "Google"
    cy.title().should('contain', 'Google')
    
    // Проверяем наличие поля поиска
    cy.get('[name="q"]').should('be.visible')
  })
  
  it('Выполняет поиск', () => {
    cy.visit('https://www.google.com')
    
    // Вводим текст в поиск
    cy.get('[name="q"]').type('Cypress testing{enter}')
    
    // Проверяем, что появились результаты
    cy.get('#search').should('be.visible')
  })
})
```

Запустите тест:
```bash
npm run cy:open
```

Выберите ваш тест и смотрите, как он выполняется!

## 🔍 Полезные команды

### Проверка установки:
```bash
npx cypress verify
npx cypress version
```

### Очистка кеша:
```bash
npx cypress cache clear
```

### Получение информации:
```bash
npx cypress info
```

## ⚠️ Частые проблемы

### 1. Ошибка "command not found"
**Решение**: Убедитесь, что Node.js установлен и добавлен в PATH

### 2. Cypress не открывается
**Решение**: 
```bash
npx cypress cache clear
npx cypress install
```

### 3. Тесты падают с timeout
**Решение**: Увеличьте таймауты в `cypress.config.js`

## 🎯 Проверь себя

### Практическое задание:

1. Создайте новый проект
2. Установите Cypress
3. Настройте базовую конфигурацию
4. Создайте тест, который:
   - Открывает любой сайт
   - Проверяет наличие какого-то элемента
   - Выполняет простое действие (клик, ввод текста)

### Ожидаемый результат:
- Проект создан и настроен
- Тест успешно выполняется
- Вы понимаете структуру проекта

## 🚀 Что дальше?

В следующем уроке мы разберем анатомию теста и напишем более сложные сценарии.

---

💡 **Совет**: Сохраните этот урок как шпаргалку по установке - он пригодится при создании новых проектов!