# Урок 6: Взаимодействие с элементами

## 🎯 Цели урока
- Изучить все способы взаимодействия с элементами
- Научиться работать с формами, кнопками, ссылками
- Понять особенности различных типов элементов
- Освоить продвинутые техники взаимодействия

## 📖 Основы взаимодействия

### Принцип работы Cypress

Cypress имитирует действия реального пользователя:

```javascript
// Пользователь видит кнопку → кликает → происходит действие
cy.get('#login-btn')    // Найти кнопку
  .should('be.visible') // Убедиться что видима
  .click()              // Кликнуть

// Пользователь видит поле → фокусируется → вводит текст
cy.get('#email')        // Найти поле
  .should('be.enabled') // Убедиться что активно
  .clear()              // Очистить
  .type('test@test.com') // Ввести текст
```

## 🖱️ Клики и нажатия

### Простые клики

```javascript
// Обычный клик
cy.get('#button').click()

// Клик с опциями
cy.get('#button').click({
  force: true,          // принудительный клик (даже если элемент скрыт)
  multiple: true,       // клик по нескольким элементам
  timeout: 10000        // таймаут ожидания
})

// Клик по координатам
cy.get('#canvas').click(50, 100)  // x=50, y=100 от левого верхнего угла
cy.get('#element').click('topLeft') // углы: topLeft, topRight, bottomLeft, bottomRight
```

### Специальные клики

```javascript
// Двойной клик
cy.get('#file-item').dblclick()

// Правый клик (контекстное меню)
cy.get('#context-area').rightclick()

// Клик с модификаторами
cy.get('#link').click({ ctrlKey: true })   // Ctrl+Click
cy.get('#link').click({ shiftKey: true }) // Shift+Click
cy.get('#link').click({ metaKey: true })  // Cmd+Click (Mac) / Win+Click
```

### Практический пример:

```javascript
describe('Различные типы кликов', () => {
  it('Демонстрирует разные способы кликов', () => {
    cy.visit('/interactive-page')

    // Обычная кнопка
    cy.get('[data-testid="normal-button"]').click()
    cy.get('.success-message').should('contain', 'Кнопка нажата')

    // Скрытая кнопка (принудительный клик)
    cy.get('[data-testid="hidden-button"]').click({ force: true })

    // Двойной клик для редактирования
    cy.get('.editable-text').dblclick()
    cy.get('.edit-input').should('be.focused').type('Новый текст{enter}')

    // Клик по координатам в canvas
    cy.get('#drawing-canvas').click(100, 150)
  })
})
```

## ⌨️ Ввод текста и клавиатура

### Основной ввод текста

```javascript
// Простой ввод
cy.get('#input').type('Привет мир!')

// Очистка и ввод
cy.get('#input').clear().type('Новый текст')

// Ввод с опциями
cy.get('#input').type('медленный текст', {
  delay: 100,           // задержка между символами (мс)
  force: true,          // принудительный ввод
  parseSpecialCharSequences: false // отключить спец символы
})
```

### Специальные клавиши

```javascript
// Клавиши навигации
cy.get('#input').type('{enter}')      // Enter
cy.get('#input').type('{tab}')        // Tab
cy.get('#input').type('{esc}')        // Escape
cy.get('#input').type('{backspace}')  // Backspace
cy.get('#input').type('{del}')        // Delete

// Стрелки
cy.get('#input').type('{leftarrow}')  // ←
cy.get('#input').type('{rightarrow}') // →
cy.get('#input').type('{uparrow}')    // ↑
cy.get('#input').type('{downarrow}')  // ↓

// Модификаторы
cy.get('#input').type('{ctrl+a}')     // Ctrl+A (выделить все)
cy.get('#input').type('{ctrl+c}')     // Ctrl+C (копировать)
cy.get('#input').type('{ctrl+v}')     // Ctrl+V (вставить)
cy.get('#input').type('{shift+end}')  // Shift+End
```

### Работа с текстовыми полями

```javascript
describe('Ввод текста', () => {
  it('Тестирует различные поля ввода', () => {
    cy.visit('/forms')

    // Обычное текстовое поле
    cy.get('#firstName')
      .clear()
      .type('Иван')
      .should('have.value', 'Иван')

    // Email поле с валидацией
    cy.get('#email')
      .type('неправильный-email')
      .blur() // убрать фокус для срабатывания валидации
    cy.get('.error-message').should('contain', 'Неверный формат email')

    cy.get('#email')
      .clear()
      .type('ivan@example.com')
      .should('have.value', 'ivan@example.com')

    // Поле с маской телефона
    cy.get('#phone')
      .type('9001234567')
      .should('have.value', '+7 (900) 123-45-67') // автоформатирование

    // Многострочное текстовое поле
    cy.get('#comments')
      .type('Первая строка{enter}Вторая строка{enter}Третья строка')
      .should('contain.value', 'Первая строка\nВторая строка\nТретья строка')
  })
})
```

## 🎛️ Работа с формами

### Чекбоксы и радиокнопки

```javascript
// Чекбоксы
cy.get('#agree').check()              // отметить
cy.get('#agree').uncheck()            // снять отметку
cy.get('#agree').should('be.checked') // проверить состояние

// Множественные чекбоксы
cy.get('[type="checkbox"]').check(['option1', 'option3'])

// Радиокнопки
cy.get('#gender-male').check()
cy.get('#gender-female').should('not.be.checked')

// Проверка значения радиокнопки
cy.get('[name="subscription"]:checked').should('have.value', 'premium')
```

### Выпадающие списки (Select)

```javascript
// Выбор по тексту
cy.get('#country').select('Россия')

// Выбор по значению
cy.get('#country').select('RU')

// Выбор по индексу
cy.get('#country').select(1)

// Множественный выбор
cy.get('#skills').select(['JavaScript', 'Python', 'Testing'])

// Проверка выбранного значения
cy.get('#country').should('have.value', 'RU')
cy.get('#skills').should('have.value', ['js', 'python', 'testing'])
```

### Сложные элементы форм

```javascript
describe('Сложная форма', () => {
  it('Заполняет форму со всеми типами элементов', () => {
    cy.visit('/registration-form')

    // Текстовые поля
    cy.get('#firstName').type('Анна')
    cy.get('#lastName').type('Смирнова')
    
    // Дата рождения
    cy.get('#birthDate').type('1985-03-15')
    
    // Выпадающий список
    cy.get('#education').select('Высшее')
    
    // Радиокнопки
    cy.get('#gender-female').check()
    
    // Чекбоксы
    cy.get('#interests').within(() => {
      cy.get('[value="sports"]').check()
      cy.get('[value="music"]').check()
    })
    
    // Файл
    cy.get('#avatar').selectFile('cypress/fixtures/avatar.jpg')
    
    // Диапазон (range)
    cy.get('#experience').invoke('val', 5).trigger('input')
    
    // Цвет
    cy.get('#favorite-color').invoke('val', '#ff0000').trigger('input')
    
    // Согласие с условиями
    cy.get('#terms').check()
    
    // Отправка формы
    cy.get('#submit-form').click()
    
    // Проверка результата
    cy.url().should('include', '/success')
    cy.get('.success-message').should('contain', 'Регистрация успешна')
  })
})
```

## 🎯 Фокус и навигация

### Управление фокусом

```javascript
// Установить фокус
cy.get('#input').focus()

// Убрать фокус
cy.get('#input').blur()

// Проверка фокуса
cy.get('#input').should('be.focused')
cy.get('#input').should('not.be.focused')

// Навигация по Tab
cy.get('#first-input').focus().tab()  // переход к следующему элементу
cy.focused().should('have.id', 'second-input')
```

### Прокрутка страницы

```javascript
// Прокрутка к элементу
cy.get('#footer').scrollIntoView()

// Прокрутка страницы
cy.scrollTo(0, 500)        // к координатам
cy.scrollTo('bottom')      // вниз
cy.scrollTo('top')         // вверх
cy.scrollTo('center')      // к центру

// Прокрутка в контейнере
cy.get('#scrollable-div').scrollTo('bottom')
```

## 🎪 Продвинутое взаимодействие

### Drag & Drop (Перетаскивание)

```javascript
// Простое перетаскивание
cy.get('#draggable').drag('#drop-zone')

// Перетаскивание с координатами
cy.get('#draggable')
  .trigger('mousedown', { button: 0 })
  .trigger('mousemove', { clientX: 300, clientY: 200 })
  .trigger('mouseup')

// Использование dataTransfer для HTML5 drag&drop
cy.get('#file-item').trigger('dragstart')
cy.get('#drop-area').trigger('drop')
```

### Hover (Наведение мыши)

```javascript
// Наведение мыши
cy.get('#menu-item').trigger('mouseover')
cy.get('#submenu').should('be.visible')

// Сложное взаимодействие с hover
cy.get('#interactive-element')
  .trigger('mouseenter')
  .wait(500) // ждем анимацию
  .get('.tooltip').should('be.visible')
  .trigger('mouseleave')
  .get('.tooltip').should('not.exist')
```

### Работа с файлами

```javascript
// Загрузка файла
cy.get('#file-upload').selectFile('cypress/fixtures/test-file.pdf')

// Множественная загрузка
cy.get('#multiple-files').selectFile([
  'cypress/fixtures/file1.jpg',
  'cypress/fixtures/file2.pdf'
])

// Создание файла на лету
cy.get('#text-upload').selectFile({
  contents: Cypress.Buffer.from('Содержимое файла'),
  fileName: 'test.txt',
  mimeType: 'text/plain'
})

// Drag & drop файла
cy.get('#drop-zone').selectFile('cypress/fixtures/image.png', {
  action: 'drag-drop'
})
```

## ⚠️ Обработка особых случаев

### Элементы, которые не сразу доступны

```javascript
// Ожидание появления элемента
cy.get('#loading-button', { timeout: 10000 }).click()

// Принудительное взаимодействие
cy.get('#hidden-element').click({ force: true })

// Ожидание изменения состояния
cy.get('#submit-btn')
  .should('not.be.disabled')
  .click()
```

### Модальные окна и overlay

```javascript
describe('Модальные окна', () => {
  it('Работает с модальными окнами', () => {
    cy.visit('/modal-demo')

    // Открытие модального окна
    cy.get('#open-modal').click()
    cy.get('.modal').should('be.visible')

    // Взаимодействие внутри модального окна
    cy.get('.modal').within(() => {
      cy.get('#modal-input').type('Тест')
      cy.get('#modal-submit').click()
    })

    // Закрытие модального окна
    cy.get('.modal-backdrop').click() // клик по фону
    cy.get('.modal').should('not.exist')
  })
})
```

### Iframe и внешний контент

```javascript
// Работа с iframe
cy.get('#payment-iframe')
  .its('0.contentDocument')
  .its('body')
  .should('not.be.empty')
  .then(cy.wrap)
  .within(() => {
    cy.get('#card-number').type('4111111111111111')
    cy.get('#submit-payment').click()
  })
```

## 🎨 Продвинутые техники

### Цепочки команд (Command Chaining)

```javascript
// Цепочка действий
cy.get('#search-input')
  .clear()                          // очистить
  .type('Cypress testing')          // ввести
  .should('have.value', 'Cypress testing') // проверить
  .type('{enter}')                  // нажать Enter
  .should('not.be.focused')         // фокус должен уйти

// Условная цепочка
cy.get('#optional-field').then(($field) => {
  if ($field.is(':visible')) {
    cy.wrap($field).type('Дополнительная информация')
  }
})
```

### Пользовательские действия

```javascript
// Кастомная команда для тройного клика
Cypress.Commands.add('tripleClick', (selector) => {
  cy.get(selector)
    .click()
    .click()
    .click()
})

// Использование
cy.tripleClick('#editable-text')
```

## 📝 Практическое задание

Создайте тесты для интерактивной веб-страницы с различными элементами:

```html
<!-- Тестовая страница для практики -->
<form id="interactive-form">
  <input type="text" id="username" placeholder="Имя пользователя">
  <input type="password" id="password" placeholder="Пароль">
  <input type="email" id="email" placeholder="Email">
  <select id="role">
    <option value="">Выберите роль</option>
    <option value="admin">Администратор</option>
    <option value="user">Пользователь</option>
  </select>
  <input type="checkbox" id="remember"> Запомнить меня
  <button type="submit" id="login">Войти</button>
</form>
```

**Задания:**
1. Заполните все поля формы
2. Проверьте состояния чекбокса
3. Протестируйте навигацию по Tab
4. Добавьте проверки валидации
5. Протестируйте отправку формы

## 🎯 Ключевые выводы

- ✅ Всегда проверяйте состояние элемента перед взаимодействием
- ✅ Используйте `.should()` для проверки результата
- ✅ Помните про асинхронность - Cypress ждет элементы автоматически
- ✅ Применяйте `{ force: true }` осторожно - только когда необходимо
- ✅ Группируйте логически связанные действия

## 🚀 Что дальше?

В следующем уроке мы изучим assertions (утверждения) - как правильно проверять результаты наших взаимодействий.

---

💡 **Совет**: Лучший тест - тот, который максимально точно имитирует поведение реального пользователя!