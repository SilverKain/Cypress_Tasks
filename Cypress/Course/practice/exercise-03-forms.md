# Практическое задание 3: Работа с формами

## 🎯 Цель
Научиться тестировать различные типы форм и их валидацию с использованием продвинутых селекторов.

## 📋 Предварительные условия
- Выполнены задания 1 и 2
- Изучены уроки 4-6 курса
- Понимание основных селекторов и взаимодействий

## 🎮 Задания

### Задание 3.1: Форма регистрации

Создайте файл `cypress/e2e/exercises/03-1-registration-form.cy.js`:

```javascript
describe('Форма регистрации', () => {
  beforeEach(() => {
    cy.visit('/commands/actions')
  })

  it('Заполняет все поля формы регистрации', () => {
    // TODO: Найти и заполнить email поле
    // TODO: Найти поле с атрибутом data-cy="action-canvas" и кликнуть по нему
    // TODO: Заполнить текстовое поле (action-email)
    // TODO: Очистить поле и заполнить новым значением
    // TODO: Проверить, что значение сохранилось
  })

  it('Тестирует валидацию email поля', () => {
    // TODO: Ввести невалидный email
    // TODO: Убрать фокус с поля (blur)
    // TODO: Проверить появление ошибки валидации (если есть)
    // TODO: Ввести валидный email
    // TODO: Проверить исчезновение ошибки
  })

  it('Работает с чекбоксами и радиокнопками', () => {
    // TODO: Найти чекбоксы на странице
    // TODO: Отметить несколько чекбоксов
    // TODO: Проверить их состояние
    // TODO: Снять отметки с некоторых
    // TODO: Работа с радиокнопками (если есть на странице)
  })
})
```

**Ваша задача:** Реализовать все TODO комментарии.

### Задание 3.2: Сложные селекторы

Создайте файл `cypress/e2e/exercises/03-2-complex-selectors.cy.js`:

```javascript
describe('Сложные селекторы', () => {
  beforeEach(() => {
    cy.visit('/commands/querying')
  })

  it('Использует различные типы селекторов', () => {
    // TODO: Найти элемент по ID
    // TODO: Найти элементы по классу  
    // TODO: Найти элементы по атрибуту data-cy
    // TODO: Найти элементы по комбинированному селектору
    // TODO: Использовать селекторы потомков (пробел)
    // TODO: Использовать селекторы прямых детей (>)
  })

  it('Фильтрует и находит элементы по позиции', () => {
    // TODO: Найти первый элемент в списке
    // TODO: Найти последний элемент
    // TODO: Найти элемент по индексу (eq)
    // TODO: Отфильтровать видимые элементы
    // TODO: Найти элементы, содержащие определенный текст
  })

  it('Работает с иерархией элементов', () => {
    // TODO: Использовать .within() для поиска внутри контейнера
    // TODO: Найти родительский элемент (.parent())
    // TODO: Найти соседние элементы (.siblings())
    // TODO: Найти ближайшего предка (.closest())
  })
})
```

### Задание 3.3: Продвинутые взаимодействия

Создайте файл `cypress/e2e/exercises/03-3-advanced-interactions.cy.js`:

```javascript
describe('Продвинутые взаимодействия', () => {
  beforeEach(() => {
    cy.visit('/commands/actions')
  })

  it('Тестирует различные типы кликов', () => {
    // TODO: Обычный клик по кнопке
    // TODO: Двойной клик (если есть элементы поддерживающие dblclick)
    // TODO: Клик с force: true
    // TODO: Клик по координатам
  })

  it('Работает с клавиатурой и специальными клавишами', () => {
    // TODO: Ввести текст с использованием специальных клавиш
    // TODO: Использовать {enter}, {tab}, {backspace}
    // TODO: Комбинации клавиш (Ctrl+A, Ctrl+C, Ctrl+V)
    // TODO: Навигация стрелками (если применимо)
  })

  it('Тестирует фокус и навигацию', () => {
    // TODO: Установить фокус на элемент
    // TODO: Проверить состояние фокуса
    // TODO: Убрать фокус (blur)
    // TODO: Навигация по Tab между элементами
  })
})
```

### Задание 3.4: Валидация форм

Создайте файл `cypress/e2e/exercises/03-4-form-validation.cy.js`:

```javascript
describe('Валидация форм', () => {
  beforeEach(() => {
    cy.visit('/commands/actions')
  })

  it('Проверяет валидацию обязательных полей', () => {
    // TODO: Попытаться отправить пустую форму
    // TODO: Проверить появление сообщений об ошибках
    // TODO: Заполнить поля по одному
    // TODO: Проверить исчезновение ошибок
  })

  it('Тестирует форматирование ввода', () => {
    // TODO: Ввести данные в поле email
    // TODO: Проверить формат введенных данных
    // TODO: Тестировать поля с маской (если есть)
    // TODO: Проверить автоформатирование
  })

  it('Проверяет ограничения полей', () => {
    // TODO: Тестировать максимальную длину ввода
    // TODO: Проверить ограничения на типы символов
    // TODO: Тестировать числовые поля (min/max)
  })
})
```

## 📚 Справочные материалы

### Полезные селекторы для форм:

```javascript
// Типы полей ввода
cy.get('[type="email"]')      // email поля
cy.get('[type="password"]')   // пароли
cy.get('[type="number"]')     // числовые поля
cy.get('[type="date"]')       // даты
cy.get('[type="checkbox"]')   // чекбоксы
cy.get('[type="radio"]')      // радиокнопки

// Селекторы состояния
cy.get(':checked')            // отмеченные элементы
cy.get(':disabled')           // заблокированные
cy.get(':enabled')            // активные
cy.get(':focus')              // в фокусе
cy.get(':visible')            // видимые

// Атрибуты валидации
cy.get('[required]')          // обязательные поля
cy.get(':valid')              // валидные поля
cy.get(':invalid')            // невалидные поля
```

### Команды для работы с формами:

```javascript
// Ввод и очистка
cy.get('#input').type('text')
cy.get('#input').clear()
cy.get('#input').type('{selectall}new text')

// Выбор значений
cy.get('#select').select('option')
cy.get('#checkbox').check()
cy.get('#checkbox').uncheck()

// Проверка значений
cy.get('#input').should('have.value', 'expected')
cy.get('#checkbox').should('be.checked')
cy.get('#input').should('be.focused')
```

### Работа с состояниями элементов:

```javascript
// Ожидание изменения состояния
cy.get('#submit').should('not.be.disabled')
cy.get('#loading').should('not.exist')

// Проверка видимости
cy.get('#error-message').should('be.visible')
cy.get('#success-message').should('not.be.visible')

// Проверка содержимого
cy.get('#result').should('contain', 'Success')
cy.get('#error').should('have.class', 'error-active')
```

## ✅ Критерии выполнения

### Обязательные требования:
- [ ] Созданы все 4 файла с тестами
- [ ] Реализованы все TODO комментарии  
- [ ] Использованы различные типы селекторов
- [ ] Протестированы разные способы взаимодействия
- [ ] Добавлены проверки состояний элементов

### Дополнительные задачи:
- [ ] Добавлены тесты для edge cases
- [ ] Использованы алиасы для переиспользования селекторов
- [ ] Добавлена обработка ошибок и таймаутов
- [ ] Тесты структурированы с помощью describe/context

## 🎯 Продвинутые задачи

### Бонусное задание: Создайте универсальную форму

Создайте файл `cypress/e2e/exercises/03-bonus-universal-form.cy.js`:

```javascript
describe('Универсальная форма', () => {
  // Создайте функцию для заполнения любой формы
  const fillForm = (formData) => {
    Object.entries(formData).forEach(([fieldName, value]) => {
      // TODO: Универсальная логика заполнения полей
    })
  }

  it('Заполняет форму используя объект данных', () => {
    const testData = {
      email: 'test@example.com',
      password: 'securepassword',
      rememberMe: true
    }

    cy.visit('/form-page')
    fillForm(testData)
    
    // TODO: Проверить, что все поля заполнены корректно
  })
})
```

## 🔍 Самопроверка

После выполнения ответьте на вопросы:

1. **В чем разница между `.get()` и `.find()`?**
2. **Когда использовать `{ force: true }` при клике?**
3. **Как проверить, что элемент имеет определенный CSS класс?**
4. **Что делает команда `.within()`?**
5. **Как дождаться изменения состояния элемента?**

<details>
<summary>🔍 Ответы</summary>

1. `.get()` ищет по всей странице, `.find()` ищет среди уже найденных элементов
2. Когда элемент перекрыт другим или временно скрыт, но функционально доступен
3. `cy.get('#element').should('have.class', 'my-class')`
4. Ограничивает область поиска внутри определенного контейнера
5. Использовать `.should()` с условием: `cy.get('#element').should('be.visible')`
</details>

## 🚨 Частые ошибки

### 1. Слишком общие селекторы
```javascript
// ❌ Плохо
cy.get('button').click()

// ✅ Хорошо  
cy.get('[data-testid="submit-button"]').click()
```

### 2. Не дожидаются изменения состояния
```javascript
// ❌ Плохо
cy.get('#submit').click()
cy.get('#result').should('contain', 'Success') // может сработать слишком рано

// ✅ Хорошо
cy.get('#submit').click()
cy.get('#loading').should('not.exist') // дожидаемся окончания загрузки
cy.get('#result').should('contain', 'Success')
```

### 3. Неправильная работа с асинхронностью
```javascript
// ❌ Плохо
cy.get('#dynamic-content').then(() => {
  cy.get('.item').should('have.length', 5) // может не найти элементы
})

// ✅ Хорошо
cy.get('#dynamic-content').should('be.visible')
cy.get('.item').should('have.length', 5)
```

## 🎓 Что изучили

- ✅ Продвинутые селекторы CSS и их комбинации
- ✅ Различные способы взаимодействия с элементами
- ✅ Работа с формами и их валидацией
- ✅ Техники отладки селекторов
- ✅ Обработка сложных состояний элементов

## 🚀 Следующий шаг

После выполнения этого задания переходите к заданию 4, где будем тестировать навигацию и сложные пользовательские сценарии.

---

💡 **Совет**: Практикуйте разные селекторы на реальных сайтах, используя DevTools браузера!