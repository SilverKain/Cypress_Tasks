# Практическое задание 4: Навигация и меню

## 🎯 Цель
Научиться тестировать навигацию, многоуровневые меню, хлебные крошки и сложные пользовательские сценарии.

## 📋 Предварительные условия
- Выполнены задания 1-3
- Изучены уроки 4-6 курса
- Понимание работы с селекторами и взаимодействиями

## 🎮 Задания

### Задание 4.1: Основная навигация

Создайте файл `cypress/e2e/exercises/04-1-basic-navigation.cy.js`:

```javascript
describe('Основная навигация сайта', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  it('Проверяет главное меню навигации', () => {
    // TODO: Найти главное меню навигации
    // TODO: Проверить наличие основных пунктов меню
    // TODO: Кликнуть по каждому пункту меню
    // TODO: Проверить, что URL изменился корректно
    // TODO: Проверить, что активный пункт подсвечен
  })

  it('Тестирует навигацию по хлебным крошкам', () => {
    // TODO: Перейти на глубокую страницу (например, /commands/actions)
    // TODO: Найти breadcrumb навигацию
    // TODO: Проверить правильность пути в хлебных крошках
    // TODO: Кликнуть по промежуточному элементу в breadcrumb
    // TODO: Проверить переход на соответствующую страницу
  })

  it('Проверяет кнопки назад/вперед браузера', () => {
    // TODO: Перейти на несколько страниц
    // TODO: Использовать cy.go('back') для возврата
    // TODO: Проверить, что вернулись на предыдущую страницу
    // TODO: Использовать cy.go('forward')
    // TODO: Проверить переход вперед
  })

  it('Тестирует поиск по сайту', () => {
    // TODO: Найти поле поиска (если есть)
    // TODO: Ввести поисковый запрос
    // TODO: Нажать Enter или кнопку поиска
    // TODO: Проверить результаты поиска
    // TODO: Проверить, что URL содержит параметры поиска
  })
})
```

### Задание 4.2: Многоуровневые меню

Создайте файл `cypress/e2e/exercises/04-2-dropdown-menus.cy.js`:

```javascript
describe('Многоуровневые меню', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  it('Работает с выпадающими меню', () => {
    // TODO: Найти элемент меню с подменю
    // TODO: Навести мышь (hover) для открытия подменю
    // TODO: Проверить, что подменю стало видимым
    // TODO: Кликнуть по элементу подменю
    // TODO: Проверить переход на нужную страницу
  })

  it('Тестирует клавиатурную навигацию в меню', () => {
    // TODO: Сфокусироваться на меню
    // TODO: Использовать стрелки для навигации
    // TODO: Использовать Enter для активации пункта
    // TODO: Использовать Escape для закрытия меню
  })

  it('Проверяет мобильное меню (гамбургер)', () => {
    // TODO: Установить мобильный viewport
    // TODO: Найти кнопку мобильного меню (гамбургер)
    // TODO: Кликнуть для открытия меню
    // TODO: Проверить, что меню открылось
    // TODO: Протестировать навигацию в мобильном меню
    // TODO: Закрыть меню
  })
})
```

### Задание 4.3: Сложные пользовательские сценарии

Создайте файл `cypress/e2e/exercises/04-3-user-scenarios.cy.js`:

```javascript
describe('Сложные пользовательские сценарии', () => {
  it('Сценарий: Новый пользователь изучает сайт', () => {
    // TODO: Зайти на главную страницу
    // TODO: Прочитать основную информацию
    // TODO: Перейти в раздел "О нас" или подобный
    // TODO: Вернуться на главную
    // TODO: Изучить примеры или документацию
    // TODO: Перейти к началу работы/установке
  })

  it('Сценарий: Опытный пользователь ищет конкретную информацию', () => {
    // TODO: Использовать поиск для нахождения конкретной команды
    // TODO: Перейти в раздел API документации
    // TODO: Найти конкретную команду (например, cy.get)
    // TODO: Прочитать примеры использования
    // TODO: Перейти к связанным командам
  })

  it('Сценарий: Пользователь следует обучающему материалу', () => {
    // TODO: Перейти в раздел примеров/туториалов
    // TODO: Выбрать первый пример
    // TODO: Последовательно пройти по шагам
    // TODO: Проверить, что каждый шаг работает корректно
    // TODO: Перейти к следующему примеру
  })
})
```

### Задание 4.4: Отзывчивая навигация

Создайте файл `cypress/e2e/exercises/04-4-responsive-navigation.cy.js`:

```javascript
describe('Отзывчивая навигация', () => {
  const viewports = [
    { name: 'mobile', width: 375, height: 667 },
    { name: 'tablet', width: 768, height: 1024 },
    { name: 'desktop', width: 1200, height: 800 }
  ]

  viewports.forEach((viewport) => {
    describe(`Навигация на ${viewport.name}`, () => {
      beforeEach(() => {
        cy.viewport(viewport.width, viewport.height)
        cy.visit('/')
      })

      it(`Проверяет основную навигацию на ${viewport.name}`, () => {
        // TODO: Проверить, что навигация адаптируется под размер экрана
        // TODO: Найти элементы навигации (могут отличаться для разных размеров)
        // TODO: Протестировать доступность всех основных разделов
        // TODO: Проверить удобство использования на данном размере экрана
      })

      it(`Тестирует взаимодействие с меню на ${viewport.name}`, () => {
        if (viewport.name === 'mobile') {
          // TODO: Тестировать мобильное меню (гамбургер)
        } else {
          // TODO: Тестировать десктопное меню (hover, клики)
        }
      })
    })
  })
})
```

## 📚 Справочные материалы

### Команды навигации:

```javascript
// Переходы
cy.visit('/page')                    // переход на страницу
cy.go('back')                        // назад
cy.go('forward')                     // вперед
cy.go(-2)                           // на 2 страницы назад
cy.reload()                         // обновить страницу

// Проверки URL
cy.url().should('include', '/path')
cy.url().should('eq', 'https://example.com/page')
cy.location('pathname').should('eq', '/commands')
cy.location('search').should('contain', 'q=test')

// Работа с ссылками
cy.get('a[href="/page"]').click()
cy.get('a').should('have.attr', 'href', '/expected')
```

### Работа с hover эффектами:

```javascript
// Наведение мыши
cy.get('#menu-item').trigger('mouseover')
cy.get('#submenu').should('be.visible')

// Комплексное hover взаимодействие
cy.get('#dropdown')
  .trigger('mouseenter')
  .get('.dropdown-menu')
  .should('be.visible')
  .within(() => {
    cy.get('a').contains('Submenu Item').click()
  })
```

### Viewport и отзывчивость:

```javascript
// Установка размера экрана
cy.viewport(1280, 720)              // конкретные размеры
cy.viewport('iphone-6')             // предустановки
cy.viewport('macbook-15')

// Проверка видимости на разных экранах
cy.get('#desktop-menu').should('be.visible')
cy.viewport('iphone-6')
cy.get('#desktop-menu').should('not.be.visible')
cy.get('#mobile-menu').should('be.visible')
```

### Работа с клавиатурой:

```javascript
// Навигация клавишами
cy.get('#menu').focus()
cy.focused().type('{downarrow}')    // стрелка вниз
cy.focused().type('{enter}')        // активация
cy.focused().type('{esc}')          // закрытие

// Tab навигация
cy.get('#first-link').focus()
cy.focused().tab()                  // переход к следующему элементу
```

## ✅ Критерии выполнения

### Обязательные требования:
- [ ] Созданы все 4 файла с тестами
- [ ] Протестирована основная навигация сайта
- [ ] Проверены переходы между страницами
- [ ] Тестированы hover эффекты (если есть)
- [ ] Проверена отзывчивость навигации

### Дополнительные задачи:
- [ ] Добавлены тесты клавиатурной навигации
- [ ] Протестированы хлебные крошки
- [ ] Проверена работа в разных браузерах
- [ ] Добавлены проверки доступности (accessibility)

## 🎯 Продвинутые техники

### Создание переиспользуемых команд:

```javascript
// В cypress/support/commands.js
Cypress.Commands.add('navigateToSection', (sectionName) => {
  cy.get('#main-nav')
    .contains(sectionName)
    .click()
  
  cy.url().should('include', sectionName.toLowerCase())
  cy.get('.page-title').should('contain', sectionName)
})

// Использование
cy.navigateToSection('Commands')
cy.navigateToSection('Examples')
```

### Проверка производительности навигации:

```javascript
it('Проверяет скорость навигации', () => {
  const startTime = performance.now()
  
  cy.visit('/').then(() => {
    const loadTime = performance.now() - startTime
    expect(loadTime).to.be.lessThan(3000) // менее 3 секунд
  })
})
```

### Тестирование Single Page Applications (SPA):

```javascript
it('Тестирует SPA навигацию', () => {
  cy.visit('/')
  
  // В SPA URL может изменяться без перезагрузки
  cy.get('#spa-link').click()
  cy.url().should('include', '/spa-page')
  
  // Проверяем, что содержимое изменилось
  cy.get('#main-content').should('contain', 'SPA Content')
  
  // Проверяем, что страница не перезагружалась
  cy.window().its('performance.navigation.type').should('eq', 0)
})
```

## 🔍 Самопроверка

После выполнения ответьте на вопросы:

1. **Как проверить, что пользователь находится на правильной странице?**
2. **В чем разница между `cy.visit()` и переходом по ссылке?**
3. **Как тестировать hover эффекты?**
4. **Что такое viewport и зачем его изменять?**
5. **Как обрабатывать навигацию в SPA?**

<details>
<summary>🔍 Ответы</summary>

1. Проверить URL, заголовок страницы, уникальные элементы на странице
2. `cy.visit()` - прямой переход, клик по ссылке имитирует действия пользователя
3. Использовать `trigger('mouseover')` или `trigger('mouseenter')`
4. Размер окна браузера, нужен для тестирования отзывчивого дизайна
5. Проверять изменения URL и DOM без перезагрузки страницы
</details>

## 🚨 Частые ошибки

### 1. Не дождались загрузки страницы
```javascript
// ❌ Плохо
cy.get('#menu-link').click()
cy.get('#page-content').should('be.visible') // может сработать слишком рано

// ✅ Хорошо
cy.get('#menu-link').click()
cy.url().should('include', '/new-page')
cy.get('#loading').should('not.exist')
cy.get('#page-content').should('be.visible')
```

### 2. Неправильная работа с hover
```javascript
// ❌ Плохо
cy.get('#menu').click() // сразу клик

// ✅ Хорошо
cy.get('#menu').trigger('mouseover')
cy.get('#submenu').should('be.visible')
cy.get('#submenu-item').click()
```

### 3. Забыли про мобильную версию
```javascript
// ❌ Плохо - тестируем только десктоп
cy.get('#desktop-menu').click()

// ✅ Хорошо - учитываем responsive
cy.viewport('iphone-6')
cy.get('#mobile-menu-button').click()
cy.get('#mobile-menu').should('be.visible')
```

## 🎓 Что изучили

- ✅ Тестирование различных типов навигации
- ✅ Работа с многоуровневыми меню
- ✅ Проверка отзывчивого дизайна
- ✅ Создание комплексных пользовательских сценариев
- ✅ Тестирование клавиатурной доступности

## 🚀 Следующий шаг

Поздравляем! Вы завершили неделю 2 курса. Переходите к неделе 3, где будем изучать assertions, фикстуры и работу с API.

---

💡 **Совет**: Хорошая навигация - это основа пользовательского опыта. Тщательно тестируйте все пути пользователя по сайту!