# Практическое задание 1: Настройка проекта

## 🎯 Цель
Создать рабочий проект с Cypress и настроить его для дальнейшего обучения.

## 📋 Что нужно сделать

### Часть 1: Подготовка окружения

1. **Создать новую папку проекта**
   ```bash
   mkdir cypress-learning
   cd cypress-learning
   ```

2. **Инициализировать npm проект**
   ```bash
   npm init -y
   ```

3. **Установить Cypress**
   ```bash
   npm install --save-dev cypress
   ```

4. **Добавить скрипты в package.json**
   ```json
   {
     "scripts": {
       "cy:open": "cypress open",
       "cy:run": "cypress run",
       "cy:run:headed": "cypress run --headed",
       "cy:run:chrome": "cypress run --browser chrome"
     }
   }
   ```

### Часть 2: Базовая конфигурация

1. **Создать файл cypress.config.js**
   ```javascript
   const { defineConfig } = require('cypress')

   module.exports = defineConfig({
     e2e: {
       baseUrl: 'https://example.cypress.io',
       viewportWidth: 1280,
       viewportHeight: 720,
       defaultCommandTimeout: 10000,
       video: false, // отключаем видео для экономии места
       screenshotOnRunFailure: true,
       
       setupNodeEvents(on, config) {
         // здесь будем добавлять плагины
       },
     },
   })
   ```

### Часть 3: Первые тесты

1. **Удалить примеры Cypress**
   ```bash
   rm -rf cypress/e2e/*
   ```

2. **Создать тест cypress/e2e/00-setup-test.cy.js**
   ```javascript
   describe('Проверка настройки', () => {
     it('Cypress запускается и может открыть страницу', () => {
       cy.visit('/')
       cy.contains('Kitchen Sink').should('be.visible')
     })

     it('Может найти и кликнуть элементы', () => {
       cy.visit('/')
       cy.contains('Commands').click()
       cy.url().should('include', '/commands')
     })

     it('Может вводить текст', () => {
       cy.visit('/commands/actions')
       cy.get('.action-email')
         .type('test@example.com')
         .should('have.value', 'test@example.com')
     })
   })
   ```

### Часть 4: Организация проекта

1. **Создать структуру папок**
   ```
   cypress/
   ├── e2e/
   │   ├── 00-setup-test.cy.js
   │   └── exercises/         # будущие упражнения
   ├── fixtures/
   │   └── test-data.json     # тестовые данные
   ├── support/
   │   ├── commands.js        # пользовательские команды
   │   └── e2e.js            # настройки для всех тестов
   └── downloads/             # скачанные файлы
   ```

2. **Создать файл с тестовыми данными cypress/fixtures/test-data.json**
   ```json
   {
     "users": {
       "validUser": {
         "email": "test@example.com",
         "password": "SecurePassword123",
         "firstName": "Тест",
         "lastName": "Пользователь"
       },
       "invalidUser": {
         "email": "invalid-email",
         "password": "123"
       }
     },
     "urls": {
       "login": "/login",
       "dashboard": "/dashboard",
       "profile": "/profile"
     }
   }
   ```

## ✅ Критерии выполнения

### Обязательные требования:
- [ ] Проект создан и инициализирован
- [ ] Cypress установлен как dev зависимость  
- [ ] Конфигурационный файл создан и настроен
- [ ] Все 3 теста из setup-test.cy.js проходят успешно
- [ ] Структура папок создана

### Дополнительные задачи:
- [ ] Добавлены полезные npm скрипты
- [ ] Создан файл с тестовыми данными
- [ ] Cypress Test Runner успешно открывается командой `npm run cy:open`
- [ ] Тесты можно запустить в headless режиме командой `npm run cy:run`

## 🔍 Проверка результата

### 1. Запустите интерактивный режим:
```bash
npm run cy:open
```
Должен открыться Cypress Test Runner со списком тестов.

### 2. Запустите тест setup-test.cy.js
Все тесты должны быть зелеными (PASSED).

### 3. Запустите headless режим:
```bash
npm run cy:run
```
Должен показать результат выполнения в консоли.

## 🚨 Частые ошибки

### Ошибка: "Cypress cannot be opened"
**Решение:**
```bash
npx cypress install
npx cypress verify
```

### Ошибка: "baseUrl is not defined"
**Решение:** Проверьте cypress.config.js, должен быть baseUrl.

### Ошибка: Tests not found
**Решение:** Убедитесь, что файл теста имеет расширение `.cy.js` и находится в папке `cypress/e2e/`.

### Ошибка: Cannot resolve module
**Решение:** 
```bash
npm install
```

## 📖 Ожидаемая структура проекта

По завершении у вас должна быть такая структура:

```
cypress-learning/
├── cypress/
│   ├── e2e/
│   │   ├── 00-setup-test.cy.js
│   │   └── exercises/
│   ├── fixtures/
│   │   └── test-data.json
│   ├── support/
│   │   ├── commands.js
│   │   └── e2e.js
│   └── downloads/
├── cypress.config.js
├── package.json
├── package-lock.json
└── node_modules/
```

## 🏆 Дополнительные вызовы

Если все получилось легко, попробуйте:

1. **Добавить больше браузеров**
   ```javascript
   // в cypress.config.js
   browsers: [
     {
       name: 'chrome',
       family: 'chromium',
       channel: 'stable'
     },
     {
       name: 'firefox',
       family: 'firefox'
     }
   ]
   ```

2. **Настроить папку для скриншотов**
   ```javascript
   screenshotsFolder: 'cypress/screenshots',
   videosFolder: 'cypress/videos',
   ```

3. **Добавить eslint для проверки кода**
   ```bash
   npm install --save-dev eslint eslint-plugin-cypress
   ```

## 🎓 Что изучили

- ✅ Как создать проект с нуля
- ✅ Как установить и настроить Cypress
- ✅ Основную структуру проекта
- ✅ Как писать и запускать простые тесты
- ✅ Как организовать файлы проекта

## 🚀 Готовность к следующему заданию

После успешного выполнения этого задания вы готовы к написанию более сложных тестов. В следующем упражнении мы будем тестировать формы и взаимодействие с элементами.

---

💡 **Совет**: Сохраните этот проект - он будет базой для всех следующих упражнений!