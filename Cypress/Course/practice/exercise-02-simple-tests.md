# Практическое задание 2: Простые тесты

## 🎯 Цель
Научиться писать базовые тесты для типичных веб-элементов: кнопки, ссылки, текст, изображения.

## 📋 Предварительные условия
- Выполнено задание 1 (проект настроен)
- Cypress запускается без ошибок

## 🎮 Задания

### Задание 2.1: Тестирование навигации

Создайте файл `cypress/e2e/exercises/02-1-navigation.cy.js`:

```javascript
describe('Навигация по сайту', () => {
  beforeEach(() => {
    cy.visit('/')
  })

  it('Главная страница загружается корректно', () => {
    // TODO: Проверить, что заголовок содержит "Kitchen Sink"
    // TODO: Проверить, что URL корректный
    // TODO: Проверить наличие навигационного меню
  })

  it('Переход в раздел Commands работает', () => {
    // TODO: Кликнуть по ссылке "Commands"
    // TODO: Проверить, что URL изменился на /commands
    // TODO: Проверить, что заголовок страницы изменился
  })

  it('Переход назад работает', () => {
    // TODO: Перейти в Commands
    // TODO: Вернуться назад через браузер
    // TODO: Проверить, что вернулись на главную
  })
})
```

**Ваша задача:** Реализовать все TODO комментарии.

<details>
<summary>💡 Подсказки</summary>

```javascript
// Проверка заголовка
cy.get('h1').should('contain', 'Kitchen Sink')

// Проверка URL
cy.url().should('include', '/')

// Клик по ссылке
cy.contains('Commands').click()

// Переход назад
cy.go('back')
```
</details>

### Задание 2.2: Работа с текстом

Создайте файл `cypress/e2e/exercises/02-2-text-content.cy.js`:

```javascript
describe('Работа с текстовым контентом', () => {
  it('Проверяет различные типы текста', () => {
    cy.visit('/commands/querying')

    // TODO: Найти элемент с ID "query-btn" и проверить его текст
    // TODO: Найти элемент с классом "query-list" и проверить, что он содержит несколько элементов
    // TODO: Проверить, что на странице есть текст "cy.get()"
    // TODO: Найти элемент по атрибуту data-cy="query-alias-div"
  })

  it('Работает с различными селекторами', () => {
    cy.visit('/commands/querying')
    
    // TODO: Найти элемент по ID
    // TODO: Найти элемент по классу
    // TODO: Найти элемент по тегу
    // TODO: Найти элемент по атрибуту
    // TODO: Найти элемент по комбинированному селектору
  })
})
```

### Задание 2.3: Интерактивные элементы

Создайте файл `cypress/e2e/exercises/02-3-interactions.cy.js`:

```javascript
describe('Взаимодействие с элементами', () => {
  beforeEach(() => {
    cy.visit('/commands/actions')
  })

  it('Работает с текстовыми полями', () => {
    // TODO: Найти поле email и ввести тестовый email
    // TODO: Проверить, что значение сохранилось
    // TODO: Очистить поле
    // TODO: Проверить, что поле пустое
  })

  it('Работает с кнопками', () => {
    // TODO: Найти кнопку "Button" и кликнуть по ней
    // TODO: Найти скрытую кнопку и попробовать кликнуть (должна быть ошибка)
    // TODO: Принудительно кликнуть по скрытой кнопке с { force: true }
  })

  it('Работает с чекбоксами', () => {
    // TODO: Найти чекбоксы и отметить их
    // TODO: Проверить, что они отмечены
    // TODO: Снять отметки
    // TODO: Проверить, что отметки сняты
  })
})
```

### Задание 2.4: Работа со списками

Создайте файл `cypress/e2e/exercises/02-4-lists.cy.js`:

```javascript
describe('Работа со списками и таблицами', () => {
  it('Проверяет элементы списка', () => {
    cy.visit('/commands/traversal')

    // TODO: Найти список с классом "traversal-breadcrumb"
    // TODO: Проверить количество элементов в списке
    // TODO: Проверить текст первого элемента
    // TODO: Проверить текст последнего элемента
    // TODO: Найти конкретный элемент списка по тексту
  })

  it('Фильтрует элементы', () => {
    cy.visit('/commands/traversal')
    
    // TODO: Найти все кнопки на странице
    // TODO: Отфильтровать только видимые кнопки
    // TODO: Найти первую кнопку
    // TODO: Найти последнюю кнопку
    // TODO: Найти кнопку, содержащую определенный текст
  })
})
```

## 📚 Справочная информация

### Полезные команды Cypress:

```javascript
// Поиск элементов
cy.get('#id')                    // по ID
cy.get('.class')                 // по классу
cy.get('[data-cy="test"]')       // по атрибуту
cy.get('button')                 // по тегу
cy.contains('текст')             // по тексту

// Действия
cy.click()                       // клик
cy.type('текст')                 // ввод текста
cy.clear()                       // очистка поля
cy.check()                       // отметить чекбокс
cy.uncheck()                     // снять отметку

// Проверки
cy.should('be.visible')          // элемент видим
cy.should('contain', 'текст')    // содержит текст
cy.should('have.value', 'значение') // имеет значение
cy.should('be.checked')          // отмечен
cy.should('not.exist')           // не существует

// Навигация
cy.url().should('include', '/path') // проверка URL
cy.go('back')                    // назад
cy.go('forward')                 // вперед
cy.reload()                      // обновить
```

### Чейнинг (цепочки команд):

```javascript
// Можно объединять команды в цепочки
cy.get('#email')
  .type('test@example.com')
  .should('have.value', 'test@example.com')
  .clear()
  .should('be.empty')
```

## ✅ Критерии выполнения

### Обязательные требования:
- [ ] Созданы все 4 файла с тестами
- [ ] Все TODO комментарии реализованы
- [ ] Тесты проходят успешно (зеленые в Test Runner)
- [ ] Использованы разные типы селекторов
- [ ] Использованы разные типы проверок

### Дополнительные задачи:
- [ ] Добавлены описательные комментарии к коду
- [ ] Тесты имеют понятные названия
- [ ] Используется beforeEach где это уместно
- [ ] Проверена работа в разных браузерах

## 🔍 Самопроверка

Ответьте на вопросы:

1. **В чем разница между `cy.get()` и `cy.contains()`?**
2. **Когда нужно использовать `{ force: true }`?**
3. **Как проверить, что элемент НЕ существует на странице?**
4. **Что такое чейнинг команд?**
5. **Как найти элемент по частичному тексту?**

<details>
<summary>🔍 Ответы</summary>

1. `cy.get()` ищет по селектору, `cy.contains()` ищет по тексту
2. Когда элемент скрыт или перекрыт другим элементом
3. `cy.get('#element').should('not.exist')`
4. Объединение нескольких команд через точку
5. `cy.contains('частичный текст')`
</details>

## 🚨 Частые ошибки

### 1. Элемент не найден
```javascript
// ❌ Плохо - слишком общий селектор
cy.get('button').click()

// ✅ Хорошо - конкретный селектор  
cy.get('[data-cy="submit-button"]').click()
```

### 2. Неправильные проверки
```javascript
// ❌ Плохо - проверка наличия вместо видимости
cy.get('#message').should('exist')

// ✅ Хорошо - проверка видимости
cy.get('#message').should('be.visible')
```

### 3. Неправильная работа с чейнингом
```javascript
// ❌ Плохо - потеря контекста
cy.get('#form')
cy.get('#submit').click() // ищет по всей странице

// ✅ Хорошо - сохранение контекста
cy.get('#form').within(() => {
  cy.get('#submit').click()
})
```

## 🎓 Что изучили

- ✅ Основные селекторы CSS
- ✅ Команды для взаимодействия с элементами
- ✅ Различные типы assertions
- ✅ Чейнинг команд
- ✅ Работу с различными типами элементов

## 🚀 Следующий шаг

После выполнения этого задания вы готовы к работе с формами и более сложными сценариями тестирования.

---

💡 **Совет**: Экспериментируйте с селекторами в браузерных DevTools перед использованием в тестах!