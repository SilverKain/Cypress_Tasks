# 📚 Глоссарий терминов тестирования

## A

**Assertion (Утверждение)** - проверка, которая определяет прошел ли тест или нет. Например: `cy.get('.title').should('be.visible')`

**API (Application Programming Interface)** - набор методов для взаимодействия с приложением программным способом.

**Alias (Алиас)** - именованная ссылка на элемент, запрос или данные для переиспользования: `cy.get('.element').as('myElement')`

## B

**BaseURL (Базовый URL)** - основной адрес приложения, настраивается в `cypress.config.js`

**Browser (Браузер)** - программа для просмотра веб-страниц (Chrome, Firefox, Edge)

**Before/After hooks** - функции, выполняемые до или после тестов

## C

**CI/CD (Continuous Integration/Continuous Deployment)** - автоматическая система запуска тестов при изменении кода

**Command (Команда)** - действие в Cypress: `cy.get()`, `cy.click()`, `cy.type()`

**Custom Command (Пользовательская команда)** - своя команда Cypress, созданная для переиспользования логики

**Chai** - библиотека assertions, встроенная в Cypress

**Chainable** - возможность объединять команды в цепочку: `cy.get().click().should()`

## D

**Data-Driven Testing** - тестирование с множественными наборами данных

**DOM (Document Object Model)** - структура HTML документа

**Debug (Отладка)** - процесс поиска и исправления ошибок

**describe()** - блок группировки тестов

## E

**E2E (End-to-End)** - сквозное тестирование всего приложения от начала до конца

**Element (Элемент)** - часть веб-страницы: кнопка, поле ввода, текст

**expect()** - функция для явных проверок: `expect(value).to.eq(5)`

## F

**Fixture (Фикстура)** - файл с тестовыми данными, обычно JSON

**Flaky Test (Нестабильный тест)** - тест, который иногда проходит, иногда падает

**Framework (Фреймворк)** - набор инструментов для разработки (React, Vue, Angular)

## H

**Hook (Хук)** - функция, выполняемая в определенный момент: `before()`, `beforeEach()`, `after()`, `afterEach()`

**Headless Mode** - запуск браузера без графического интерфейса (быстрее)

**HTTP** - протокол передачи данных в интернете

## I

**Intercept (Перехват)** - контроль сетевых запросов: `cy.intercept()`

**it()** - функция определяющая один тест

**Implicit Assertion** - встроенная проверка в командах Cypress

## L

**Locator (Локатор)** - способ найти элемент на странице (селектор)

**Localhost** - адрес локального сервера (127.0.0.1)

## M

**Mock (Мок)** - подмена реальных данных или API тестовыми

**Mocha** - test runner, встроенный в Cypress

## P

**Page Object Model (POM)** - паттерн организации тестов через классы страниц

**Parallel Testing** - параллельный запуск тестов для ускорения

**Plugin (Плагин)** - дополнительный модуль расширяющий функциональность

## R

**Regression Testing (Регрессионное тестирование)** - проверка что новые изменения не сломали существующую функциональность

**Retry (Повтор)** - автоматическая повторная попытка выполнить команду

**Runner (Раннер)** - программа, которая запускает тесты

## S

**Selector (Селектор)** - способ указать какой элемент нужен: `.class`, `#id`, `[data-cy]`

**Stub (Заглушка)** - замена функции тестовой версией

**Spy (Шпион)** - слежение за вызовами функций

**Smoke Testing** - быстрая проверка основных функций

**Screenshot (Скриншот)** - снимок экрана

**Session** - сохранение состояния между тестами (например, авторизации)

## T

**Test Suite (Тестовый набор)** - группа связанных тестов

**Test Case (Тест-кейс)** - один конкретный тест

**Timeout (Таймаут)** - максимальное время ожидания действия

**Test Runner** - интерфейс Cypress для запуска тестов

**TypeScript** - язык программирования, надстройка над JavaScript с типами

## U

**UI (User Interface)** - пользовательский интерфейс, то что видит пользователь

**Unit Test** - тестирование отдельной функции или компонента

**URL (Uniform Resource Locator)** - адрес веб-страницы

## V

**Viewport (Область просмотра)** - размер окна браузера: `cy.viewport(1280, 720)`

**Validation (Валидация)** - проверка корректности данных

## W

**Wait (Ожидание)** - пауза в выполнении теста

**Wrapper (Обертка)** - функция оборачивающая другую функцию

## X

**XHR (XMLHttpRequest)** - способ отправки HTTP запросов из JavaScript

---

## 🎯 Основные понятия простыми словами

### Тест
Автоматическая проверка что приложение работает правильно.

### Селектор
"Адрес" элемента на странице. Как мы объясняем Cypress что нажать.

### Assertion
Проверка ожидаемого результата. "Кнопка должна быть видна" - это assertion.

### Fixture
Файл с тестовыми данными. Вместо "ivan@mail.com" в каждом тесте, храним в одном месте.

### Mock
Подделка. Вместо реального API возвращаем тестовые данные.

### Hook
Действие "перед" или "после" тестов. Например, открыть страницу перед каждым тестом.

### Page Object
Класс описывающий страницу. Все кнопки и действия страницы в одном месте.

### Flaky Test
"Капризный" тест. То работает, то нет. Плохой тест, надо исправлять.

### CI/CD
Автоматический запуск тестов когда программист коммитит код.

---

## 📖 Примеры использования

```javascript
// Assertion - проверка
cy.get('.title').should('be.visible')

// Selector - селектор
cy.get('[data-cy="submit-button"]')

// Fixture - фикстура
cy.fixture('users.json')

// Mock - мокирование
cy.intercept('GET', '/api/users', { body: [] })

// Hook - хук
beforeEach(() => {
  cy.visit('/')
})

// Alias - алиас
cy.get('.element').as('myElement')
cy.get('@myElement').click()
```

---

**Совет:** Не бойтесь терминов! С практикой всё станет понятнее 😊
