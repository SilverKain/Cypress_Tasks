# ЭТАП 1: JavaScript для тестировщика

---

## Модуль 3. База JavaScript

### 🎯 Зачем это нужно в реальной работе

Cypress — это JavaScript фреймворк. Без базового понимания JavaScript ты не сможешь писать тесты.

**НО!** Хорошая новость: тебе НЕ нужно становиться крутым разработчиком. Достаточно понимать основы:
- Как хранить данные (переменные)
- Как повторять действия (циклы)
- Как принимать решения (условия)
- Как работать со списками (массивы)

90% твоих тестов будут использовать именно эти базовые концепции.

---

### 📦 Переменные

#### Что это такое:
Переменная — это контейнер для хранения данных. Как коробка с ярлыком.

#### Как это работает:
В JavaScript есть три способа объявить переменную:
- **let** — можно изменить значение
- **const** — нельзя изменить значение (константа)
- **var** — старый способ (лучше не использовать)

#### 📖 Пример из жизни:

**Переменная = Коробка с ярлыком**

У тебя есть коробка с надписью "Пароль". Внутри лежит бумажка со значением "12345".

Когда нужен пароль — ты смотришь в коробку "Пароль" и берёшь значение.

#### Пример кода:

```javascript
// Объявление переменных
let username = 'testuser'     // Можно изменить
const password = '12345'      // Нельзя изменить
let age = 25                    // Число

// Изменение значения
username = 'newuser'          // ✅ Работает (let)
// password = 'new123'         // ❌ Ошибка! (const)

// Использование в Cypress
cy.get('#username').type(username)
cy.get('#password').type(password)
```

#### Разбор кода построчно:

1. `let username = 'testuser'` — создаём переменную username и кладём в неё текст 'testuser'
2. `const password = '12345'` — создаём константу password. Её значение изменить нельзя
3. `username = 'newuser'` — меняем значение username (работает, т.к. это let)
4. `cy.get('#username').type(username)` — используем значение переменной в тесте

#### ✅ Главное правило:
- Используй **const** по умолчанию
- Если нужно изменить — используй **let**
- Никогда не используй **var**

---

### 🔢 Типы данных

#### Основные типы данных в JavaScript:

| Тип | Описание | Пример |
|-----|----------|---------|
| **String** | Текст | 'Hello', "World" |
| **Number** | Число | 42, 3.14 |
| **Boolean** | Истина/Ложь | true, false |
| **Array** | Список | [1, 2, 3] |
| **Object** | Объект | {name: 'Alex'} |
| **null** | Пусто (намеренно) | null |
| **undefined** | Не определено | undefined |

#### Примеры типов данных:

```javascript
// Примеры типов данных
const email = 'test@example.com'  // String (текст)
const price = 99.99                 // Number (число)
const isLoggedIn = true           // Boolean (истина)
const items = ['apple', 'banana']  // Array (массив)
const user = { name: 'Alex' }      // Object (объект)

// Проверка типа
console.log(typeof email)        // "string"
console.log(typeof price)        // "number"
console.log(typeof isLoggedIn)   // "boolean"
```

#### 📖 Практическое применение в тестах:

```javascript
// Тестовые данные для Cypress
const testData = {
  username: 'testuser',        // String
  password: 'Test123!',        // String
  age: 25,                     // Number
  isAdmin: false,             // Boolean
  emails: [                   // Array
    'test1@mail.com',
    'test2@mail.com'
  ]
}

// Использование в тесте
cy.get('#username').type(testData.username)
cy.get('#age').type(testData.age)
```

---

### ⚡ Функции

#### Что это такое:
Функция — это блок кода, который можно использовать многократно. Как рецепт блюда.

#### Зачем нужны:
- Избежать дублирования кода
- Сделать код читаемым
- Переиспользовать логику

#### 📖 Аналогия:

**Функция = Рецепт приготовления кофе**

Вместо того чтобы каждый раз описывать "возьми чашку, насыпь кофе, залей водой"...

Ты просто говоришь: "Приготовь кофе" — и все шаги выполняются автоматически.

#### Пример кода:

```javascript
// Объявление функции
function login(username, password) {
  cy.get('#username').type(username)
  cy.get('#password').type(password)
  cy.get('button[type="submit"]').click()
}

// Использование функции
login('testuser', '12345')
login('admin', 'admin123')

// Стрелочная функция (современный синтаксис)
const login = (username, password) => {
  cy.get('#username').type(username)
  cy.get('#password').type(password)
  cy.get('button[type="submit"]').click()
}
```

#### Разбор кода:

1. `function login(username, password)` — объявляем функцию с именем "login", которая принимает 2 параметра
2. `username, password` — параметры (входные данные)
3. Внутри {} — тело функции (что она делает)
4. `login('testuser', '12345')` — вызов функции с аргументами

#### ✅ Два способа написания функций:
- **Классический:** `function name() {}`
- **Стрелочный:** `const name = () => {}`
- В Cypress чаще используют стрелочные функции

---

### 🔀 Условия (if/else)

#### Что это такое:
Условия позволяют выполнять разный код в зависимости от ситуации.

#### Как это работает:
**ЕСЛИ** условие истинно → выполни код A  
**ИНАЧЕ** → выполни код B

#### 📖 Пример из жизни:

**ЕСЛИ** на улице дождь → возьми зонт  
**ИНАЧЕ** → не бери зонт

#### Пример кода:

```javascript
// Простое условие
const age = 20

if (age >= 18) {
  console.log('Совершеннолетний')
} else {
  console.log('Несовершеннолетний')
}

// Множественные условия
const status = 200

if (status === 200) {
  console.log('Успешно')
} else if (status === 404) {
  console.log('Не найдено')
} else {
  console.log('Другая ошибка')
}

// Применение в Cypress
cy.get('body').then(($body) => {
  if ($body.find('.error').length > 0) {
    // Если есть ошибка на странице
    cy.log('Найдена ошибка')
  } else {
    cy.log('Ошибок нет')
  }
})
```

#### ⚠️ Операторы сравнения:
- `===` — строгое равенство (лучше использовать)
- `!==` — не равно
- `>` — больше
- `<` — меньше
- `>=` — больше или равно
- `<=` — меньше или равно

---

### 🔁 Циклы

#### Что это такое:
Цикл — это повторение одного и того же действия несколько раз.

#### Зачем нужны:
- Обработать список элементов
- Повторить действие N раз
- Избежать копипаста

#### 📖 Пример из жизни:

Вместо того чтобы написать:  
"Помой тарелку 1, помой тарелку 2, помой тарелку 3..."

Ты говоришь: "Помой ВСЕ тарелки"

#### Пример кода:

```javascript
// Цикл for - классический
for (let i = 0; i < 3; i++) {
  console.log('Итерация ' + i)
}
// Выведет: Итерация 0, Итерация 1, Итерация 2

// forEach - для массивов (современный способ)
const users = ['Alice', 'Bob', 'Charlie']

users.forEach((user) => {
  console.log('Привет, ' + user)
})
// Выведет: Привет, Alice | Привет, Bob | Привет, Charlie

// Применение в Cypress
const testEmails = [
  'test1@mail.com',
  'test2@mail.com',
  'test3@mail.com'
]

testEmails.forEach((email) => {
  cy.get('#email').clear().type(email)
  cy.get('button').click()
  // Проверяем каждый email
})
```

#### Разбор for:

`for (let i = 0; i < 3; i++)`

1. `let i = 0` — начальное значение счётчика
2. `i < 3` — условие продолжения (пока i меньше 3)
3. `i++` — увеличиваем счётчик на 1 после каждой итерации

#### ✅ Какой цикл использовать:
- **forEach** — для массивов (самый читаемый)
- **for** — когда нужен контроль над счётчиком
- В Cypress чаще используют forEach

---

### 📋 Массивы

#### Что это такое:
Массив — это список значений. Как список покупок.

#### Как это работает:
Массив хранит несколько элементов под одним именем. Каждый элемент имеет индекс (позицию), начиная с 0.

#### 📖 Аналогия:

**Массив = Список покупок**

[Яблоки, Молоко, Хлеб]

Индекс 0: Яблоки | Индекс 1: Молоко | Индекс 2: Хлеб

#### Пример кода:

```javascript
// Создание массива
const fruits = ['apple', 'banana', 'orange']

// Получение элемента по индексу
console.log(fruits[0])  // "apple"
console.log(fruits[1])  // "banana"

// Длина массива
console.log(fruits.length)  // 3

// Добавление элемента
fruits.push('grape')  // Добавит в конец

// Полезные методы массивов
const numbers = [1, 2, 3, 4, 5]

// filter - фильтрация
const evenNumbers = numbers.filter(num => num % 2 === 0)
// [2, 4]

// map - преобразование
const doubled = numbers.map(num => num * 2)
// [2, 4, 6, 8, 10]

// find - поиск элемента
const found = numbers.find(num => num > 3)
// 4 (первый элемент > 3)
```

#### 📖 Применение в Cypress:

```javascript
// Тестирование с разными данными
const testUsers = [
  { username: 'user1', password: 'pass1' },
  { username: 'user2', password: 'pass2' },
  { username: 'user3', password: 'pass3' }
]

testUsers.forEach((user) => {
  cy.visit('/login')
  cy.get('#username').type(user.username)
  cy.get('#password').type(user.password)
  cy.get('button').click()
})
```

---

### 🗂️ Объекты

#### Что это такое:
Объект — это структура данных с именованными свойствами. Как карточка сотрудника.

#### Разница с массивом:
- **Массив:** доступ по индексу (номеру) — `arr[0]`
- **Объект:** доступ по имени свойства — `obj.name`

#### 📖 Аналогия:

**Объект = Карточка пользователя**

Имя: Алексей  
Возраст: 25  
Email: alex@mail.com

#### Пример кода:

```javascript
// Создание объекта
const user = {
  name: 'Алексей',
  age: 25,
  email: 'alex@mail.com',
  isAdmin: false
}

// Доступ к свойствам
console.log(user.name)   // "Алексей"
console.log(user['age']) // 25 (альтернативный синтаксис)

// Изменение свойства
user.age = 26

// Добавление нового свойства
user.city = 'Москва'

// Вложенные объекты
const testData = {
  user: {
    username: 'testuser',
    password: '12345'
  },
  product: {
    name: 'iPhone',
    price: 999
  }
}

console.log(testData.user.username)  // "testuser"
console.log(testData.product.price)  // 999
```

#### 📖 Применение в Cypress:

```javascript
// Организация тестовых данных
const testData = {
  validUser: {
    username: 'john_doe',
    password: 'SecurePass123',
    email: 'john@example.com'
  },
  invalidUser: {
    username: '',
    password: '123',
    email: 'invalid-email'
  }
}

// Использование в тесте
describe('Логин', () => {
  it('успешный логин', () => {
    cy.get('#username').type(testData.validUser.username)
    cy.get('#password').type(testData.validUser.password)
    cy.get('button').click()
  })
})
```

---

### ❌ Типичные ошибки новичков:

- **Использование var вместо let/const.** Используй const по умолчанию.
- **Путаница с индексами массива.** Помни: индексы начинаются с 0.
- **Использование == вместо ===.** Всегда используй строгое равенство ===.
- **Забывают про scope переменных.** Переменная внутри {} доступна только там.
- **Не понимают разницу между массивом и объектом.** Массив для списков, объект для структур.

---

### 🎯 МИНИ-ПРАКТИКА

**Задание:** Написать функцию проверки валидности формы.

**Задача:** Создать функцию `validateForm`, которая проверяет данные формы регистрации.

**Требования:**
- Email должен содержать символ @
- Пароль должен быть длиннее 6 символов
- Возраст должен быть больше 18
- Имя не должно быть пустым

**Функция должна возвращать объект:**
```javascript
{
  isValid: true/false,
  errors: [список ошибок]
}
```

**Шаблон для начала:**

```javascript
function validateForm(formData) {
  const errors = []
  
  // Проверка email
  if (!formData.email.includes('@')) {
    errors.push('Email должен содержать @')
  }
  
  // TODO: Добавь проверку пароля
  
  // TODO: Добавь проверку возраста
  
  // TODO: Добавь проверку имени
  
  return {
    isValid: errors.length === 0,
    errors: errors
  }
}

// Тестируй так:
const testUser = {
  name: 'Алексей',
  email: 'test@mail.com',
  password: 'secure123',
  age: 25
}

const result = validateForm(testUser)
console.log(result)  // { isValid: true, errors: [] }
```

**Попробуй также протестировать с невалидными данными:**

```javascript
const invalidUser = {
  name: '',
  email: 'invalid-email',  // нет @
  password: '123',            // короткий
  age: 16                  // меньше 18
}

const result2 = validateForm(invalidUser)
// Должно вернуть список всех ошибок
```

---

## Модуль 4. Асинхронность

### 🎯 Зачем это нужно в реальной работе

Cypress — это асинхронный фреймворк. Без понимания асинхронности ты будешь писать тесты, которые падают непредсказуемо.

**Важно:** Ты НЕ будешь писать асинхронный код в Cypress (Cypress делает это за тебя), НО ты должен понимать, КАК это работает.

Это поможет тебе:
- Понять, почему команды выполняются в определённом порядке
- Избежать типичных ошибок с ожиданиями
- Разобраться в работе API тестов

---

### ⏱️ Что такое асинхронность

#### Что это такое:
Асинхронность — это когда программа не ждёт завершения одной операции, а идёт дальше выполнять другой код.

#### Синхронный код (обычный):
Выполняется строго по порядку, одна строка за другой.

#### Асинхронный код:
Некоторые операции выполняются "в фоне", программа не ждёт их завершения.

#### 📖 Аналогия из жизни:

**Синхронный подход (последовательный):**
1. Поставил чайник
2. ЖДУ пока закипит (5 минут стою и смотрю)
3. Налил чай
4. Начал делать бутерброд

**Асинхронный подход (параллельный):**
1. Поставил чайник
2. Пока он кипятится, делаю бутерброд
3. Когда чайник закипел — налил чай

Во втором случае ты не тратишь время на ожидание!

#### Пример кода:

```javascript
// СИНХРОННЫЙ КОД
console.log('1. Начало')
console.log('2. Середина')
console.log('3. Конец')

// Вывод строго по порядку:
// 1. Начало
// 2. Середина  
// 3. Конец

// АСИНХРОННЫЙ КОД
console.log('1. Начало')

setTimeout(() => {
  console.log('2. Это выполнится через 2 секунды')
}, 2000)

console.log('3. Конец')

// Вывод:
// 1. Начало
// 3. Конец  (!)
// 2. Это выполнится через 2 секунды (через 2 секунды)
```

#### Почему так происходит:
Функция `setTimeout` асинхронная. Она говорит: "Выполни это через 2 секунды".

JavaScript не ждёт эти 2 секунды, а сразу идёт дальше и выполняет следующую строку.

---

### 🤝 Promise (Промисы) простыми словами

#### Что это такое:
Promise (обещание) — это объект, который представляет результат асинхронной операции в будущем.

#### Состояния Promise:
- **Pending** (ожидание) — операция выполняется
- **Fulfilled** (выполнено) — операция успешна
- **Rejected** (отклонено) — операция провалилась

#### 📖 Аналогия:

**Promise = Заказ в кафе**

1. Ты заказал кофе (создал Promise)
2. Бариста готовит (Pending)
3. **Вариант 1:** Кофе готов, тебе отдали (Fulfilled)
4. **Вариант 2:** Кофе-машина сломалась, заказ отменили (Rejected)

#### Пример кода:

```javascript
// Создание Promise
const fetchData = new Promise((resolve, reject) => {
  // Симуляция запроса к серверу
  setTimeout(() => {
    const success = true
    
    if (success) {
      resolve('Данные получены!')  // Успех
    } else {
      reject('Ошибка сервера')     // Ошибка
    }
  }, 1000)
})

// Использование Promise
fetchData
  .then((data) => {
    console.log(data)  // "Данные получены!"
  })
  .catch((error) => {
    console.log(error) // Если была ошибка
  })
```

#### Разбор кода построчно:

1. `new Promise((resolve, reject) => {})` — создаём Promise
2. `resolve('данные')` — вызываем, когда операция успешна
3. `reject('ошибка')` — вызываем, если операция провалилась
4. `.then()` — выполнится, когда Promise успешен
5. `.catch()` — выполнится, если Promise отклонён

#### 📖 Реальный пример — запрос к API:

```javascript
// Обычный способ (с Promise)
fetch('https://api.example.com/users')
  .then(response => response.json())
  .then(data => {
    console.log('Пользователи:', data)
  })
  .catch(error => {
    console.log('Ошибка:', error)
  })

// Современный способ (async/await)
async function getUsers() {
  try {
    const response = await fetch('https://api.example.com/users')
    const data = await response.json()
    console.log('Пользователи:', data)
  } catch (error) {
    console.log('Ошибка:', error)
  }
}
```

---

### 🔄 Почему код выполняется не по порядку

#### Главное правило:
JavaScript выполняет код построчно, НО асинхронные операции откладываются и выполняются позже.

#### Пример "неожиданного" поведения:

```javascript
console.log('Шаг 1')

setTimeout(() => {
  console.log('Шаг 2')
}, 0)  // Даже с задержкой 0 мс!

console.log('Шаг 3')

// Вывод:
// Шаг 1
// Шаг 3
// Шаг 2
```

#### Почему так:
Даже с задержкой 0 мс, `setTimeout` — асинхронная функция. Она уходит в очередь и выполняется после всего синхронного кода.

#### 📖 Как это выглядит в Cypress:

```javascript
// ❌ НЕПРАВИЛЬНО
let text

cy.get('h1').invoke('text').then((value) => {
  text = value
})

console.log(text)  // undefined (!) - выполнится раньше, чем .then()

// ✅ ПРАВИЛЬНО
cy.get('h1').invoke('text').then((value) => {
  text = value
  console.log(text)  // Работает! - выполняется внутри .then()
})
```

---

### ♻️ Event Loop (без сложной теории)

#### Что это такое:
Event Loop — это механизм, который управляет выполнением кода в JavaScript.

#### Упрощённо работает так:
1. Выполни весь синхронный код
2. Когда синхронный код закончился — возьми из очереди асинхронные задачи
3. Выполни их
4. Повтори

#### 📖 Визуализация Event Loop:

```javascript
console.log('Старт')              // 1️⃣ Синхронно

setTimeout(() => {
  console.log('Таймаут 1')     // 4️⃣ Асинхронно (из очереди)
}, 0)

Promise.resolve().then(() => {
  console.log('Promise')      // 3️⃣ Приоритет выше setTimeout
})

console.log('Финиш')             // 2️⃣ Синхронно

// Вывод:
// Старт
// Финиш
// Promise
// Таймаут 1
```

#### ✅ Что важно запомнить:
- Синхронный код выполняется ВСЕГДА первым
- Promise имеют приоритет над setTimeout
- Cypress команды — асинхронные, они выполняются в очереди

---

### ❌ Типичные ошибки новичков:

- **Ожидают, что код выполнится по порядку.** Асинхронный код идёт в очередь.
- **Используют переменные из .then() снаружи.** Они будут undefined.
- **Не понимают, почему console.log выводит не то.** Он выполняется раньше асинхронного кода.
- **Пытаются использовать async/await с Cypress командами.** Cypress команды уже асинхронные, не нужно await.

---

### 🎯 МИНИ-ПРАКТИКА

**Задание:** Симуляция запроса к серверу.

**Задача:** Создать функцию, которая симулирует получение данных пользователя с сервера.

**Требования:**
- Функция должна возвращать Promise
- Через 2 секунды Promise должен resolve с данными пользователя
- Если передан userId = 0, то Promise должен reject с ошибкой

**Шаблон:**

```javascript
function fetchUser(userId) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      if (userId === 0) {
        reject('Пользователь не найден')
      } else {
        resolve({
          id: userId,
          name: 'Алексей',
          email: 'alex@mail.com'
        })
      }
    }, 2000)
  })
}

// Тестирование:
console.log('Начало запроса...')

fetchUser(1)
  .then((user) => {
    console.log('Успех!', user)
  })
  .catch((error) => {
    console.log('Ошибка!', error)
  })

console.log('Запрос отправлен')

// Ожидаемый вывод:
// Начало запроса...
// Запрос отправлен
// (через 2 секунды) Успех! { id: 1, name: 'Алексей', ... }
```

**Дополнительное задание:**

Переписать с использованием async/await:

```javascript
async function getUserData() {
  try {
    console.log('Начало запроса...')
    const user = await fetchUser(1)
    console.log('Успех!', user)
  } catch (error) {
    console.log('Ошибка!', error)
  }
}

getUserData()
```

**Проверь себя:** Что выведет этот код и в каком порядке?

```javascript
console.log('A')

fetchUser(1).then(() => console.log('B'))

console.log('C')

// Ответ: A, C, (через 2 сек) B
```

---

[← Вернуться к плану курса](План_курса.md)
