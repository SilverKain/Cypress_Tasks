# ЭТАП 2: Основы автоматизации

---

## Модуль 5. Зачем автоматизация

### 🎯 Зачем это нужно в реальной работе

Перед тем как писать хоть одну строчку кода для автоматизации, нужно понять: **а стоит ли оно того?**

В реальной работе часто встречается ситуация:
- Джуниор автоматизирует всё подряд → тратит месяц → тесты падают → никто ими не пользуется
- Мидл анализирует, что выгодно автоматизировать → тратит неделю → тесты стабильные → команда экономит часы каждый день

Этот модуль научит тебя **думать как мидл**, а не просто писать код.

**Что ты получишь:**
- Понимание, когда автоматизация экономит деньги, а когда — тратит
- Умение обосновать руководству, почему нужна автоматизация
- Навык выбора правильных тестов для автоматизации

---

### 💰 Когда выгодно автоматизировать

#### Что это значит:
Автоматизация — это **инвестиция**. Ты тратишь время сейчас, чтобы сэкономить время потом.

Но не всегда это выгодно. Иногда проще проверить руками.

#### Как это работает:

**Автоматизация выгодна, когда:**

1. **Тест выполняется часто**
   - Регрессионные проверки перед каждым релизом
   - Smoke тесты после каждого деплоя
   - Проверка критического функционала

2. **Тест занимает много времени вручную**
   - Проверка 100 полей формы
   - Тестирование различных комбинаций параметров
   - Проверка расчётов с множеством данных

3. **Тест подвержен человеческим ошибкам**
   - Нужно сверить 500 строк таблицы
   - Проверка сложных вычислений
   - Проверка данных в базе

4. **Функционал стабилен**
   - Логин/регистрация (редко меняется)
   - Базовые CRUD операции
   - Критические бизнес-процессы

#### ❌ Автоматизация НЕ выгодна, когда:

1. **Функционал часто меняется**
   - Новая фича, которую переписывают каждый день
   - Экспериментальные разделы сайта
   - Прототипы

2. **Тест выполняется редко**
   - Проверка годового отчёта (1 раз в год)
   - Редкие edge cases

3. **Тест проще сделать вручную**
   - Визуальная проверка дизайна
   - Проверка UX (удобство)
   - Исследовательское тестирование

4. **Нет ресурсов на поддержку**
   - Некому будет поправить тесты при изменениях
   - Нет времени на стабилизацию

#### 📖 Пример из жизни:

**Ситуация:** Интернет-магазин. Нужно протестировать два функционала.

**Функционал 1: Логин**
- Выполняется: Перед каждым тестом других фич (100+ раз в неделю)
- Время вручную: 30 секунд
- Стабильность: Не меняется уже год
- **Вывод:** ✅ Автоматизировать!

**Время на автоматизацию:** 2 часа  
**Экономия:** 50 минут в неделю  
**Окупается через:** 2-3 недели

---

**Функционал 2: Новый экспериментальный калькулятор доставки**
- Выполняется: 1-2 раза в неделю
- Время вручную: 5 минут
- Стабильность: Дизайнер переделывает каждый день
- **Вывод:** ❌ Пока не автоматизировать

**Время на автоматизацию:** 4 часа  
**Время на доработку каждый день:** 1 час  
**Вывод:** Дождаться стабилизации, потом автоматизировать

---

### 📊 ROI автоматизации (Return on Investment)

#### Что это такое:
ROI — это показатель того, когда автоматизация окупится.

**Простая формула:**

```
Время на автоматизацию ÷ Экономия времени = Срок окупаемости
```

#### Как это работает:

**Считаем выгоду:**

1. **Считаем экономию:**
   ```
   Время ручного теста × Количество запусков в неделю = Экономия в неделю
   ```

2. **Считаем затраты:**
   - Время на написание теста
   - Время на поддержку (примерно 20% от времени написания в месяц)

3. **Считаем окупаемость:**
   ```
   Затраты ÷ Экономия в неделю = Недель до окупаемости
   ```

#### 📖 Реальный пример:

**Автоматизация тестирования формы регистрации**

**Исходные данные:**
- Время ручного теста: 5 минут
- Частота тестирования: 20 раз в неделю (каждый регресс)
- Время на автоматизацию: 4 часа (240 минут)
- Поддержка: 30 минут в месяц

**Расчёт:**

```
Экономия в неделю = 5 минут × 20 = 100 минут
Экономия в месяц = 100 × 4 = 400 минут (6.7 часов)

Затраты на написание = 4 часа = 240 минут
Затраты на поддержку в месяц = 30 минут

Окупаемость:
240 ÷ 100 = 2.4 недели

Через месяц:
Потратили: 240 + 30 = 270 минут
Сэкономили: 400 минут
Чистая выгода: 130 минут (2+ часа)
```

**Через 6 месяцев:**
```
Потратили: 240 + (30 × 6) = 420 минут (7 часов)
Сэкономили: 400 × 6 = 2400 минут (40 часов)
Чистая выгода: 1980 минут (33 часа)
```

#### ✅ Правило большого пальца:

**Автоматизируй, если:**
- Окупится меньше чем за 1 месяц → Точно автоматизировать! ✅
- Окупится за 1-3 месяца → Скорее всего выгодно ✅
- Окупится за 3-6 месяцев → Подумать 🤔
- Окупится больше 6 месяцев → Скорее всего не выгодно ❌

---

### 🎯 Что чаще всего автоматизируют

#### Топ функционалов для автоматизации:

**1. Аутентификация и авторизация** ⭐⭐⭐
- Логин/выход
- Регистрация
- Восстановление пароля
- Проверка прав доступа

**Почему:**
- ✅ Используется в каждом тесте
- ✅ Стабильно (редко меняется)
- ✅ Критично для бизнеса
- ✅ Быстро окупается

**Пример кода:**
```javascript
// Часто используемая команда логина
describe('Тесты магазина', () => {
  beforeEach(() => {
    // Логин перед каждым тестом
    cy.visit('/login')
    cy.get('#email').type('test@mail.com')
    cy.get('#password').type('12345')
    cy.get('button[type="submit"]').click()
  })

  it('Проверка добавления товара', () => {
    // Логин уже выполнен автоматически
    cy.visit('/products')
    cy.contains('Добавить в корзину').click()
  })
})
```

---

**2. Smoke тесты** ⭐⭐⭐
- Главная страница открывается
- Основные разделы доступны
- Критические кнопки работают

**Почему:**
- ✅ Запускаются после каждого деплоя
- ✅ Быстрые (1-2 минуты)
- ✅ Позволяют быстро понять, что сломалось

**Пример:**
```javascript
describe('Smoke тесты', () => {
  it('Главная страница загружается', () => {
    cy.visit('/')
    cy.contains('Добро пожаловать')
  })

  it('Страница логина доступна', () => {
    cy.visit('/login')
    cy.get('#email').should('be.visible')
  })

  it('Страница товаров доступна', () => {
    cy.visit('/products')
    cy.get('.product-card').should('exist')
  })
})
```

---

**3. CRUD операции** ⭐⭐⭐
- Create (создание)
- Read (чтение)
- Update (обновление)
- Delete (удаление)

**Почему:**
- ✅ Повторяющийся паттерн
- ✅ Критичный функционал
- ✅ Легко автоматизируется

**Пример:**
```javascript
describe('CRUD для задач', () => {
  it('Создание задачи', () => {
    cy.visit('/tasks')
    cy.get('[data-testid="new-task"]').click()
    cy.get('#title').type('Новая задача')
    cy.get('#description').type('Описание')
    cy.get('button[type="submit"]').click()
    cy.contains('Новая задача').should('be.visible')
  })

  it('Редактирование задачи', () => {
    cy.visit('/tasks')
    cy.contains('Новая задача').click()
    cy.get('[data-testid="edit"]').click()
    cy.get('#title').clear().type('Изменённая задача')
    cy.get('button[type="submit"]').click()
    cy.contains('Изменённая задача').should('be.visible')
  })

  it('Удаление задачи', () => {
    cy.visit('/tasks')
    cy.contains('Изменённая задача').click()
    cy.get('[data-testid="delete"]').click()
    cy.contains('Изменённая задача').should('not.exist')
  })
})
```

---

**4. API тесты** ⭐⭐⭐
- Проверка эндпоинтов
- Валидация ответов
- Статус-коды
- Структура данных

**Почему:**
- ✅ Быстрые (нет UI)
- ✅ Стабильные
- ✅ Можно проверить больше сценариев
- ✅ Находят баги раньше

**Пример:**
```javascript
describe('API тесты', () => {
  it('Получение списка пользователей', () => {
    cy.request('/api/users')
      .then((response) => {
        expect(response.status).to.eq(200)
        expect(response.body).to.have.length.greaterThan(0)
        expect(response.body[0]).to.have.property('name')
        expect(response.body[0]).to.have.property('email')
      })
  })

  it('Создание пользователя', () => {
    cy.request({
      method: 'POST',
      url: '/api/users',
      body: {
        name: 'Test User',
        email: 'test@mail.com'
      }
    }).then((response) => {
      expect(response.status).to.eq(201)
      expect(response.body.name).to.eq('Test User')
    })
  })
})
```

---

**5. Регрессионные тесты на критический функционал** ⭐⭐
- Оформление заказа
- Оплата
- Корзина
- Фильтры/поиск

**Почему:**
- ✅ Часто проверяются перед релизом
- ✅ Критичны для бизнеса
- ✅ Занимают много времени вручную

---

**6. Формы с множеством полей** ⭐⭐
- Заполнение анкет
- Настройки профиля
- Сложные фильтры

**Почему:**
- ✅ Много времени на ручную проверку
- ✅ Легко ошибиться при ручном тесте
- ✅ Нужно проверять валидацию каждого поля

---

### ❌ Типичные ошибки новичков

#### Ошибка 1: Автоматизировать всё подряд
```javascript
// ❌ Плохо: автоматизация нестабильной фичи
describe('Новый калькулятор', () => {
  it('Проверка расчёта', () => {
    // Этот калькулятор переписывают каждый день
    // Тест будет ломаться постоянно
  })
})
```

**Решение:** Подожди, пока фича стабилизируется.

---

#### Ошибка 2: Не считать ROI
"Автоматизирую всё, потому что так надо!"

**Проблема:** Потратишь месяц на тесты, которые никто не запускает.

**Решение:** Считай выгоду перед началом автоматизации.

---

#### Ошибка 3: Автоматизация визуальных проверок
```javascript
// ❌ Плохо: автоматизация проверки "красиво ли"
it('Кнопка красивая', () => {
  // Как проверить автоматически, что "красиво"? 🤔
})
```

**Решение:** Визуальные проверки оставь для ручного тестирования.

---

### 🎓 Мини-практика

**Задача:** Определи, что автоматизировать в следующих сценариях.

**Проект: Интернет-магазин**

| Функционал | Частота теста | Время вручную | Стабильность | Автоматизировать? |
|------------|---------------|---------------|--------------|-------------------|
| Логин | 50 раз/неделю | 1 мин | Не менялся год | ? |
| Новая анимация кнопки | 2 раза/неделю | 30 сек | Меняется каждый день | ? |
| Фильтр товаров | 10 раз/неделю | 5 мин | Стабилен 6 месяцев | ? |
| Годовой отчёт | 1 раз/год | 30 мин | Стабилен | ? |
| Регистрация | 20 раз/неделю | 2 мин | Стабильна | ? |

**Твои ответы:**
1. Логин: __________ (✅ или ❌)
2. Анимация кнопки: __________ (✅ или ❌)
3. Фильтр: __________ (✅ или ❌)
4. Годовой отчёт: __________ (✅ или ❌)
5. Регистрация: __________ (✅ или ❌)

**Правильные ответы:**

<details>
<summary>Нажми, чтобы увидеть ответы</summary>

1. **Логин: ✅** — Частый, быстрый, стабильный. Идеально для автоматизации!
2. **Анимация кнопки: ❌** — Меняется каждый день. Будешь тратить больше времени на поддержку, чем экономить.
3. **Фильтр: ✅** — Средняя частота, занимает время, стабилен. Окупится за месяц.
4. **Годовой отчёт: ❌** — 1 раз в год. 30 минут в год экономии не стоит 4 часов на автоматизацию.
5. **Регистрация: ✅** — Частая, критичная, стабильная. Must have для автоматизации!

</details>

---

## Модуль 6. Основы E2E тестирования

### 🎯 Зачем это нужно в реальной работе

Cypress — это E2E фреймворк. Но что такое E2E? И почему все говорят, что UI тесты "хрупкие" и постоянно падают?

Понимание основ E2E тестирования:
- Поможет писать **стабильные** тесты, которые не падают без причины
- Научит понимать, **почему** тесты падают
- Покажет, **где не нужно** использовать E2E (иногда API тесты лучше)

**В реальной работе:**
- Junior пишет тесты, которые падают каждый день → команда перестаёт им доверять
- Middle пишет стабильные тесты → команда полагается на автоматизацию

Этот модуль сделает тебя Middle.

---

### 🔄 Что такое E2E тестирование

#### Что это такое:
E2E (End-to-End) — это тестирование "от начала до конца". Проверяем весь путь пользователя через систему.

**E2E = Имитация реального пользователя**

Тест делает всё то же самое, что делал бы реальный пользователь:
1. Открывает браузер
2. Переходит на сайт
3. Кликает по кнопкам
4. Вводит данные
5. Проверяет результат

#### Как это работает:

**Пример пути пользователя:**
```
Открыть сайт → Нажать "Регистрация" → Заполнить форму → 
Отправить → Получить email → Подтвердить → Войти в систему
```

**E2E тест проверяет ВЕСЬ этот путь целиком.**

#### 📖 Пример из жизни:

**Аналогия: Покупка в интернет-магазине**

**НЕ E2E (юнит-тест):**
- Проверить, что функция `calculatePrice()` правильно считает цену
- Это как проверить калькулятор отдельно от магазина

**E2E тест:**
1. Зайти на сайт  
2. Найти товар  
3. Добавить в корзину  
4. Перейти в корзину  
5. Оформить заказ  
6. Ввести данные карты  
7. Получить подтверждение заказа  

**Весь путь пользователя от начала до конца.**

---

#### Примеры E2E vs не E2E

**Пример 1: Регистрация**

```javascript
// ❌ Это НЕ E2E (проверка только API)
it('API создаёт пользователя', () => {
  cy.request('POST', '/api/register', {
    email: 'test@mail.com',
    password: '12345'
  }).then((response) => {
    expect(response.status).to.eq(201)
  })
})

// ✅ Это E2E (весь путь пользователя)
it('Пользователь может зарегистрироваться', () => {
  cy.visit('/register')                    // 1. Открыть страницу
  cy.get('#email').type('test@mail.com')   // 2. Ввести email
  cy.get('#password').type('12345')        // 3. Ввести пароль
  cy.get('#confirm').type('12345')         // 4. Подтвердить пароль
  cy.get('button').click()                 // 5. Нажать кнопку
  cy.url().should('include', '/dashboard') // 6. Проверить редирект
  cy.contains('Добро пожаловать!')         // 7. Увидеть приветствие
})
```

**Разница:**
- API тест: проверяет только бэкенд
- E2E тест: проверяет UI + бэкенд + весь флоу

---

#### Уровни тестирования (пирамида тестирования)

```
      /\
     /  \      E2E (мало, медленные, хрупкие)
    /____\     
   /      \    Integration тесты (средне)
  /________\   
 /          \  Unit тесты (много, быстрые, стабильные)
/____________\ 
```

**Unit тесты (70%):**
- Проверяют отдельные функции
- Быстрые (миллисекунды)
- Стабильные
- Пример: `expect(sum(2, 3)).toBe(5)`

**Integration тесты (20%):**
- Проверяют взаимодействие компонентов
- Средняя скорость (секунды)
- Пример: API тесты

**E2E тесты (10%):**
- Проверяют весь путь пользователя
- Медленные (минуты)
- Хрупкие (могут падать)
- Пример: Cypress тесты

#### ✅ Главное правило:

**E2E тестов должно быть МАЛО и они должны проверять КРИТИЧЕСКИЙ функционал.**

Не пытайся покрыть E2E тестами всё приложение!

---

### 💔 Почему UI тесты хрупкие (flaky)

#### Что это значит:
**Flaky тест** — это тест, который то проходит, то падает **без изменений в коде**.

Запускаешь тест:
- 1-й раз: ✅ Прошёл
- 2-й раз: ❌ Упал
- 3-й раз: ✅ Прошёл
- 4-й раз: ❌ Упал

**Почему это проблема:**
- Команда перестаёт доверять тестам
- Тратится время на расследование ложных падений
- CI/CD пайплайны блокируются

#### Почему UI тесты хрупкие:

**1. Асинхронность** ⏱️

UI — это асинхронная среда. Данные загружаются, анимации проигрываются, запросы уходят на сервер.

```javascript
// ❌ ПЛОХО: Тест не ждёт загрузки данных
it('Проверка списка', () => {
  cy.visit('/users')
  cy.get('.user-item').should('have.length', 10)
  // ❌ Может упасть, если данные ещё не загрузились
})

// ✅ ХОРОШО: Cypress автоматически ждёт
it('Проверка списка', () => {
  cy.visit('/users')
  cy.get('.user-item', { timeout: 10000 })
    .should('have.length', 10)
  // ✅ Cypress будет ждать до 10 секунд
})
```

---

**2. Хрупкие селекторы** 🎯

Селекторы могут измениться при рефакторинге UI.

```javascript
// ❌ ПЛОХО: Селектор по классу (может измениться)
cy.get('.btn-primary-large-rounded').click()

// ❌ ПЛОХО: Селектор по позиции (хрупкий)
cy.get('button').eq(2).click()

// ❌ ПЛОХО: Селектор по тексту (может измениться при переводе)
cy.contains('Submit').click()

// ✅ ХОРОШО: data-testid (стабильный)
cy.get('[data-testid="submit-button"]').click()
```

---

**3. Зависимость от данных** 📊

UI тесты зависят от данных в системе.

```javascript
// ❌ ПЛОХО: Зависит от существующих данных
it('Удаление пользователя', () => {
  cy.visit('/users')
  cy.contains('John Doe').click() // А что если John Doe удалили?
  cy.get('[data-testid="delete"]').click()
})

// ✅ ХОРОШО: Создаём свои тестовые данные
it('Удаление пользователя', () => {
  // Сначала создаём пользователя через API
  cy.request('POST', '/api/users', { name: 'Test User' })
  
  cy.visit('/users')
  cy.contains('Test User').click()
  cy.get('[data-testid="delete"]').click()
  cy.contains('Test User').should('not.exist')
})
```

---

**4. Влияние других тестов** 🔗

Тесты могут влиять друг на друга через общие данные.

```javascript
// ❌ ПЛОХО: Тесты зависят друг от друга
describe('Пользователи', () => {
  it('Создание пользователя', () => {
    // Создаёт пользователя "Test"
  })

  it('Удаление пользователя', () => {
    // Удаляет "Test"
    // ❌ Если этот тест упадёт, следующий тоже упадёт
  })

  it('Редактирование пользователя', () => {
    // Ищет "Test"
    // ❌ Но его уже нет! Тест упал
  })
})

// ✅ ХОРОШО: Каждый тест независимый
describe('Пользователи', () => {
  beforeEach(() => {
    // Создать чистое состояние перед каждым тестом
    cy.request('POST', '/api/reset-db')
  })

  it('Создание пользователя', () => {
    // Независимый тест
  })

  it('Удаление пользователя', () => {
    // Сам создаёт данные, потом удаляет
    cy.request('POST', '/api/users', { name: 'Test' })
    // ... удаление
  })
})
```

---

**5. Анимации и переходы** 🎨

Анимации могут мешать взаимодействию с элементами.

```javascript
// ❌ Может упасть: кнопка ещё анимируется
it('Клик по кнопке', () => {
  cy.get('#menu-button').click()
  cy.get('#dropdown-item').click() // Выпадашка ещё не открылась!
})

// ✅ ХОРОШО: Ждём появления элемента
it('Клик по кнопке', () => {
  cy.get('#menu-button').click()
  cy.get('#dropdown-item').should('be.visible').click()
})
```

---

**6. Внешние зависимости** 🌐

Зависимость от внешних сервисов (API, БД, браузера).

**Проблемы:**
- Медленный интернет
- Таймауты сервера
- Разные версии браузера
- Разное разрешение экрана

```javascript
// ❌ Зависит от внешнего API
it('Загрузка пользователей', () => {
  cy.visit('/users')
  // Что если сервер медленный или упал?
})

// ✅ ЛУЧШЕ: Мокировать внешние запросы
it('Загрузка пользователей', () => {
  cy.intercept('GET', '/api/users', {
    fixture: 'users.json' // Используем тестовые данные
  })
  cy.visit('/users')
  // Тест не зависит от реального API
})
```

---

### 🔥 Основные причины падения тестов

#### 1. Тайминги и ожидания

**Проблема:** Тест не дождался загрузки элемента.

```javascript
// ❌ ПЛОХО
cy.visit('/dashboard')
cy.get('.user-name').should('contain', 'John')
// Упадёт, если загрузка медленная

// ✅ ХОРОШО
cy.visit('/dashboard')
cy.get('.user-name', { timeout: 10000 })
  .should('contain', 'John')
```

**Решение:**
- Используй встроенные ожидания Cypress
- Увеличивай таймауты для медленных операций
- Проверяй, что элемент видим: `.should('be.visible')`

---

#### 2. Изменение селекторов

**Проблема:** Разработчик переименовал className.

```javascript
// Было:
<button class="submit-btn">Отправить</button>

// Стало:
<button class="primary-button">Отправить</button>

// Тест упал:
cy.get('.submit-btn').click() // ⚠️ Элемент не найден
```

**Решение:**
- Используй `data-testid`: `<button data-testid="submit">...</button>`
- Договорись с разработчиками не менять data-testid

```javascript
// ✅ Стабильно
cy.get('[data-testid="submit"]').click()
```

---

#### 3. Порядок выполнения тестов

**Проблема:** Тесты влияют друг на друга.

```javascript
// Тест 1
it('Создать пользователя Bob', () => {
  // Создаёт Bob
})

// Тест 2
it('Создать пользователя Bob', () => {
  // ❌ Упадёт: Bob уже существует!
})
```

**Решение:**
- Используй `beforeEach` для сброса состояния
- Создавай уникальные данные (timestamp, random)

```javascript
beforeEach(() => {
  cy.request('POST', '/api/reset-db')
})

it('Создать пользователя', () => {
  const uniqueName = `User_${Date.now()}`
  // Каждый раз уникальное имя
})
```

---

#### 4. Изменения в приложении

**Проблема:** Разработчики изменили логику.

```javascript
// Тест ожидает редирект на /dashboard
cy.url().should('include', '/dashboard')

// Но теперь редирект на /home
// ❌ Тест упал
```

**Решение:**
- Обновить тесты при изменении логики
- Это **нормально** — тесты должны отражать реальность

---

#### 5. Проблемы с окружением

**Проблемы:**
- Разные версии браузера
- Разное разрешение экрана
- Кэш браузера

**Решение:**
```javascript
// Очистка перед каждым тестом
beforeEach(() => {
  cy.clearCookies()
  cy.clearLocalStorage()
})

// Задать размер окна
beforeEach(() => {
  cy.viewport(1280, 720)
})
```

---

### ❌ Типичные ошибки новичков

#### Ошибка 1: Не использовать data-testid

```javascript
// ❌ Хрупкий селектор
cy.get('.btn.btn-primary.btn-lg').click()

// ✅ Стабильный селектор
cy.get('[data-testid="login-button"]').click()
```

---

#### Ошибка 2: Использовать sleep/wait

```javascript
// ❌ УЖАСНО
cy.visit('/users')
cy.wait(3000) // Тупое ожидание 3 секунды
cy.get('.user').should('exist')

// ✅ ПРАВИЛЬНО
cy.visit('/users')
cy.get('.user').should('exist') // Cypress сам подождёт
```

---

#### Ошибка 3: Тесты зависят друг от друга

```javascript
// ❌ ПЛОХО
describe('Тесты', () => {
  it('Создать пользователя', () => { /*...*/ })
  it('Удалить пользователя', () => { 
    // Зависит от первого теста!
  })
})

// ✅ ХОРОШО
describe('Тесты', () => {
  it('Создать и удалить пользователя', () => {
    // Всё в одном тесте
  })
  
  // Или каждый тест создаёт свои данные
})
```

---

### 🎓 Мини-практика

**Задача:** Найди проблемы в этих тестах и исправь их.

**Тест 1:**
```javascript
it('Проверка логина', () => {
  cy.visit('/login')
  cy.get('input').eq(0).type('user@mail.com')
  cy.get('input').eq(1).type('12345')
  cy.get('button').click()
  cy.wait(2000)
  cy.get('.welcome').should('exist')
})
```

**Проблемы:**
1. ________________________
2. ________________________
3. ________________________

<details>
<summary>Правильный ответ</summary>

**Проблемы:**
1. Хрупкие селекторы: `.eq(0)`, `.eq(1)`, `button` (непонятно какие это элементы)
2. Использование `cy.wait(2000)` (тупое ожидание)
3. Нет проверки URL или конкретного текста

**Исправленный вариант:**
```javascript
it('Проверка логина', () => {
  cy.visit('/login')
  cy.get('[data-testid="email-input"]').type('user@mail.com')
  cy.get('[data-testid="password-input"]').type('12345')
  cy.get('[data-testid="submit-button"]').click()
  cy.url().should('include', '/dashboard')
  cy.get('[data-testid="welcome-message"]')
    .should('be.visible')
    .and('contain', 'Добро пожаловать')
})
```
</details>

---

**Тест 2:**
```javascript
describe('CRUD пользователей', () => {
  it('Создание', () => {
    cy.visit('/users')
    cy.contains('Добавить').click()
    cy.get('#name').type('John')
    cy.get('form').submit()
  })

  it('Удаление', () => {
    cy.visit('/users')
    cy.contains('John').click()
    cy.contains('Удалить').click()
  })
})
```

**Проблема:** ________________________

<details>
<summary>Правильный ответ</summary>

**Проблема:** Тесты зависят друг от друга. Второй тест ожидает, что "John" уже создан первым тестом.

**Решение 1: Сделать тесты независимыми**
```javascript
it('Создание', () => {
  cy.visit('/users')
  cy.contains('Добавить').click()
  cy.get('[data-testid="name"]').type('John')
  cy.get('form').submit()
  cy.contains('John').should('be.visible')
})

it('Удаление', () => {
  // Создаём своего пользователя
  cy.request('POST', '/api/users', { name: 'TestUser' })
  
  cy.visit('/users')
  cy.contains('TestUser').click()
  cy.get('[data-testid="delete"]').click()
  cy.contains('TestUser').should('not.exist')
})
```

**Решение 2: Объединить в один E2E тест**
```javascript
it('Создание и удаление пользователя', () => {
  cy.visit('/users')
  
  // Создание
  cy.contains('Добавить').click()
  cy.get('[data-testid="name"]').type('John')
  cy.get('form').submit()
  cy.contains('John').should('be.visible')
  
  // Удаление
  cy.contains('John').click()
  cy.get('[data-testid="delete"]').click()
  cy.contains('John').should('not.exist')
})
```
</details>

---

## 🎯 Итоги Этапа 2

### Что ты узнал:

✅ **Когда автоматизация выгодна:**
- Частые проверки
- Стабильный функционал
- Экономия времени > затраты на написание

✅ **Как считать ROI:**
- Экономия в неделю × 4 = Экономия в месяц
- Затраты ÷ Экономия = Срок окупаемости
- Автоматизируй то, что окупится < 1 месяца

✅ **Что автоматизировать в первую очередь:**
- Логин/регистрация
- Smoke тесты
- CRUD операции
- API проверки

✅ **Почему UI тесты хрупкие:**
- Асинхронность
- Хрупкие селекторы
- Зависимость от данных
- Влияние других тестов

✅ **Как писать стабильные тесты:**
- Используй `data-testid`
- Избегай `cy.wait(time)`
- Делай тесты независимыми
- Мокируй внешние зависимости

---

### Чек-лист перед переходом к следующему этапу:

- [ ] Понимаешь, когда автоматизация выгодна
- [ ] Умеешь считать ROI автоматизации
- [ ] Знаешь, что такое E2E тестирование
- [ ] Понимаешь, почему UI тесты падают
- [ ] Можешь отличить хороший тест от плохого

---

### Следующий шаг:

**ЭТАП 3: Cypress с нуля**

Теперь, когда ты понимаешь **ЗАЧЕМ** нужна автоматизация и **ЧТО** нужно автоматизировать, пора научиться **КАК** это делать с помощью Cypress!

🚀 Переходи к Этапу 3!
