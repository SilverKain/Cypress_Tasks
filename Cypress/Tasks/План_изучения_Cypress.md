# 📚 План изучения Cypress: От новичка до профессионала

## Введение

Cypress - это современный инструмент для автоматизации тестирования веб-приложений. Он работает прямо в браузере, что делает тесты быстрыми и надежными. Давайте пройдем путь от азов до профессионального уровня!

---

## 🎯 Уровень 1: Основы тестирования (1-2 недели)

### Что такое тестирование?

**Простыми словами:** Тестирование - это проверка того, что программа работает как задумано.

**Пример из жизни:**
- Вы покупаете телефон и проверяете: включается ли он, работает ли камера, звонит ли он
- Это и есть тестирование!

### Виды тестирования

#### 1. Ручное тестирование
Когда вы сами кликаете мышкой, вводите текст, проверяете результат.

**Пример:**
- Открыли сайт
- Нажали кнопку "Войти"
- Ввели логин и пароль
- Проверили, что вошли в систему

#### 2. Автоматизированное тестирование
Программа делает это за вас! Именно для этого нужен Cypress.

### Зачем автоматизировать?

✅ Экономия времени (тесты работают быстрее)  
✅ Можно запускать ночью, пока вы спите  
✅ Избежание человеческих ошибок  
✅ Можно проверить сразу в разных браузерах  

---

## 🚀 Уровень 2: Знакомство с Cypress (1 неделя)

### Что такое Cypress?

Cypress - это инструмент, который умеет:
- Открывать сайты
- Кликать на кнопки
- Вводить текст
- Проверять, что на странице есть нужный текст

**Представьте робота**, который делает всё то же самое, что делаете вы вручную!

### Установка Cypress

#### Шаг 1: Установка Node.js
Node.js - это среда для запуска JavaScript. Скачайте с официального сайта.

#### Шаг 2: Создание проекта
```bash
mkdir my-cypress-project
cd my-cypress-project
npm init -y
```

#### Шаг 3: Установка Cypress
```bash
npm install cypress --save-dev
```

#### Шаг 4: Открытие Cypress
```bash
npx cypress open
```

### Структура проекта

После установки у вас появятся папки:
```
cypress/
  ├── e2e/           → Здесь ваши тесты
  ├── fixtures/      → Тестовые данные (например, JSON файлы)
  ├── support/       → Вспомогательные функции
  └── downloads/     → Скачанные файлы во время тестов
```

---

## 📝 Уровень 3: Первый тест (1-2 недели)

### Структура теста Cypress

Каждый тест состоит из трех частей (методология AAA):
1. **Arrange (Подготовка)** - открываем страницу
2. **Act (Действие)** - что-то делаем
3. **Assert (Проверка)** - проверяем результат

### Пример 1: Самый простой тест

```javascript
describe('Мой первый тест', () => {
  it('Открывает Google', () => {
    // Подготовка: открываем сайт
    cy.visit('https://www.google.com')
    
    // Проверка: смотрим, что на странице есть поле поиска
    cy.get('textarea[name="q"]').should('be.visible')
  })
})
```

**Разбор:**
- `describe` - группа тестов (как папка)
- `it` - конкретный тест (как файл)
- `cy.visit()` - открыть страницу
- `cy.get()` - найти элемент
- `should('be.visible')` - проверить, что элемент виден

### Пример 2: Тест с действием

```javascript
describe('Поиск в Google', () => {
  it('Ищет слово Cypress', () => {
    // Шаг 1: Открываем Google
    cy.visit('https://www.google.com')
    
    // Шаг 2: Вводим текст в поиск
    cy.get('textarea[name="q"]').type('Cypress testing')
    
    // Шаг 3: Нажимаем Enter
    cy.get('textarea[name="q"]').type('{enter}')
    
    // Шаг 4: Проверяем, что результаты появились
    cy.contains('Cypress').should('be.visible')
  })
})
```

### Основные команды Cypress

| Команда | Что делает | Пример |
|---------|------------|---------|
| `cy.visit()` | Открыть страницу | `cy.visit('https://site.com')` |
| `cy.get()` | Найти элемент | `cy.get('.button')` |
| `cy.contains()` | Найти по тексту | `cy.contains('Войти')` |
| `cy.click()` | Кликнуть | `cy.get('.btn').click()` |
| `cy.type()` | Ввести текст | `cy.get('#email').type('test@mail.com')` |
| `cy.should()` | Проверить | `cy.get('.title').should('be.visible')` |

---

## 🎨 Уровень 4: Селекторы - как найти элемент (1-2 недели)

### Что такое селектор?

Селектор - это "адрес" элемента на странице. Как вы объясняете таксисту, куда ехать!

### Виды селекторов

#### 1. По ID (самый надежный)
```javascript
cy.get('#username')  // элемент с id="username"
```

#### 2. По классу
```javascript
cy.get('.btn-primary')  // элемент с class="btn-primary"
```

#### 3. По тегу
```javascript
cy.get('button')  // все кнопки
```

#### 4. По атрибуту
```javascript
cy.get('[type="email"]')  // элемент с type="email"
cy.get('[data-test="login-button"]')  // по data-атрибуту (лучшая практика!)
```

#### 5. По тексту
```javascript
cy.contains('Войти')  // элемент с текстом "Войти"
```

### Комбинированные селекторы

```javascript
// Кнопка с классом submit
cy.get('button.submit')

// Инпут внутри формы
cy.get('form input[name="email"]')

// Первый элемент списка
cy.get('ul li').first()

// Последний элемент
cy.get('ul li').last()

// Конкретный по индексу (начинается с 0)
cy.get('ul li').eq(2)  // третий элемент
```

### Практика: Найдите элементы

```html
<!-- HTML страница -->
<div>
  <h1 id="title">Добро пожаловать</h1>
  <form class="login-form">
    <input type="text" name="username" placeholder="Логин">
    <input type="password" name="password" placeholder="Пароль">
    <button class="btn btn-primary" data-test="submit">Войти</button>
  </form>
</div>
```

```javascript
// Решения:
cy.get('#title')                    // заголовок
cy.get('[name="username"]')         // поле логина
cy.get('[type="password"]')         // поле пароля
cy.get('[data-test="submit"]')      // кнопка (лучший способ!)
cy.contains('Войти')                // кнопка по тексту
```

---

## 🔥 Уровень 5: Assertions - проверки (2 недели)

### Что такое assertion?

Assertion (утверждение) - это проверка, что результат соответствует ожиданию.

**Пример из жизни:** Вы заказали пиццу "Маргарита". Вам привезли пиццу, и вы проверяете (assert):
- ✅ Есть ли сыр?
- ✅ Есть ли томаты?
- ✅ Пицца горячая?

### Основные проверки

#### 1. Видимость элемента
```javascript
cy.get('.header').should('be.visible')       // элемент виден
cy.get('.popup').should('not.be.visible')    // элемент не виден
cy.get('.menu').should('exist')              // элемент есть в DOM
cy.get('.error').should('not.exist')         // элемента нет
```

#### 2. Текст
```javascript
cy.get('h1').should('have.text', 'Главная')
cy.get('.message').should('contain', 'Успешно')
cy.get('.title').should('include.text', 'Добро')
```

#### 3. Атрибуты
```javascript
cy.get('input').should('have.attr', 'type', 'email')
cy.get('button').should('have.class', 'active')
cy.get('a').should('have.attr', 'href', '/home')
```

#### 4. Значение (для input полей)
```javascript
cy.get('#username').should('have.value', 'admin')
cy.get('#email').should('be.empty')
```

#### 5. Состояние
```javascript
cy.get('button').should('be.enabled')        // кнопка активна
cy.get('button').should('be.disabled')       // кнопка неактивна
cy.get('checkbox').should('be.checked')      // чекбокс отмечен
cy.get('checkbox').should('not.be.checked')  // не отмечен
```

#### 6. Количество элементов
```javascript
cy.get('li').should('have.length', 5)        // ровно 5 элементов
cy.get('.item').should('have.length.gt', 0)  // больше 0
cy.get('.row').should('have.length.at.least', 3)  // минимум 3
```

### Практический пример

```javascript
describe('Проверка формы регистрации', () => {
  it('Валидирует все поля', () => {
    cy.visit('https://example.com/register')
    
    // Проверяем заголовок
    cy.get('h1').should('have.text', 'Регистрация')
    
    // Проверяем, что кнопка неактивна
    cy.get('[data-test="submit"]').should('be.disabled')
    
    // Заполняем форму
    cy.get('#email').type('test@example.com')
    cy.get('#password').type('Pass123!')
    
    // Проверяем, что значения ввелись
    cy.get('#email').should('have.value', 'test@example.com')
    
    // Проверяем, что кнопка теперь активна
    cy.get('[data-test="submit"]').should('be.enabled')
  })
})
```

---

## 🛠 Уровень 6: Продвинутые техники (2-3 недели)

### 1. Хуки (Hooks) - подготовка к тестам

Хуки - это функции, которые запускаются в определенное время.

```javascript
describe('Тесты магазина', () => {
  
  // Запускается один раз перед всеми тестами
  before(() => {
    console.log('Подготовка: загружаем данные')
  })
  
  // Запускается перед каждым тестом
  beforeEach(() => {
    cy.visit('https://shop.com')
    cy.login('user@test.com', 'password123')  // создадим эту команду позже
  })
  
  // Запускается после каждого теста
  afterEach(() => {
    cy.clearCookies()
    cy.clearLocalStorage()
  })
  
  // Запускается один раз после всех тестов
  after(() => {
    console.log('Очистка: удаляем тестовые данные')
  })
  
  it('Добавляет товар в корзину', () => {
    cy.get('.product').first().click()
    cy.get('[data-test="add-to-cart"]').click()
    cy.get('.cart-count').should('have.text', '1')
  })
  
  it('Удаляет товар из корзины', () => {
    cy.get('.cart').click()
    cy.get('.remove-item').click()
    cy.get('.cart-empty').should('be.visible')
  })
})
```

### 2. Кастомные команды

Создайте свои собственные команды для повторяющихся действий!

**Файл:** `cypress/support/commands.js`

```javascript
// Команда для логина
Cypress.Commands.add('login', (email, password) => {
  cy.visit('/login')
  cy.get('[data-test="email"]').type(email)
  cy.get('[data-test="password"]').type(password)
  cy.get('[data-test="submit"]').click()
  cy.url().should('include', '/dashboard')
})

// Команда для добавления товара в корзину
Cypress.Commands.add('addToCart', (productName) => {
  cy.contains(productName).click()
  cy.get('[data-test="add-to-cart"]').click()
  cy.get('.notification').should('contain', 'Добавлено в корзину')
})
```

**Использование:**

```javascript
describe('Покупка товара', () => {
  it('Пользователь покупает книгу', () => {
    // Используем нашу кастомную команду
    cy.login('test@mail.com', 'password123')
    
    // И еще одну
    cy.addToCart('Изучаем Cypress')
    
    cy.get('.cart').click()
    cy.get('.checkout').click()
  })
})
```

### 3. Работа с Fixtures (тестовые данные)

Храните данные в отдельных файлах!

**Файл:** `cypress/fixtures/users.json`

```json
{
  "admin": {
    "email": "admin@test.com",
    "password": "Admin123!",
    "role": "admin"
  },
  "user": {
    "email": "user@test.com",
    "password": "User123!",
    "role": "user"
  }
}
```

**Использование:**

```javascript
describe('Разные роли пользователей', () => {
  
  it('Логин как админ', () => {
    cy.fixture('users').then((users) => {
      cy.login(users.admin.email, users.admin.password)
      cy.get('.role').should('have.text', 'admin')
    })
  })
  
  it('Логин как обычный пользователь', () => {
    cy.fixture('users').then((users) => {
      cy.login(users.user.email, users.user.password)
      cy.get('.role').should('have.text', 'user')
    })
  })
})
```

### 4. Работа с API

Cypress может не только кликать по кнопкам, но и работать с API!

```javascript
describe('Тесты API', () => {
  
  it('Получает список пользователей', () => {
    cy.request('GET', 'https://api.example.com/users')
      .then((response) => {
        expect(response.status).to.eq(200)
        expect(response.body).to.have.length(10)
      })
  })
  
  it('Создает нового пользователя', () => {
    cy.request('POST', 'https://api.example.com/users', {
      name: 'Иван Иванов',
      email: 'ivan@test.com'
    }).then((response) => {
      expect(response.status).to.eq(201)
      expect(response.body.name).to.eq('Иван Иванов')
    })
  })
})
```

**Зачем это нужно?**
- Быстрая подготовка данных (создать пользователя через API быстрее, чем через UI)
- Проверка того, что UI корректно взаимодействует с backend

### 5. Перехват запросов (Intercept)

Можно "подслушивать" и изменять запросы!

```javascript
describe('Перехват запросов', () => {
  
  it('Ждет загрузки данных', () => {
    // Перехватываем GET запрос
    cy.intercept('GET', '/api/products').as('getProducts')
    
    cy.visit('/catalog')
    
    // Ждем, пока запрос завершится
    cy.wait('@getProducts')
    
    cy.get('.product').should('have.length.gt', 0)
  })
  
  it('Имитирует ошибку сети', () => {
    // Возвращаем ошибку 500
    cy.intercept('GET', '/api/products', {
      statusCode: 500,
      body: { error: 'Server Error' }
    })
    
    cy.visit('/catalog')
    cy.get('.error-message').should('be.visible')
  })
  
  it('Подменяет данные', () => {
    // Возвращаем фейковые данные
    cy.intercept('GET', '/api/products', {
      statusCode: 200,
      body: [
        { id: 1, name: 'Тестовый товар', price: 100 }
      ]
    })
    
    cy.visit('/catalog')
    cy.contains('Тестовый товар').should('be.visible')
  })
})
```

---

## 🎯 Уровень 7: Лучшие практики (1-2 недели)

### 1. Используйте data-атрибуты

❌ **Плохо:**
```javascript
cy.get('.btn.btn-primary.mt-3.disabled')  // сломается при смене стилей
```

✅ **Хорошо:**
```javascript
cy.get('[data-test="submit-button"]')  // не зависит от стилей
```

### 2. Не используйте фиксированные ожидания

❌ **Плохо:**
```javascript
cy.wait(5000)  // что если загрузится за 1 секунду? или за 10?
cy.get('.data').should('be.visible')
```

✅ **Хорошо:**
```javascript
cy.get('.data', { timeout: 10000 }).should('be.visible')
// или
cy.intercept('GET', '/api/data').as('getData')
cy.wait('@getData')
```

### 3. Один тест = одна проверка

❌ **Плохо:**
```javascript
it('Проверяет всё приложение', () => {
  cy.visit('/')
  cy.login()
  cy.createProduct()
  cy.editProduct()
  cy.deleteProduct()
  cy.logout()
  // Если упадет первый шаг, не узнаем о багах в остальных
})
```

✅ **Хорошо:**
```javascript
it('Создает продукт', () => {
  cy.login()
  cy.createProduct()
})

it('Редактирует продукт', () => {
  cy.login()
  cy.createProduct()
  cy.editProduct()
})

it('Удаляет продукт', () => {
  cy.login()
  cy.createProduct()
  cy.deleteProduct()
})
```

### 4. Используйте Page Object Model (POM)

Организуйте код по страницам!

**Файл:** `cypress/support/pages/LoginPage.js`

```javascript
class LoginPage {
  visit() {
    cy.visit('/login')
  }
  
  fillEmail(email) {
    cy.get('[data-test="email"]').type(email)
    return this  // для chain вызовов
  }
  
  fillPassword(password) {
    cy.get('[data-test="password"]').type(password)
    return this
  }
  
  submit() {
    cy.get('[data-test="submit"]').click()
  }
  
  login(email, password) {
    this.visit()
    this.fillEmail(email)
    this.fillPassword(password)
    this.submit()
  }
}

export default new LoginPage()
```

**Использование:**

```javascript
import LoginPage from '../support/pages/LoginPage'

describe('Тесты логина', () => {
  it('Логин с валидными данными', () => {
    LoginPage.login('test@mail.com', 'password123')
    cy.url().should('include', '/dashboard')
  })
})
```

### 5. Независимость тестов

Каждый тест должен работать независимо!

```javascript
describe('Тесты корзины', () => {
  
  beforeEach(() => {
    // Каждый тест начинается с чистого состояния
    cy.clearCookies()
    cy.clearLocalStorage()
    cy.visit('/')
  })
  
  it('Тест 1', () => {
    // независимый тест
  })
  
  it('Тест 2', () => {
    // независимый тест
  })
})
```

---

## 🚀 Уровень 8: Настройка и оптимизация (1 неделя)

### Конфигурация Cypress

**Файл:** `cypress.config.js`

```javascript
const { defineConfig } = require('cypress')

module.exports = defineConfig({
  e2e: {
    // Базовый URL (не нужно писать в каждом тесте)
    baseUrl: 'https://myapp.com',
    
    // Размер окна браузера
    viewportWidth: 1920,
    viewportHeight: 1080,
    
    // Таймауты
    defaultCommandTimeout: 10000,  // ожидание команды
    pageLoadTimeout: 60000,        // загрузка страницы
    
    // Скриншоты и видео
    screenshotOnRunFailure: true,
    video: true,
    videoCompression: 32,
    
    // Количество попыток при падении теста
    retries: {
      runMode: 2,      // в режиме запуска (CI/CD)
      openMode: 0      // в режиме разработки
    },
    
    // Переменные окружения
    env: {
      apiUrl: 'https://api.myapp.com',
      adminEmail: 'admin@test.com'
    }
  }
})
```

**Использование переменных:**

```javascript
it('Использует переменные окружения', () => {
  cy.visit(Cypress.env('apiUrl'))
  cy.login(Cypress.env('adminEmail'), 'password')
})
```

### Запуск тестов

```bash
# Открыть Cypress UI
npx cypress open

# Запустить все тесты в headless режиме
npx cypress run

# Запустить конкретный файл
npx cypress run --spec "cypress/e2e/login.cy.js"

# Запустить в конкретном браузере
npx cypress run --browser chrome
npx cypress run --browser firefox
npx cypress run --browser edge

# Запустить с конкретными переменными
npx cypress run --env apiUrl=https://staging.api.com
```

### Организация тестов

```
cypress/
  ├── e2e/
  │   ├── auth/
  │   │   ├── login.cy.js
  │   │   └── registration.cy.js
  │   ├── catalog/
  │   │   ├── products.cy.js
  │   │   └── search.cy.js
  │   └── cart/
  │       └── checkout.cy.js
  ├── fixtures/
  │   ├── users.json
  │   └── products.json
  └── support/
      ├── commands.js
      └── pages/
          ├── LoginPage.js
          └── CartPage.js
```

---

## 🎓 Уровень 9: Реальные сценарии (2-3 недели)

### Сценарий 1: E-commerce магазин

```javascript
describe('Покупка товара (E2E)', () => {
  
  it('Полный путь покупки', () => {
    // 1. Регистрация
    cy.visit('/register')
    cy.get('[data-test="name"]').type('Иван Петров')
    cy.get('[data-test="email"]').type('ivan@test.com')
    cy.get('[data-test="password"]').type('Pass123!')
    cy.get('[data-test="submit"]').click()
    
    // 2. Поиск товара
    cy.get('[data-test="search"]').type('ноутбук{enter}')
    cy.get('.product-card').should('have.length.gt', 0)
    
    // 3. Фильтрация
    cy.get('[data-test="filter-price"]').click()
    cy.get('[data-test="price-min"]').type('30000')
    cy.get('[data-test="price-max"]').type('50000')
    cy.get('[data-test="apply-filter"]').click()
    
    // 4. Добавление в корзину
    cy.get('.product-card').first().find('[data-test="add-to-cart"]').click()
    cy.get('.notification').should('contain', 'Добавлено в корзину')
    
    // 5. Оформление заказа
    cy.get('[data-test="cart-icon"]').click()
    cy.get('[data-test="checkout"]').click()
    
    // 6. Заполнение адреса доставки
    cy.get('[data-test="address"]').type('ул. Пушкина, д. 10')
    cy.get('[data-test="city"]').type('Москва')
    cy.get('[data-test="phone"]').type('+79991234567')
    
    // 7. Подтверждение заказа
    cy.get('[data-test="confirm-order"]').click()
    
    // 8. Проверка успеха
    cy.get('.success-message').should('contain', 'Заказ оформлен')
    cy.url().should('include', '/order-confirmation')
  })
})
```

### Сценарий 2: Форма с валидацией

```javascript
describe('Валидация формы контактов', () => {
  
  beforeEach(() => {
    cy.visit('/contact')
  })
  
  it('Показывает ошибки для пустых полей', () => {
    cy.get('[data-test="submit"]').click()
    
    cy.get('[data-test="name-error"]')
      .should('be.visible')
      .and('contain', 'Имя обязательно')
    
    cy.get('[data-test="email-error"]')
      .should('be.visible')
      .and('contain', 'Email обязателен')
  })
  
  it('Показывает ошибку для невалидного email', () => {
    cy.get('[data-test="email"]').type('невалидный-email')
    cy.get('[data-test="submit"]').click()
    
    cy.get('[data-test="email-error"]')
      .should('contain', 'Введите корректный email')
  })
  
  it('Успешно отправляет форму', () => {
    cy.get('[data-test="name"]').type('Иван')
    cy.get('[data-test="email"]').type('ivan@test.com')
    cy.get('[data-test="message"]').type('Тестовое сообщение')
    cy.get('[data-test="submit"]').click()
    
    cy.get('.success-notification')
      .should('be.visible')
      .and('contain', 'Сообщение отправлено')
  })
})
```

### Сценарий 3: Работа с таблицей

```javascript
describe('Управление таблицей пользователей', () => {
  
  beforeEach(() => {
    cy.login('admin@test.com', 'admin123')
    cy.visit('/users')
  })
  
  it('Сортирует таблицу по имени', () => {
    cy.get('[data-test="sort-name"]').click()
    
    // Проверяем, что первый элемент начинается с A
    cy.get('tbody tr').first()
      .find('td').first()
      .invoke('text')
      .should('match', /^A/)
  })
  
  it('Фильтрует пользователей', () => {
    cy.get('[data-test="filter-input"]').type('Иван')
    
    cy.get('tbody tr').each(($row) => {
      cy.wrap($row).should('contain', 'Иван')
    })
  })
  
  it('Удаляет пользователя', () => {
    // Считаем количество строк до удаления
    cy.get('tbody tr').its('length').then((initialCount) => {
      
      // Удаляем первого пользователя
      cy.get('tbody tr').first()
        .find('[data-test="delete-btn"]').click()
      
      cy.get('[data-test="confirm-delete"]').click()
      
      // Проверяем, что стало на 1 меньше
      cy.get('tbody tr').should('have.length', initialCount - 1)
    })
  })
})
```

---

## 🏆 Уровень 10: Профессиональные практики (2-3 недели)

### CI/CD интеграция

**GitHub Actions** (`.github/workflows/cypress.yml`):

```yaml
name: Cypress Tests

on: [push, pull_request]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Cypress run
        uses: cypress-io/github-action@v5
        with:
          browser: chrome
          headless: true
          
      - name: Upload screenshots
        uses: actions/upload-artifact@v3
        if: failure()
        with:
          name: cypress-screenshots
          path: cypress/screenshots
```

### Параллельное выполнение

```json
{
  "scripts": {
    "test": "cypress run",
    "test:parallel": "cypress run --record --parallel --group UI-Tests"
  }
}
```

### Отчеты о тестировании

```bash
npm install --save-dev mochawesome mochawesome-merge mochawesome-report-generator
```

**cypress.config.js:**

```javascript
module.exports = defineConfig({
  e2e: {
    reporter: 'mochawesome',
    reporterOptions: {
      reportDir: 'cypress/reports',
      overwrite: false,
      html: true,
      json: true
    }
  }
})
```

### Управление тестовыми данными

```javascript
// cypress/support/dataManager.js

class DataManager {
  createUser(userData) {
    return cy.request('POST', '/api/users', userData)
      .then((response) => response.body)
  }
  
  deleteUser(userId) {
    return cy.request('DELETE', `/api/users/${userId}`)
  }
  
  cleanupTestData() {
    return cy.request('DELETE', '/api/test-data')
  }
}

export default new DataManager()
```

**Использование:**

```javascript
import DataManager from '../support/dataManager'

describe('Тесты с динамическими данными', () => {
  
  let testUser
  
  before(() => {
    // Создаем тестового пользователя через API
    DataManager.createUser({
      email: 'test@example.com',
      password: 'Test123!'
    }).then((user) => {
      testUser = user
    })
  })
  
  after(() => {
    // Удаляем тестовые данные
    if (testUser) {
      DataManager.deleteUser(testUser.id)
    }
  })
  
  it('Пользователь может войти', () => {
    cy.login(testUser.email, 'Test123!')
    cy.url().should('include', '/dashboard')
  })
})
```

---

## 📚 Дополнительные темы

### Работа с iframe

```javascript
cy.get('iframe').then(($iframe) => {
  const $body = $iframe.contents().find('body')
  cy.wrap($body).find('.button').click()
})
```

### Загрузка файлов

```bash
npm install --save-dev cypress-file-upload
```

```javascript
cy.get('[data-test="file-input"]')
  .attachFile('example.jpg')
```

### Работа с cookie и localStorage

```javascript
// Cookie
cy.setCookie('session_id', '123456')
cy.getCookie('session_id').should('have.property', 'value', '123456')
cy.clearCookie('session_id')

// LocalStorage
cy.window().then((win) => {
  win.localStorage.setItem('token', 'abc123')
})

cy.getAllLocalStorage()
cy.clearLocalStorage()
```

### Скриншоты и видео

```javascript
// Скриншот всей страницы
cy.screenshot('my-screenshot')

// Скриншот конкретного элемента
cy.get('.header').screenshot('header')

// Автоматическое видео при падении теста включено по умолчанию
```

---

## 🎯 План обучения по неделям

### Недели 1-2: Основы
- [ ] Понимание тестирования
- [ ] Установка Cypress
- [ ] Первый тест
- [ ] Базовые команды

### Недели 3-4: Селекторы и проверки
- [ ] Все виды селекторов
- [ ] Assertions
- [ ] Практика на реальных сайтах

### Недели 5-6: Продвинутые техники
- [ ] Хуки
- [ ] Кастомные команды
- [ ] Fixtures
- [ ] API тестирование

### Недели 7-8: Практика
- [ ] Page Object Model
- [ ] Реальные сценарии
- [ ] Лучшие практики

### Недели 9-10: Профессионализм
- [ ] CI/CD
- [ ] Отчеты
- [ ] Оптимизация тестов
- [ ] Управление данными

---

## 💡 Советы для успешного обучения

### 1. Практикуйтесь каждый день
Хотя бы 30 минут - это лучше, чем 5 часов раз в неделю.

### 2. Пишите реальные тесты
Выберите любимый сайт и пишите тесты для него:
- Google
- YouTube
- Интернет-магазин
- Социальные сети

### 3. Читайте чужой код
Смотрите открытые проекты на GitHub с тестами Cypress.

### 4. Задавайте вопросы
- Stack Overflow
- Официальная документация Cypress
- Discord сообщества

### 5. Делайте перерывы
Мозгу нужен отдых для усвоения информации!

---

## 🔗 Полезные ресурсы

### Официальная документация
- [Cypress Documentation](https://docs.cypress.io)
- [Best Practices](https://docs.cypress.io/guides/references/best-practices)

### Видео и курсы
- YouTube канал Cypress
- Курсы на Udemy
- Бесплатные туториалы

### Сообщества
- [Cypress Discord](https://discord.com/invite/cypress)
- [GitHub Discussions](https://github.com/cypress-io/cypress/discussions)

### Примеры проектов
- [Cypress RealWorld App](https://github.com/cypress-io/cypress-realworld-app)
- [Cypress Examples](https://github.com/cypress-io/cypress-example-recipes)

---

## ✅ Чек-лист: "Я готов к работе с Cypress"

- [ ] Понимаю разницу между ручным и автоматизированным тестированием
- [ ] Могу установить и настроить Cypress
- [ ] Пишу простые тесты с базовыми командами
- [ ] Уверенно использую селекторы
- [ ] Применяю assertions для проверок
- [ ] Создаю кастомные команды
- [ ] Использую fixtures для данных
- [ ] Работаю с API в тестах
- [ ] Применяю Page Object Model
- [ ] Организую тесты по структуре проекта
- [ ] Настраиваю CI/CD
- [ ] Генерирую отчеты о тестировании

---

## 🎉 Заключение

Поздравляю! Вы прошли путь от новичка до профессионала в Cypress. Помните:

> **Тестирование - это не проверка того, что всё работает.**  
> **Это поиск того, что может сломаться!**

Продолжайте практиковаться, учите других, и вы станете настоящим экспертом!

**Успехов в изучении Cypress! 🚀**

---

_Версия: 1.0_  
_Обновлено: Февраль 2026_
