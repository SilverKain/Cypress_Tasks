# ЭТАП 3: Cypress с нуля

---

## Модуль 7. Установка и структура

### 🎯 Зачем это нужно в реальной работе

Это твой первый практический шаг в автоматизацию. После этого модуля ты сможешь:
- Установить Cypress на любой машине
- Понимать структуру проекта
- Запустить первый тест
- Объяснить коллегам, как настроить окружение

**В реальной работе:**
- Тебя возьмут на проект → попросят настроить Cypress на твоей машине
- Ты должен понимать, что и зачем устанавливаешь
- Понимать структуру, чтобы не потеряться в файлах

---

### 🟢 Node.js — что это и зачем

#### Что это такое:
Node.js — это программа, которая позволяет запускать JavaScript код **вне браузера** (на твоём компьютере).

#### Зачем нужен для Cypress:
Cypress написан на JavaScript и работает через Node.js. Без Node.js Cypress не запустится.

#### 📖 Простая аналогия:

**Node.js = Двигатель для JavaScript**

Как Java нужна Java Virtual Machine (JVM), так JavaScript для работы на компьютере нужен Node.js.

**Браузер тоже запускает JavaScript, но:**
- Браузер: JavaScript для веб-страниц
- Node.js: JavaScript для программ на компьютере

#### Как установить Node.js:

**Шаг 1: Скачать**
1. Перейди на [nodejs.org](https://nodejs.org/)
2. Скачай **LTS версию** (Long Term Support — стабильная версия)
3. Установи как обычную программу (Next → Next → Install)

**Шаг 2: Проверить установку**

Открой PowerShell (или терминал) и выполни:

```bash
node --version
```

**Ожидаемый результат:**
```
v20.11.0
```

Если видишь версию — Node.js установлен! ✅

**Разбор команды:**
- `node` — запуск Node.js
- `--version` — показать версию

---

### 📦 npm — менеджер пакетов

#### Что это такое:
npm (Node Package Manager) — это инструмент для установки библиотек и пакетов JavaScript.

#### Зачем нужен:
С помощью npm ты будешь устанавливать Cypress и другие библиотеки для тестирования.

#### 📖 Простая аналогия:

**npm = Магазин приложений для JavaScript**

Как App Store для iPhone или Google Play для Android, npm — это магазин библиотек для разработчиков.

Вместо того чтобы скачивать библиотеки вручную, ты пишешь одну команду:
```bash
npm install cypress
```

И npm сам всё скачает и установит.

#### npm устанавливается вместе с Node.js

Проверь:
```bash
npm --version
```

**Ожидаемый результат:**
```
10.2.4
```

Если видишь версию — npm готов к работе! ✅

---

### 🎯 Установка Cypress

Теперь всё готово для установки Cypress!

#### Шаг 1: Создать папку для проекта

Создай папку для проекта, например:
```
D:\Projects\cypress-tests
```

#### Шаг 2: Открыть терминал в этой папке

**В Windows:**
1. Открой папку в проводнике
2. В адресной строке напиши `cmd` и нажми Enter
3. Или правой кнопкой → "Открыть в терминале"

**В PowerShell:**
```bash
cd D:\Projects\cypress-tests
```

#### Шаг 3: Инициализировать npm проект

```bash
npm init -y
```

**Что происходит:**
- Создаётся файл `package.json` — конфигурация проекта
- `-y` — автоматически ответить "да" на все вопросы

**Разбор:**
- `npm init` — создать новый проект
- `-y` (yes) — использовать настройки по умолчанию

**Результат:**
В папке появится файл `package.json`:
```json
{
  "name": "cypress-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
```

---

#### Шаг 4: Установить Cypress

```bash
npm install cypress --save-dev
```

**Что происходит:**
- npm скачивает Cypress и все его зависимости
- Устанавливает в папку `node_modules`
- Записывает в `package.json` как зависимость для разработки

**Время установки:** 1-3 минуты (зависит от интернета)

**Разбор команды:**
- `npm install` — установить пакет
- `cypress` — имя пакета
- `--save-dev` — сохранить как dev-зависимость (для разработки)

**Что появится после установки:**

```
cypress-tests/
├── node_modules/        ← Все библиотеки (не трогай!)
├── package.json         ← Конфигурация проекта
└── package-lock.json    ← Точные версии пакетов
```

**⚠️ Важно:**
Папка `node_modules` может весить 200+ МБ. Это нормально!

---

#### Шаг 5: Открыть Cypress

```bash
npx cypress open
```

**Что происходит:**
- Запускается Cypress Test Runner
- При первом запуске создаёт структуру проекта
- Открывается окно Cypress

**Разбор команды:**
- `npx` — запустить пакет из node_modules
- `cypress open` — открыть Cypress в интерактивном режиме

**При первом запуске Cypress предложит:**

1. **Выбрать тип тестирования:**
   - E2E Testing ← Выбери это
   - Component Testing

2. **Cypress создаст файлы:**
   ```
   ✔ cypress/
   ✔ cypress.config.js
   ```

3. **Выбрать браузер:**
   - Chrome ← Рекомендуется
   - Edge
   - Firefox
   - Electron

**Поздравляю! Cypress установлен! 🎉**

---

### 📁 Структура проекта Cypress

После первого запуска проект выглядит так:

```
cypress-tests/
├── node_modules/           ← Библиотеки (НЕ коммитим в Git)
├── cypress/
│   ├── downloads/          ← Скачанные файлы во время тестов
│   ├── e2e/                ← 🎯 ТВОИ ТЕСТЫ (здесь работаешь!)
│   │   └── spec.cy.js      ← Пример теста
│   ├── fixtures/           ← Тестовые данные (JSON файлы)
│   │   └── example.json
│   ├── support/            ← Вспомогательные функции
│   │   ├── commands.js     ← Кастомные команды
│   │   └── e2e.js          ← Настройки перед каждым тестом
├── cypress.config.js       ← Конфигурация Cypress
├── package.json            ← Зависимости проекта
├── package-lock.json       ← Версии пакетов
└── .gitignore              ← Что не добавлять в Git
```

---

### 🗂️ Разбор структуры по полочкам

#### 1️⃣ **cypress/e2e/** — Твои тесты

**Самая важная папка!** Здесь ты пишешь все тесты.

```javascript
// cypress/e2e/login.cy.js
describe('Тесты логина', () => {
  it('Успешный вход', () => {
    cy.visit('https://example.com/login')
    cy.get('#email').type('test@mail.com')
    cy.get('#password').type('12345')
    cy.get('button').click()
  })
})
```

**Правила именования:**
- Файлы должны заканчиваться на `.cy.js`
- Примеры: `login.cy.js`, `registration.cy.js`, `checkout.cy.js`

---

#### 2️⃣ **cypress/fixtures/** — Тестовые данные

Здесь хранятся JSON файлы с тестовыми данными.

**Пример: fixtures/users.json**
```json
{
  "validUser": {
    "email": "test@example.com",
    "password": "SecurePass123"
  },
  "adminUser": {
    "email": "admin@example.com",
    "password": "AdminPass456"
  }
}
```

**Использование в тесте:**
```javascript
cy.fixture('users').then((users) => {
  cy.get('#email').type(users.validUser.email)
  cy.get('#password').type(users.validUser.password)
})
```

**Зачем нужно:**
- ✅ Не дублировать данные в каждом тесте
- ✅ Легко менять тестовые данные в одном месте
- ✅ Хранить большие объёмы данных отдельно от кода

---

#### 3️⃣ **cypress/support/** — Вспомогательные файлы

**cypress/support/commands.js** — Кастомные команды

Здесь создаются переиспользуемые команды.

**Пример:**
```javascript
// cypress/support/commands.js
Cypress.Commands.add('login', (email, password) => {
  cy.visit('/login')
  cy.get('#email').type(email)
  cy.get('#password').type(password)
  cy.get('button[type="submit"]').click()
})
```

**Использование:**
```javascript
// Теперь в любом тесте:
cy.login('test@mail.com', '12345')
// Вместо 4 строк — 1 строка!
```

---

**cypress/support/e2e.js** — Настройки перед тестами

Код, который выполняется перед каждым тестом.

**Пример:**
```javascript
// cypress/support/e2e.js

// Очищать куки перед каждым тестом
beforeEach(() => {
  cy.clearCookies()
  cy.clearLocalStorage()
})

// Игнорировать ошибки в консоли
Cypress.on('uncaught:exception', (err, runnable) => {
  return false
})
```

---

#### 4️⃣ **cypress.config.js** — Конфигурация

Главный файл настроек Cypress.

**Базовая конфигурация:**
```javascript
const { defineConfig } = require('cypress')

module.exports = defineConfig({
  e2e: {
    baseUrl: 'http://localhost:3000',  // Базовый URL
    viewportWidth: 1280,                // Ширина окна
    viewportHeight: 720,                // Высота окна
    defaultCommandTimeout: 10000,       // Таймаут команд (10 сек)
    
    setupNodeEvents(on, config) {
      // Настройки событий
    },
  },
})
```

**Разбор параметров:**

- **baseUrl** — базовый адрес сайта
  ```javascript
  // Вместо:
  cy.visit('http://localhost:3000/login')
  
  // Можно:
  cy.visit('/login')  // Автоматически добавится baseUrl
  ```

- **viewportWidth/Height** — размер окна браузера
  ```javascript
  viewportWidth: 1280,   // Как на обычном мониторе
  viewportHeight: 720
  ```

- **defaultCommandTimeout** — сколько ждать элементы
  ```javascript
  defaultCommandTimeout: 10000  // 10 секунд
  // Если элемент не появился за 10 сек — тест упадёт
  ```

---

#### 5️⃣ **package.json** — Зависимости и скрипты

**Пример:**
```json
{
  "name": "cypress-tests",
  "version": "1.0.0",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
```

**Теперь можно запускать:**
```bash
npm run cy:open   # Открыть Cypress UI
npm run cy:run    # Запустить все тесты в терминале
npm test          # То же, что cy:run
```

**Зачем нужны скрипты:**
- ✅ Короткие команды вместо длинных
- ✅ Все члены команды используют одинаковые команды
- ✅ Можно добавлять параметры

---

#### 6️⃣ **.gitignore** — Что не добавлять в Git

**Обязательно создай файл .gitignore:**

```
# .gitignore
node_modules/
cypress/videos/
cypress/screenshots/
cypress/downloads/
.DS_Store
```

**Зачем:**
- `node_modules/` — весит много, устанавливается через `npm install`
- `videos/screenshots/` — генерируются автоматически при тестах
- Не засоряем репозиторий временными файлами

---

### 🚀 Практика: Запустить первый тест

#### Шаг 1: Создать тестовый файл

Создай файл: `cypress/e2e/first-test.cy.js`

```javascript
describe('Мой первый тест', () => {
  it('Открывает Google', () => {
    cy.visit('https://www.google.com')
    cy.title().should('include', 'Google')
  })
})
```

**Разбор построчно:**

1. `describe('Мой первый тест', () => {` — группа тестов (test suite)
   - "Мой первый тест" — название группы
   - Внутри может быть несколько тестов

2. `it('Открывает Google', () => {` — один тест (test case)
   - "Открывает Google" — что проверяет тест
   - Внутри — сценарий теста

3. `cy.visit('https://www.google.com')` — открыть страницу
   - `cy` — объект Cypress с командами
   - `.visit()` — открыть URL

4. `cy.title().should('include', 'Google')` — проверка
   - `.title()` — получить заголовок страницы
   - `.should('include', 'Google')` — должен содержать "Google"

---

#### Шаг 2: Запустить тест

**Вариант 1: Через Cypress UI**
```bash
npx cypress open
```
1. Выбери E2E Testing
2. Выбери браузер (Chrome)
3. Кликни на `first-test.cy.js`
4. Смотри, как тест выполняется! ✅

**Вариант 2: Через терминал**
```bash
npx cypress run
```
- Тесты запустятся в фоновом режиме
- Результаты покажутся в терминале

---

### ✅ Проверь себя

После этого модуля ты должен:

- [ ] Установить Node.js и проверить версию
- [ ] Установить Cypress через npm
- [ ] Понимать структуру папок проекта
- [ ] Знать, где писать тесты (cypress/e2e/)
- [ ] Создать и запустить свой первый тест
- [ ] Понимать назначение каждой папки

---

### ❌ Типичные ошибки новичков

#### Ошибка 1: Не установлен Node.js
```bash
npm install cypress
# 'npm' is not recognized...
```
**Решение:** Установи Node.js с [nodejs.org](https://nodejs.org/)

---

#### Ошибка 2: Установка не в папке проекта
Запускаешь `npm install cypress` в любой папке → Cypress устанавливается не туда.

**Решение:** 
1. Создай папку проекта
2. Перейди в неё через терминал: `cd D:\Projects\cypress-tests`
3. Потом устанавливай Cypress

---

#### Ошибка 3: Запуск без npx
```bash
cypress open
# 'cypress' is not recognized...
```
**Решение:** Используй `npx`:
```bash
npx cypress open
```

---

#### Ошибка 4: Добавление node_modules в Git
`node_modules/` весит 200+ МБ и не должен быть в Git!

**Решение:** Создай `.gitignore` и добавь `node_modules/`

---

## Модуль 8. Первый тест

### 🎯 Зачем это нужно в реальной работе

Теперь начинается настоящая автоматизация! В этом модуле ты напишешь свой первый **осмысленный** тест.

Ты изучишь базовые команды Cypress:
- Как описывать тесты (`describe`, `it`)
- Как открывать страницы (`cy.visit`)
- Как находить элементы (`cy.get`, `cy.contains`)
- Как проверять результат (`expect`, `should`)

**90% твоих тестов будут использовать именно эти команды.**

---

### 📦 describe — группировка тестов

#### Что это такое:
`describe` — это блок, который группирует связанные тесты.

#### Зачем нужен:
- Организовать тесты по функциям (логин, регистрация, корзина)
- Сделать отчёты читаемыми
- Применить общие настройки для группы тестов

#### Синтаксис:

```javascript
describe('Название группы тестов', () => {
  // Здесь будут тесты
})
```

#### Пример:

```javascript
describe('Тесты логина', () => {
  it('Успешный вход с валидными данными', () => { })
  it('Ошибка при неправильном пароле', () => { })
  it('Ошибка при пустых полях', () => { })
})

describe('Тесты регистрации', () => {
  it('Регистрация нового пользователя', () => { })
  it('Ошибка при существующем email', () => { })
})
```

**Результат в Cypress:**
```
✓ Тесты логина
  ✓ Успешный вход с валидными данными
  ✓ Ошибка при неправильном пароле
  ✓ Ошибка при пустых полях
✓ Тесты регистрации
  ✓ Регистрация нового пользователя
  ✓ Ошибка при существующем email
```

---

#### 📖 Аналогия из жизни:

**describe = Папка с документами**

```
📁 Тесты логина (describe)
  📄 Тест 1: Успешный вход (it)
  📄 Тест 2: Неправильный пароль (it)
  
📁 Тесты регистрации (describe)
  📄 Тест 1: Новый пользователь (it)
  📄 Тест 2: Email занят (it)
```

---

#### Вложенные describe

Можно создавать вложенную структуру:

```javascript
describe('Интернет-магазин', () => {
  
  describe('Аутентификация', () => {
    it('Логин', () => { })
    it('Регистрация', () => { })
  })
  
  describe('Корзина', () => {
    it('Добавление товара', () => { })
    it('Удаление товара', () => { })
  })
})
```

**Отчёт:**
```
✓ Интернет-магазин
  ✓ Аутентификация
    ✓ Логин
    ✓ Регистрация
  ✓ Корзина
    ✓ Добавление товара
    ✓ Удаление товара
```

---

### ✅ it — один тест

#### Что это такое:
`it` — это один тестовый сценарий. Проверяет конкретное поведение.

#### Синтаксис:

```javascript
it('Описание того, что проверяет тест', () => {
  // Шаги теста
})
```

#### Правила именования:

**✅ ХОРОШО: Понятно, что проверяется**
```javascript
it('Успешный вход с валидными данными', () => { })
it('Отображается ошибка при пустом email', () => { })
it('Товар добавляется в корзину', () => { })
```

**❌ ПЛОХО: Непонятно, что проверяется**
```javascript
it('Тест 1', () => { })
it('Проверка логина', () => { })  // Что именно проверяем?
it('Работает', () => { })
```

---

#### Структура теста (AAA паттерн)

Большинство тестов следуют паттерну **AAA** (Arrange-Act-Assert):

1. **Arrange** (Подготовка) — настроить начальное состояние
2. **Act** (Действие) — выполнить действие
3. **Assert** (Проверка) — проверить результат

**Пример:**

```javascript
it('Добавление товара в корзину', () => {
  // 1. Arrange — Подготовка
  cy.visit('/products')
  
  // 2. Act — Действие
  cy.get('[data-testid="add-to-cart"]').click()
  
  // 3. Assert — Проверка
  cy.get('[data-testid="cart-count"]').should('contain', '1')
})
```

---

### 🌐 cy.visit — открыть страницу

#### Что это делает:
Открывает указанный URL в браузере.

#### Синтаксис:

```javascript
cy.visit(url)
```

#### Примеры:

```javascript
// Полный URL
cy.visit('https://example.com')

// Относительный путь (если настроен baseUrl)
cy.visit('/login')

// С параметрами
cy.visit('/products?category=books')

// С опциями
cy.visit('/dashboard', {
  timeout: 30000  // Ждать 30 секунд вместо стандартных
})
```

---

#### Работа с baseUrl

**В cypress.config.js:**
```javascript
module.exports = defineConfig({
  e2e: {
    baseUrl: 'http://localhost:3000'
  }
})
```

**В тестах:**
```javascript
// Вместо:
cy.visit('http://localhost:3000/login')
cy.visit('http://localhost:3000/register')

// Можно писать:
cy.visit('/login')
cy.visit('/register')
```

**Преимущества:**
- ✅ Короче код
- ✅ Легко сменить URL одним изменением в конфиге
- ✅ Легко переключаться между окружениями (dev, staging, prod)

---

#### Пример с разными окружениями:

```javascript
// cypress.config.js
module.exports = defineConfig({
  e2e: {
    baseUrl: process.env.BASE_URL || 'http://localhost:3000'
  }
})
```

**Запуск:**
```bash
# Локально
npm test

# На staging
BASE_URL=https://staging.example.com npm test

# На production
BASE_URL=https://example.com npm test
```

---

### 🔍 cy.get — найти элемент

#### Что это делает:
Ищет элемент на странице по селектору (как querySelector в JavaScript).

#### Синтаксис:

```javascript
cy.get(selector)
```

#### Примеры:

```javascript
// По ID
cy.get('#email')

// По классу
cy.get('.submit-button')

// По атрибуту
cy.get('[data-testid="login-form"]')

// По тегу
cy.get('button')

// Комбинированный селектор
cy.get('input[type="email"]')

// Вложенный поиск
cy.get('.user-card').get('.user-name')
```

---

#### 🎯 Лучшие практики селекторов

**1. data-testid — ЛУЧШИЙ способ** ⭐⭐⭐

```html
<button data-testid="submit-btn">Отправить</button>
```

```javascript
cy.get('[data-testid="submit-btn"]').click()
```

**Почему лучший:**
- ✅ Не зависит от стилей (классов)
- ✅ Не изменится при рефакторинге
- ✅ Явно показывает, что элемент используется в тестах

---

**2. ID — хорошо, если есть** ⭐⭐

```html
<input id="email" />
```

```javascript
cy.get('#email').type('test@mail.com')
```

**Плюсы:** Уникальный, короткий
**Минусы:** Может измениться

---

**3. Классы — плохо** ⭐

```javascript
cy.get('.btn-primary-large').click()  // ❌ Хрупкий
```

**Почему плохо:**
- Классы часто меняются при изменении стилей
- Классы могут дублироваться

---

**4. Тег + атрибут — приемлемо** ⭐⭐

```javascript
cy.get('input[name="email"]')
cy.get('button[type="submit"]')
```

---

**5. Текст — использовать осторожно** ⭐

```javascript
cy.contains('Войти').click()
```

**Проблемы:**
- Может измениться при переводе на другой язык
- Может быть несколько элементов с таким текстом

---

#### Примеры поиска элементов:

```javascript
// Простые селекторы
cy.get('#username')                    // ID
cy.get('.error-message')               // Класс
cy.get('[data-testid="form"]')         // data-testid

// Атрибуты
cy.get('input[type="password"]')       // По типу
cy.get('a[href="/login"]')             // По ссылке
cy.get('button[disabled]')             // Отключенная кнопка

// Комбинации
cy.get('form input[type="email"]')     // Внутри формы
cy.get('.user-card .user-name')        // Вложенность

// Псевдоклассы
cy.get('li:first')                     // Первый элемент
cy.get('li:last')                      // Последний
cy.get('li:nth-child(2)')              // Второй элемент
```

---

### 🔎 cy.contains — найти по тексту

#### Что это делает:
Ищет элемент, который содержит указанный текст.

#### Синтаксис:

```javascript
cy.contains(text)
cy.contains(selector, text)
```

#### Примеры:

```javascript
// Найти любой элемент с текстом "Войти"
cy.contains('Войти')

// Найти кнопку с текстом "Войти"
cy.contains('button', 'Войти')

// Найти элемент, содержащий часть текста
cy.contains('Добро пожаловать')

// С точным совпадением (регистр важен)
cy.contains('Войти').click()
```

---

#### Когда использовать cy.contains:

**✅ ХОРОШО: Когда нет data-testid**

```javascript
// Нет data-testid у кнопки
cy.contains('button', 'Отправить').click()

// Проверка текста в уведомлении
cy.contains('Пользователь создан').should('be.visible')

// Клик по ссылке с текстом
cy.contains('a', 'Регистрация').click()
```

**❌ ПЛОХО: Когда есть data-testid**

```javascript
// ❌ Плохо
cy.contains('Войти').click()

// ✅ Лучше
cy.get('[data-testid="login-btn"]').click()
```

---

#### Разница между cy.get и cy.contains:

```javascript
// cy.get — ищет по селектору
cy.get('#submit-btn')              // Элемент с id="submit-btn"
cy.get('[data-testid="submit"]')   // Элемент с data-testid

// cy.contains — ищет по тексту
cy.contains('Отправить')           // Любой элемент с этим текстом
cy.contains('button', 'Отправить') // Кнопка с этим текстом
```

---

#### Примеры:

```javascript
describe('Поиск элементов', () => {
  it('Тест cy.contains', () => {
    cy.visit('/products')
    
    // Найти карточку товара по названию
    cy.contains('iPhone 15').click()
    
    // Найти кнопку "Купить"
    cy.contains('button', 'Купить').click()
    
    // Проверить текст уведомления
    cy.contains('Товар добавлен в корзину').should('be.visible')
  })
})
```

---

### ✅ expect и should — проверки (assertions)

#### Что это такое:
Проверки (assertions) — это утверждения о том, что должно быть на странице.

Если проверка не прошла → тест падает ❌

#### Два способа проверки в Cypress:

1. **should()** — Cypress-стиль (рекомендуется)
2. **expect()** — Chai-стиль (из JavaScript)

---

### 🎯 should() — Cypress-стиль

#### Синтаксис:

```javascript
cy.get(selector).should('assertion', value)
```

#### Частые проверки:

```javascript
// Проверка видимости
cy.get('[data-testid="message"]').should('be.visible')
cy.get('[data-testid="loader"]').should('not.be.visible')

// Проверка существования
cy.get('.user-card').should('exist')
cy.get('.error').should('not.exist')

// Проверка текста
cy.get('[data-testid="title"]').should('contain', 'Добро пожаловать')
cy.get('[data-testid="title"]').should('have.text', 'Точный текст')

// Проверка значения
cy.get('#email').should('have.value', 'test@mail.com')

// Проверка атрибутов
cy.get('button').should('be.disabled')
cy.get('button').should('not.be.disabled')
cy.get('a').should('have.attr', 'href', '/login')

// Проверка класса
cy.get('button').should('have.class', 'active')

// Проверка количества
cy.get('.product-card').should('have.length', 10)
cy.get('.product-card').should('have.length.greaterThan', 5)
```

---

#### Примеры should():

```javascript
describe('Проверки', () => {
  it('Проверка элементов на странице', () => {
    cy.visit('/dashboard')
    
    // Заголовок виден и содержит текст
    cy.get('[data-testid="page-title"]')
      .should('be.visible')
      .and('contain', 'Панель управления')
    
    // Кнопка отключена
    cy.get('[data-testid="submit-btn"]')
      .should('be.disabled')
    
    // Список содержит 5 элементов
    cy.get('.user-item')
      .should('have.length', 5)
    
    // URL содержит /dashboard
    cy.url().should('include', '/dashboard')
  })
})
```

---

#### Цепочка проверок (.and)

```javascript
// Несколько проверок одного элемента
cy.get('[data-testid="username"]')
  .should('be.visible')
  .and('have.value', 'testuser')
  .and('not.be.disabled')

// Альтернативный синтаксис (то же самое)
cy.get('[data-testid="username"]')
  .should('be.visible')
  .should('have.value', 'testuser')
  .should('not.be.disabled')
```

---

### 🔍 expect() — Chai-стиль

#### Синтаксис:

```javascript
expect(value).to.assertion(expected)
```

#### Когда использовать:

`expect()` используется, когда нужно проверить **значение переменной**, а не DOM элемент.

#### Примеры:

```javascript
// Проверка переменных
const name = 'John'
expect(name).to.equal('John')

// Проверка чисел
const age = 25
expect(age).to.be.greaterThan(18)
expect(age).to.be.lessThan(100)

// Проверка массивов
const users = ['John', 'Jane', 'Bob']
expect(users).to.have.length(3)
expect(users).to.include('John')

// Проверка объектов
const user = { name: 'John', age: 25 }
expect(user).to.have.property('name')
expect(user.name).to.equal('John')
```

---

#### Использование expect() в Cypress:

```javascript
describe('API тесты', () => {
  it('Проверка ответа API', () => {
    cy.request('/api/users').then((response) => {
      // Проверка статуса
      expect(response.status).to.eq(200)
      
      // Проверка тела ответа
      expect(response.body).to.have.length(10)
      expect(response.body[0]).to.have.property('name')
      expect(response.body[0].name).to.equal('John')
    })
  })
})
```

---

#### Работа с .then() и expect:

```javascript
describe('Работа с текстом', () => {
  it('Получить текст элемента', () => {
    cy.get('[data-testid="price"]').then(($el) => {
      const price = $el.text()
      expect(price).to.include('$')
      expect(parseInt(price)).to.be.greaterThan(0)
    })
  })
})
```

---

### 📊 Сравнение should() vs expect()

| should() | expect() |
|----------|----------|
| Для DOM элементов | Для переменных и значений |
| Автоматически ждёт элемент | НЕ ждёт |
| `cy.get('.btn').should('be.visible')` | `expect(value).to.equal(10)` |
| Cypress-стиль | Chai-стиль (обычный JS) |

#### Когда что использовать:

```javascript
// ✅ should() — для элементов DOM
cy.get('[data-testid="title"]').should('contain', 'Hello')

// ✅ expect() — для переменных и API
cy.request('/api/users').then((res) => {
  expect(res.status).to.eq(200)
})
```

---

### 🎓 Практика: Написать тест проверки заголовка страницы

**Задание:**
Напиши тест, который:
1. Открывает страницу https://example.com
2. Проверяет, что заголовок страницы содержит "Example Domain"
3. Проверяет, что на странице есть заголовок H1
4. Проверяет, что H1 содержит текст "Example Domain"

**Твоё решение:**

```javascript
// cypress/e2e/example-test.cy.js

describe('Проверка Example.com', () => {
  it('Проверка заголовка страницы', () => {
    // Твой код здесь
    
  })
})
```

<details>
<summary>Посмотреть правильный ответ</summary>

```javascript
// cypress/e2e/example-test.cy.js

describe('Проверка Example.com', () => {
  it('Проверка заголовка страницы', () => {
    // 1. Открыть страницу
    cy.visit('https://example.com')
    
    // 2. Проверить заголовок страницы (title в <head>)
    cy.title().should('include', 'Example Domain')
    
    // 3. Проверить наличие H1
    cy.get('h1').should('exist')
    
    // 4. Проверить текст в H1
    cy.get('h1').should('contain', 'Example Domain')
    
    // Альтернативный вариант (всё в одной цепочке):
    cy.get('h1')
      .should('be.visible')
      .and('contain', 'Example Domain')
  })
})
```

**Запуск теста:**
```bash
npx cypress open
```
Выбери файл `example-test.cy.js` и смотри, как тест выполняется!

</details>

---

### 🔥 Ещё примеры реальных тестов

#### Пример 1: Тест формы логина

```javascript
describe('Форма логина', () => {
  it('Успешный вход с валидными данными', () => {
    // Открыть страницу логина
    cy.visit('https://example.com/login')
    
    // Заполнить email
    cy.get('[data-testid="email"]').type('test@example.com')
    
    // Заполнить пароль
    cy.get('[data-testid="password"]').type('SecurePass123')
    
    // Нажать кнопку "Войти"
    cy.get('[data-testid="submit-btn"]').click()
    
    // Проверить редирект на dashboard
    cy.url().should('include', '/dashboard')
    
    // Проверить приветственное сообщение
    cy.contains('Добро пожаловать!').should('be.visible')
  })
})
```

---

#### Пример 2: Тест поиска

```javascript
describe('Поиск товаров', () => {
  it('Поиск по ключевому слову', () => {
    // Открыть главную страницу
    cy.visit('/')
    
    // Ввести запрос в поиск
    cy.get('[data-testid="search-input"]').type('iPhone')
    
    // Нажать кнопку поиска
    cy.get('[data-testid="search-btn"]').click()
    
    // Проверить, что результаты загрузились
    cy.get('[data-testid="product-card"]').should('have.length.greaterThan', 0)
    
    // Проверить, что в каждом результате есть "iPhone"
    cy.get('[data-testid="product-title"]').each(($el) => {
      expect($el.text()).to.include('iPhone')
    })
  })
})
```

---

#### Пример 3: Тест с множественными сценариями

```javascript
describe('Корзина', () => {
  beforeEach(() => {
    // Выполняется перед КАЖДЫМ тестом
    cy.visit('/products')
  })
  
  it('Добавление товара в корзину', () => {
    cy.get('[data-testid="add-to-cart"]').first().click()
    cy.get('[data-testid="cart-count"]').should('contain', '1')
  })
  
  it('Удаление товара из корзины', () => {
    // Сначала добавить товар
    cy.get('[data-testid="add-to-cart"]').first().click()
    
    // Открыть корзину
    cy.get('[data-testid="cart-icon"]').click()
    
    // Удалить товар
    cy.get('[data-testid="remove-item"]').click()
    
    // Проверить, что корзина пустая
    cy.contains('Корзина пуста').should('be.visible')
  })
})
```

---

### ❌ Типичные ошибки новичков

#### Ошибка 1: Забыть cy перед командой

```javascript
// ❌ НЕПРАВИЛЬНО
get('[data-testid="btn"]').click()

// ✅ ПРАВИЛЬНО
cy.get('[data-testid="btn"]').click()
```

---

#### Ошибка 2: Использовать неуникальные селекторы

```javascript
// ❌ Плохо: на странице может быть несколько кнопок
cy.get('button').click()

// ✅ Хорошо: уникальный селектор
cy.get('[data-testid="submit-btn"]').click()
```

---

#### Ошибка 3: Не проверять результат действия

```javascript
// ❌ Плохо: кликнули, но не проверили результат
cy.get('[data-testid="submit"]').click()

// ✅ Хорошо: проверяем, что произошло после клика
cy.get('[data-testid="submit"]').click()
cy.contains('Успешно отправлено').should('be.visible')
```

---

#### Ошибка 4: Ждать элемент через setTimeout

```javascript
// ❌ УЖАСНО
cy.visit('/page')
setTimeout(() => {
  cy.get('.element').click()
}, 3000)

// ✅ ПРАВИЛЬНО: Cypress сам ждёт
cy.visit('/page')
cy.get('.element').click()  // Cypress подождёт появления
```

---

### 📝 Шпаргалка по базовым командам

```javascript
// Навигация
cy.visit('/login')                    // Открыть страницу
cy.visit('https://example.com')       // Полный URL
cy.go('back')                         // Назад
cy.go('forward')                      // Вперёд
cy.reload()                           // Перезагрузить

// Поиск элементов
cy.get('[data-testid="btn"]')         // По селектору
cy.contains('Войти')                  // По тексту
cy.contains('button', 'Войти')        // Кнопка с текстом

// Проверки
.should('be.visible')                 // Виден
.should('exist')                      // Существует
.should('contain', 'текст')           // Содержит текст
.should('have.text', 'текст')         // Точный текст
.should('have.value', 'значение')     // Значение input
.should('be.disabled')                // Отключен
.should('have.class', 'active')       // Имеет класс
.should('have.length', 5)             // Количество

// URL
cy.url().should('include', '/dashboard')
cy.url().should('eq', 'https://example.com/login')

// Title
cy.title().should('include', 'Example')
```

---

## 🎯 Итоги Этапа 3

### Что ты изучил:

✅ **Установка:**
- Установил Node.js и npm
- Установил Cypress
- Понял структуру проекта

✅ **Структура проекта:**
- `cypress/e2e/` — тесты
- `cypress/fixtures/` — тестовые данные
- `cypress/support/` — вспомогательные функции
- `cypress.config.js` — конфигурация

✅ **Базовые команды:**
- `describe()` — группировка тестов
- `it()` — один тест
- `cy.visit()` — открыть страницу
- `cy.get()` — найти элемент
- `cy.contains()` — найти по тексту
- `should()` и `expect()` — проверки

✅ **Написал первый тест:**
- Открыл страницу
- Нашёл элементы
- Проверил результат

---

### Чек-лист перед переходом к следующему этапу:

- [ ] Cypress установлен и запускается
- [ ] Понимаешь структуру папок
- [ ] Знаешь, где писать тесты
- [ ] Написал и запустил свой первый тест
- [ ] Понимаешь разницу между `describe` и `it`
- [ ] Умеешь использовать `cy.get()` и `cy.contains()`
- [ ] Умеешь делать проверки через `should()`

---

### Следующий шаг:

**ЭТАП 4: Основные команды**

Пора научиться взаимодействовать с элементами! Кликать, вводить текст, выбирать из списков и многое другое.

🚀 Переходи к Этапу 4!
