# ПЛАН ОБУЧЕНИЯ CYPRESS: НЕДЕЛЯ 1
## Путь от наблюдателя к проверяющему

---

## ДЕНЬ 1. Что значит «проверить руками» и почему это важно понимать

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- E2E-тест — это не магия, а имитация реальных действий пользователя
- Перед тем как писать тест, нужно самому пройти сценарий
- Cypress — инструмент записи твоих действий в виде кода
- Тест — это не проверка того, «работает ли сайт», а проверка конкретного сценария
- Автоматизация начинается с понимания ручных действий

**Что должен УМЕТЬ:**
- Разложить простое действие (например, поиск в Google) на отдельные шаги
- Понимать разницу между «что я делаю» и «что я проверяю»
- Выделить критичные сценарии от второстепенных

---

### 2. Теория (без света) — ~3-4 часа

#### Мысленный эксперимент №1: Опиши другу, как искать в Google
Представь, что твой друг никогда не видел компьютер. Как бы ты объяснил ему процесс поиска?

Вероятно, ты бы сказал:
1. Открой сайт google.com
2. Найди белое поле для ввода текста
3. Напечатай слово «котики»
4. Нажми кнопку «Поиск» или клавишу Enter
5. Убедись, что появились результаты

**Это и есть E2E-сценарий.**

Обрати внимание: ты описал НЕ техническую реализацию («найди элемент с атрибутом name='q'»), а ДЕЙСТВИЯ человека («найди белое поле»).

#### Что такое E2E на самом деле?
E2E (End-to-End) — это проверка всего пути пользователя от начала до конца.

**Чем это НЕ является:**
- Не проверка кода внутри функций
- Не проверка запросов к базе данных
- Не проверка отдельных кнопок «работает/не работает»

**Чем это ЯВЛЯЕТСЯ:**
- Проверка того, что видит и делает ЧЕЛОВЕК
- Проверка последовательности действий
- Проверка результата, который получает пользователь

**Пример из жизни:**
Ты заходишь в интернет-магазин:
1. Ищешь товар
2. Кладёшь его в корзину
3. Переходишь к оформлению
4. Вводишь адрес доставки
5. Подтверждаешь заказ

E2E-тест повторит ВЕСЬ этот путь и проверит, что заказ создался. Если хотя бы один шаг сломан — тест упадёт.

#### Зачем это нужно?

**Реальная история:**
Разработчик изменил цвет кнопки «Купить» в интернет-магазине. Случайно он также изменил её ID. Кнопка выглядит нормально, но перестала работать. Без E2E-теста об этом узнали бы только пользователи.

**Ещё пример:**
Форма регистрации работала. Потом добавили проверку email. Теперь нельзя зарегистрироваться с email без точки перед доменом (например, «user@gmailcom»). Это баг, но его никто не заметил, потому что каждый элемент формы работает ОТДЕЛЬНО. E2E-тест прошёл бы весь сценарий регистрации и поймал бы проблему.

#### Типичная ошибка новичка №1: Тестировать всё подряд
Новички думают: «Нужно тестировать ВСЁ: каждую кнопку, каждое поле, каждую страницу».

Нет. Нужно тестировать **критичные сценарии** — те, без которых сайт бесполезен.

**Критичный сценарий** — это сценарий, который:
1. Используется большинством пользователей
2. Приносит деньги бизнесу
3. Если сломается — пользователи уйдут

Для Google критично: поиск работает.
Для магазина критично: покупка проходит.
Для YouTube критично: видео воспроизводится.

**Некритичные сценарии:**
- Изменение цвета темы
- Экспорт данных в PDF (если этим пользуются 1% пользователей)
- Страница «О компании»

#### Мысленное упражнение №2
Выбери любой сайт, которым ты пользуешься (YouTube, банковское приложение, соцсеть).

Задай себе три вопроса:
1. **«Без какой функции этот сайт станет бесполезным?»** — вот это критичный сценарий.
2. **«Что я делаю на этом сайте КАЖДЫЙ раз?»** — это часто используемый сценарий.
3. **«Если это сломается, я уйду к конкурентам?»** — если да, то это критично.

Запиши 2-3 критичных сценария для выбранного сайта.

**Пример для YouTube:**
- Критично: открыть видео и посмотреть (если это не работает, YouTube мёртв)
- Критично: найти видео через поиск
- Некритично: лайкнуть видео (неприятно, но не критично)

#### Где тут Cypress?
Cypress — это программа, которая умеет:
- Открывать браузер
- Кликать по кнопкам
- Вводить текст в поля
- Проверять, что что-то появилось или исчезло
- Ждать, пока что-то загрузится

Ты пишешь на языке, понятном Cypress, что ему делать. А он это выполняет — как робот, который повторяет твои действия.

**Важно:** сначала ты сам проходишь сценарий вручную. Потом описываешь его Cypress.

#### Типичная ошибка новичка №2: Писать тест, не пройдя сценарий руками
Новичок думает: «Я знаю, как работает сайт, напишу тест сразу».

Результат: тест проверяет не то, что нужно, или падает на неожиданных местах.

**Правильный порядок:**
1. Открой сайт в обычном браузере
2. Пройди сценарий вручную
3. Запиши шаги на бумаге (или в голове)
4. Обрати внимание на детали: куда кликаешь, что вводишь, что появляется
5. Только ПОТОМ пиши тест

#### Мысленное упражнение №3: Разбор сценария
Представь сценарий: «Отправить сообщение в чате».

Какие шаги ты делаешь?
1. Открываешь чат
2. Находишь поле ввода
3. Вводишь текст
4. Нажимаешь «Отправить»
5. Видишь, что сообщение появилось в списке

Теперь подумай: что может пойти не так?
- Чат не откроется
- Поле ввода заблокировано
- Кнопка «Отправить» не работает
- Сообщение не появилось

E2E-тест должен проверить, что ВЕСЬ путь работает, а не отдельные элементы.

#### Чем E2E отличается от других тестов?

**Unit-тесты** проверяют одну функцию:
```
Функция добавить(2, 3) вернула 5? ✓
```

**Интеграционные тесты** проверяют связь между частями:
```
Кнопка отправляет данные в базу? ✓
```

**E2E-тесты** проверяют всё вместе глазами пользователя:
```
Пользователь может войти, создать заказ и оплатить его? ✓
```

#### Мысленное упражнение №4: Найди сценарий в своём опыте
Вспомни последний раз, когда что-то не работало на сайте.

Что именно сломалось?
- Весь сценарий (не смог купить товар от начала до конца)?
- Один шаг (кнопка не нажималась)?
- Результат (деньги списались, но заказ не создался)?

Подумай: как бы E2E-тест это поймал?

---

### 3. Практика (со светом) — ~2-3 часа

**Важно:** Практика идёт ПОСЛЕ теории. Если ты ещё не прочитал теоретическую часть — вернись к ней.

#### Подготовка: Настрой правильное мышление
Перед тем как запускать Cypress, ответь себе на вопросы:
1. Что я собираюсь проверить?
2. Какой результат я ожидаю увидеть?
3. Как я это сделаю руками?

Если ты не можешь ответить — возвращайся к теории.

---

#### Шаг 1: Проверь, что Cypress установлен
Открой терминал в папке проекта и выполни:
```powershell
npx cypress open
```

**Что должно произойти:**
- Откроется окно Cypress с выбором типа тестирования
- Ты увидишь кнопки «E2E Testing» и «Component Testing»

**Если окно открылось — отлично.** Закрой его.

**Если не открылось:**
- Проверь, что ты в правильной папке (там, где файлы cypress.config.js и package.json)
- Выполни `npm install` и попробуй снова

---

#### Шаг 2: Изучи структуру проекта
У тебя уже есть файл `cypress/e2e/google.cy.js`. Открой его в редакторе.

**Что ты видишь:**
```javascript
describe('Название группы тестов', () => {
  it('Название конкретного теста', () => {
    // Здесь код теста
  })
})
```

**Не важно, что там написано пока.** Важно понять структуру:
- `describe` — это группа тестов (как папка для файлов)
- `it` — это один конкретный тест (один сценарий)
- Внутри `it` — команды Cypress

**Важное правило:** один файл .cy.js = один сценарий или группа связанных сценариев.

Например:
- `login.cy.js` — тесты входа на сайт
- `checkout.cy.js` — тесты оформления заказа
- `search.cy.js` — тесты поиска

---

#### Шаг 3: Пройди сценарий вручную (ОБЯЗАТЕЛЬНО)
**Это самый важный шаг.** Не пропускай его.

1. Открой обычный браузер (не Cypress)
2. Зайди на google.com
3. Обрати внимание:
   - Как выглядит поле поиска?
   - Где оно расположено?
   - Есть ли на нём текст-подсказка («Введите запрос» или что-то подобное)?
4. Введи любое слово в поиск (например, «Cypress тестирование»)
5. Нажми Enter или кнопку поиска
6. Посмотри, что изменилось:
   - URL изменился?
   - Появились результаты?
   - Что конкретно появилось на странице?

**Запиши на бумаге (или в текстовом файле):**
```
Мой сценарий поиска в Google:
1. Открыл: https://www.google.com
2. Нашёл: белое поле для ввода текста
3. Ввёл: «Cypress тестирование»
4. Нажал: Enter
5. Увидел: список результатов поиска
```

**Почему это важно:**
Если ты не можешь описать сценарий словами, ты не сможешь написать тест.

---

#### Шаг 4: Запусти существующий тест и наблюдай
В терминале выполни:
```powershell
npx cypress open
```

**Далее:**
1. Выбери **E2E Testing**
2. Выбери браузер (Chrome, Edge или другой)
3. В списке тестов найди файл `google.cy.js`
4. Кликни на него

**Что произойдёт:**
- Откроется браузер, управляемый Cypress
- Cypress начнёт выполнять команды из теста
- Ты увидишь, как страница открывается, текст вводится, кнопки нажимаются

**Твоя задача:** просто смотреть. Не трогай код. Наблюдай за тем, ЧТО делает Cypress.

**Обрати внимание:**
- Как быстро выполняются команды?
- Появляется ли зелёная галочка рядом с каждым шагом?
- Видно ли в левой панели, какая команда сейчас выполняется?

---

#### Шаг 5: Сравни свои действия с тестом
Открой файл `cypress/e2e/google.cy.js` в редакторе.

**Найди в коде:**
1. **Открытие сайта:** где в коде написано `cy.visit(...)`?
2. **Поиск элемента:** где написано `cy.get(...)`?
3. **Ввод текста:** где написано `.type(...)`?
4. **Проверка результата:** где написано `.should(...)`?

**Сравни с тем, что ты делал руками:**
- Ты открывал сайт → Cypress использует `cy.visit()`
- Ты искал поле ввода → Cypress использует `cy.get()`
- Ты вводил текст → Cypress использует `.type()`
- Ты проверял результат → Cypress использует `.should()`

**Важно:** пока не важно, КАК работают эти команды. Важно увидеть СООТВЕТСТВИЕ между твоими действиями и командами.

---

#### Шаг 6: Запиши наблюдения
Создай файл `Мои_заметки_День1.txt` и запиши:

```
Что я понял сегодня:
1. E2E-тест — это...
2. Cypress делает то же, что я делаю руками, но...
3. Перед написанием теста нужно...

Что мне пока непонятно:
1. ...
2. ...

Вопросы к себе на завтра:
1. Как Cypress находит элементы на странице?
2. Почему важно использовать правильные селекторы?
```

**Почему это важно:**
Запись помогает структурировать мысли и не забыть важное.

---

### 4. Критерии «день завершён»

✅ **Ты понимаешь на уровне объяснения:**
- Что такое E2E-тест (можешь объяснить своими словами, без терминов)
- Зачем нужны автоматические тесты (не «чтобы проверить сайт», а конкретнее)
- Чем критичный сценарий отличается от второстепенного

✅ **Ты сделал практически:**
- Прошёл простой сценарий вручную (например, поиск в Google)
- Записал шаги этого сценария на бумаге или в файле
- Запустил Cypress и увидел, как он выполняет тест
- Нашёл в коде соответствия твоим ручным действиям

✅ **Ты осознаёшь:**
- Что файл .cy.js — это описание действий, а не магия
- Что Cypress повторяет то, что ты делаешь руками
- Что перед написанием теста нужно понимать сценарий

✅ **Ты НЕ чувствуешь:**
- Что E2E-тесты — это сложно и непонятно
- Что нужно знать программирование, чтобы писать тесты
- Что Cypress — это что-то отдельное от реальных действий пользователя

**Если хотя бы один пункт НЕ выполнен — перечитай теорию и повтори практику.**

**Если всё выполнено — день завершён.** Отдохни. Завтра ты узнаешь, как Cypress НАХОДИТ элементы на странице.

---

### 5. Куда это ведёт дальше

Сегодня ты понял, ЧТО делает Cypress — он повторяет действия пользователя.

Завтра ты узнаешь, КАК он это делает. Конкретно: как Cypress понимает, **куда кликать** и **что вводить**.

Представь: ты говоришь другу «Нажми на кнопку «Войти»». Он знает, что искать.
Но как ты скажешь это Cypress? Он не видит текст «Войти». Он видит HTML-код.

**Завтра ты научишься говорить с Cypress на его языке — языке селекторов.**

Это основа всего. Если ты не умеешь находить элементы — ты не напишешь ни одного теста.

---

### 📝 Домашнее задание (необязательно, но полезно)

Если у тебя есть свободное время БЕЗ света:

**Задание 1: Найди критичный сценарий**
Выбери сайт, которым ты пользуешься часто. Запиши 3 критичных сценария для него.

Пример для интернет-магазина:
1. Найти товар → добавить в корзину → оформить заказ
2. Войти в аккаунт → посмотреть историю заказов
3. Применить промокод при оформлении заказа

**Задание 2: Разложи сценарий на шаги**
Возьми один из сценариев и разложи на шаги КАК МОЖНО ПОДРОБНЕЕ.

Плохо:
```
1. Открыть сайт
2. Найти товар
3. Купить
```

Хорошо:
```
1. Открыть главную страницу
2. Найти поле поиска (обычно вверху справа)
3. Ввести название товара
4. Нажать Enter или кнопку поиска
5. Дождаться загрузки результатов
6. Кликнуть на первый результат
7. Дождаться открытия страницы товара
8. Найти кнопку «В корзину»
9. Кликнуть на неё
10. Убедиться, что товар добавлен (появился счётчик у иконки корзины)
```

**Задание 3: Подумай об ошибках**
Для сценария из Задания 2 подумай: что может пойти не так на каждом шаге?

Пример:
- Шаг 2: Поле поиска не найдено (дизайн изменился)
- Шаг 5: Результаты не загрузились (сервер недоступен)
- Шаг 10: Счётчик не обновился (JavaScript-ошибка)

**Зачем это нужно:**
Когда ты пишешь тесты, ты думаешь не только о «счастливом пути» (всё работает), но и о том, что может сломаться.

---



## ДЕНЬ 2. Как Cypress видит страницу (селекторы — твои координаты)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Cypress не видит кнопку как ты. Он видит HTML-элементы с атрибутами
- Селектор — это адрес элемента на странице, как почтовый адрес дома
- Есть хорошие селекторы (стабильные, надёжные) и плохие (ломкие, зависящие от случайностей)
- Правильный селектор — это 80% успеха в написании тестов
- Тест падает не потому, что сайт сломан, а потому что селектор неправильный — это самая частая причина

**Что должен УМЕТЬ:**
- Найти элемент на странице через инструменты разработчика браузера
- Понять, почему `cy.get('button')` может найти не ту кнопку
- Использовать data-атрибуты как надёжный способ поиска
- Отличить хороший селектор от плохого

---

### 2. Теория (без света) — ~3-4 часа

#### Мысленный эксперимент №1: Объясни роботу, где кнопка
Представь: ты в большой комнате с 100 одинаковыми столами. Как ты скажешь роботу, к какому столу подойти?

**Вариант 1: «Третий слева»**
Проблема: если добавят стол в начале, робот подойдёт к неправильному столу.

**Вариант 2: «Стол с биркой №47»**
Надёжно: бирка не меняется, даже если добавят или уберут другие столы.

**Вариант 3: «Стол у окна»**
Проблема: если переставят мебель, робот не найдёт стол.

**Вариант 4: «Стол с красной скатертью»**
Проблема: если поменяют скатерть, робот не найдёт стол.

**Селектор — это способ сказать Cypress, КАКОЙ именно элемент найти.**

#### Как браузер хранит страницу?
Браузер видит страницу не как картинку, а как дерево элементов (HTML).

Пример:
```html
<body>
  <header>
    <button>Войти</button>
  </header>
  <main>
    <button>Купить</button>
    <button>В корзину</button>
  </main>
</body>
```

Если ты скажешь Cypress: «Найди `<button>`», — он найдёт **три** кнопки. Какую кликнуть?

**Вот в чём проблема:** нужно указать **конкретную** кнопку.

#### Способы указать на элемент

**1. По тегу (плохо, очень плохо):**
```javascript
cy.get('button')
```
Найдёт ВСЕ кнопки на странице. Непонятно, какую нужно.

**Когда это работает:** если кнопка единственная на странице (редкий случай).
**Когда это ломается:** всегда, когда добавят ещё одну кнопку.

**2. По тексту (средне):**
```javascript
cy.contains('Купить')
```
Найдёт элемент с текстом «Купить».

**Когда это работает:** если текст уникальный и не меняется.
**Когда это ломается:**
- Дизайнер поменял текст на «Добавить в корзину»
- Текст переводится на разные языки
- На странице два элемента с текстом «Купить»

**3. По классу (средне, зависит от контекста):**
```javascript
cy.get('.buy-button')
```
Найдёт элемент с классом `buy-button`.

**Когда это работает:** если класс не меняется.
**Когда это ломается:**
- Дизайнер переименовал класс в `.purchase-btn`
- Класс используется для стилей и его часто меняют

**4. По ID (хорошо, но...):**
```javascript
cy.get('#main-buy-button')
```
Найдёт элемент с id `main-buy-button`.

**Когда это работает:** если ID не меняется.
**Когда это ломается:**
- ID используется для JavaScript-кода и его могут переименовать
- Иногда ID генерируются автоматически: `#button-1234` — при перезагрузке станет `#button-5678`

**5. По data-атрибуту (отлично, почти всегда лучший выбор):**
```javascript
cy.get('[data-test="buy-button"]')
```
Найдёт элемент с атрибутом `data-test="buy-button"`.

**Почему это лучше всего:**
- `data-test` создаётся специально для тестов
- Его не меняют просто так
- Даже если дизайн изменится, атрибут останется
- Это явный сигнал для разработчиков: «Этот элемент используется в тестах, не трогай атрибут»

**Золотое правило:** используй `data-test` (или `data-cy`, `data-testid`) везде, где можешь.

#### Типичная ошибка новичка №1: Копирование длинных селекторов
Новичок открывает инструменты разработчика, кликает правой кнопкой на элемент, выбирает «Copy → Copy selector» и получает:
```javascript
cy.get('#root > div > div.container > div:nth-child(3) > button')
```

**Проблема:** это селектор зависит от структуры страницы.
Переставили блок — селектор сломался.
Добавили ещё один div — селектор сломался.

Это как адрес: «дом 5, подъезд 2, этаж 3, квартира 7, комната 2, стол у окна».
Переставили мебель — адрес сломался.

**Лучше:** «дом с табличкой 'Иванов'». Короче и надёжнее.

#### Мысленное упражнение №2: Представь переименование
Представь сайт с кнопкой «Войти».

Какой селектор сломается, если:
1. Дизайнер поменял текст на «Sign In»?
   - Сломается: `cy.contains('Войти')`
   - Не сломается: `cy.get('[data-test="login-button"]')`

2. Дизайнер поменял класс кнопки с `.login-btn` на `.auth-button`?
   - Сломается: `cy.get('.login-btn')`
   - Не сломается: `cy.get('[data-test="login-button"]')`

3. Разработчик переместил кнопку из header в sidebar?
   - Сломается: `cy.get('header button')`
   - Не сломается: `cy.get('[data-test="login-button"]')`

**Вывод:** data-атрибуты — самые стабильные.

#### Реальная история
На проекте был тест, который использовал селектор:
```javascript
cy.get('.btn-primary')
```

Дизайнер решил, что основная кнопка должна быть не синей (primary), а зелёной. Он изменил класс на `.btn-success`.

Результат: 20 тестов упали. Пришлось искать и менять селектор в 20 файлах.

Если бы использовали `data-test`, ничего бы не сломалось.

#### Типичная ошибка новичка №2: Использование position-селекторов
Новичок пишет:
```javascript
cy.get('button').eq(2) // третья кнопка
```

**Проблема:** порядок кнопок может измениться.

**Пример:**
- Сегодня кнопки: «Назад», «Далее», «Отмена»
- Завтра добавили «Справка» в начало: «Справка», «Назад», «Далее», «Отмена»
- Теперь `.eq(2)` — это «Далее», а не «Отмена»

**Решение:** используй явный селектор.

#### Мысленное упражнение №3: Найди элемент
Открой мысленно любой сайт. Представь кнопку «Войти».

Как бы ты объяснил Cypress, где она?
- «Кнопка в правом верхнем углу» — робот не понимает «угол»
- «Первая кнопка на странице» — может измениться
- «Кнопка с текстом 'Войти'» — лучше, но текст может измениться
- «Кнопка с атрибутом data-test='login'» — идеально

#### Структура HTML и атрибуты
Элемент на странице может иметь разные атрибуты:

```html
<button 
  id="submit-btn" 
  class="btn btn-primary" 
  data-test="checkout-button"
  type="submit"
>
  Оформить заказ
</button>
```

**Что здесь есть:**
- `id` — уникальный идентификатор
- `class` — классы для стилей (может быть несколько)
- `data-test` — атрибут для тестов
- `type` — тип кнопки
- Текст внутри: «Оформить заказ»

**Как найти этот элемент:**
- По id: `cy.get('#submit-btn')`
- По классу: `cy.get('.btn-primary')`
- По data-test: `cy.get('[data-test="checkout-button"]')` ✓ лучший вариант
- По тексту: `cy.contains('Оформить заказ')`

#### Когда использовать какой селектор?

**Приоритет:**
1. **`data-test` / `data-cy`** — всегда первый выбор
2. **`id`** — если нет data-атрибута и ID стабильный
3. **`class`** — если класс явно для функциональности, а не для стилей
4. **`contains(текст)`** — если текст уникальный и не будет переводиться
5. **тег + атрибут** — например, `input[name="email"]`
6. **НЕ используй:** длинные цепочки, position-селекторы, auto-generated ID

#### Мысленное упражнение №4: Оцени селекторы
Какой селектор лучше для кнопки «Купить» в интернет-магазине?

A) `cy.get('button').eq(3)`
B) `cy.contains('Купить')`
C) `cy.get('[data-test="add-to-cart-button"]')`
D) `cy.get('#product-1234-buy-button')`

**Ответ:** C — самый надёжный.
- A — зависит от порядка
- B — текст может измениться
- D — ID может быть динамическим (1234 — это ID товара, может меняться)

---

### 3. Практика (со светом) — ~1.5 часа

#### Шаг 1: Открой инструменты разработчика
1. Открой google.com в браузере
2. Нажми F12 (или правой кнопкой → «Просмотреть код»)
3. Нажми кнопку «Выбрать элемент» (значок стрелки вверху)
4. Наведи на поле поиска Google
5. Посмотри в коде: это `<input>` или `<textarea>`?

#### Шаг 2: Изучи, как найти элемент
В инструментах разработчика найди поле поиска. Посмотри на его атрибуты:
- есть ли `name="q"`?
- есть ли `title="Поиск"`?

Попробуй в консоли браузера (вкладка Console):
```javascript
document.querySelector('[name="q"]')
```

Если элемент подсветился — селектор работает.

#### Шаг 3: Напиши простой тест с правильным селектором
Открой файл [cypress/e2e/google.cy.js](cypress/e2e/google.cy.js).

Попробуй изменить селектор поля поиска на более надёжный:
```javascript
cy.get('[name="q"]').type('Cypress testing')
```

Запусти тест:
```
npx cypress open
```

Если тест прошёл — отлично. Если нет — перепроверь селектор в инструментах разработчика.

#### Шаг 4: Эксперимент с плохим селектором
Попробуй намеренно указать плохой селектор:
```javascript
cy.get('input').type('Cypress testing')
```

Запусти тест. Cypress может найти ДРУГОЕ поле ввода (если оно есть на странице).

Это показывает, почему важно быть точным.

---

### 4. Критерии «день завершён»

✅ Ты открывал инструменты разработчика и находил элементы на странице
✅ Ты понимаешь, что селектор — это способ указать Cypress на конкретный элемент
✅ Ты видишь разницу между `cy.get('button')` и `cy.get('[data-test="login"]')`
✅ Ты изменил селектор в тесте и тест продолжил работать

---

### 5. Куда это ведёт дальше

Завтра ты узнаешь, что делать ПОСЛЕ того, как нашёл элемент: как кликать, вводить текст, проверять результат.

И поймёшь главное: Cypress выполняет команды не мгновенно — он ждёт, пока элемент появится. Это важно для стабильности.

---

## ДЕНЬ 3. Действия и ожидание (почему тесты не падают, когда страница грузится)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Cypress автоматически ждёт появления элементов (это его суперсила — retry-ability)
- Команды выполняются последовательно, как шаги инструкции для робота
- Не нужно писать «подожди 5 секунд» — Cypress умнее и знает, когда ждать
- Автоматическое ожидание — это ключевое отличие Cypress от других инструментов
- Тест не падает на медленных сайтах, потому что Cypress **ждёт**, а не требует мгновенной реакции
- Есть разница между ожиданием элемента и ожиданием времени

**Что должен УМЕТЬ:**
- Написать цепочку действий: открыть → ввести → кликнуть → проверить
- Понять, почему тест не падает, даже если страница грузится медленно
- Различать, когда Cypress ждёт автоматически, а когда нужно явное ожидание
- Читать логи Cypress и видеть, сколько времени заняла каждая команда
- Использовать базовые действия: type(), click(), clear()

---

### 2. Теория (без света) — ~3-4 часа

#### Мысленный эксперимент №1: Инструкция для человека
Представь, что ты пишешь инструкцию для помощника:
1. Открой дверь
2. Войди в комнату
3. Включи свет
4. Сядь на стул

**Человек не включит свет, пока не войдёт в комнату.** Он понимает последовательность и ждёт завершения каждого шага.

**Cypress работает так же.**

Ты пишешь:
```javascript
cy.visit('https://example.com')
cy.get('[name="email"]').type('user@test.com')
cy.get('[data-test="submit"]').click()
cy.contains('Успешно').should('be.visible')
```

Cypress выполняет команды **по порядку**. Он НЕ перейдёт к следующей команде, пока не завершит предыдущую.

#### Что значит «последовательное выполнение»?

**Плохой пример (не Cypress, а обычный код):**
```javascript
открой_сайт()
найди_поле()  // Сайт ещё не загрузился!
введи_текст() // Поле ещё не найдено!
```
Всё сломается, потому что каждая команда выполняется мгновенно, не дожидаясь готовности.

**Хороший пример (Cypress):**
```javascript
cy.visit('сайт')       // Выполнилось → переход к следующей команде
cy.get('поле')         // Ждёт появления поля → нашёл → переход дальше
cy.get('поле').type('текст') // Ждёт, пока поле станет доступным → ввёл → дальше
```

**Ключевой момент:** Cypress НЕ выполняет команды параллельно. Он идёт строго по порядку.

#### Что такое автоматическое ожидание (retry-ability)?

**История из жизни:**
Ты заказал пиццу. Звонишь в дверь.
- **Вариант 1 (без ожидания):** Позвонил раз → никто не открыл → ушёл. Пицца так и не была получена.
- **Вариант 2 (с ожиданием):** Позвонил → подождал 10 секунд → позвонил снова → подождал → дверь открылась → получил пиццу.

**Cypress работает как второй вариант.**

Когда ты пишешь `cy.get('[data-test="button"]')`, Cypress:
1. Ищет элемент → не нашёл (страница ещё грузится)
2. Ждёт 50 миллисекунд
3. Ищет снова → не нашёл (загрузка продолжается)
4. Ждёт ещё 50 миллисекунд
5. Ищет снова → **НАШЁЛ!** → переходит к следующей команде

**По умолчанию Cypress ждёт до 4 секунд** (4000 миллисекунд). Если за это время элемент не появился — тест упадёт с ошибкой.

#### Почему Cypress не падает на медленных сайтах?

**Обычная программа:**
```
1. Открыла страницу
2. Сразу ищет кнопку → НЕ НАШЛА (страница ещё грузится)
3. ОШИБКА: "Элемент не найден"
```

**Cypress:**
```
1. Открыл страницу
2. Ищет кнопку → не видит → ждёт
3. Ищет снова (через 50 мс) → не видит → ждёт
4. Ищет снова → не видит → ждёт
5. Ищет снова → НАШЁЛ → кликает
```

**Вывод:** Cypress **терпеливый**. Он понимает, что сайты загружаются не мгновенно.

#### Когда тест падает, а когда нет?

**Тест упадёт, если:**
1. **Элемента нет на странице вообще**
   - Неправильный селектор
   - Страница не та
   - Элемент удалён разработчиками

2. **Элемент скрыт (display: none, visibility: hidden)**
   - Cypress видит элемент в HTML, но он невидим для пользователя
   - Пользователь не может с ним взаимодействовать → тест не должен тоже

3. **Элемент заблокирован (disabled)**
   - Кнопка есть, но она неактивна
   - Поле есть, но в него нельзя ввести текст

4. **Элемент появляется дольше 4 секунд**
   - Очень медленная загрузка
   - Долгий запрос к серверу

**Тест НЕ упадёт, если:**
1. **Страница грузится 2 секунды** — Cypress подождёт
2. **Кнопка появляется через 1 секунду после загрузки** — Cypress подождёт
3. **Элемент временно скрыт, но потом появляется** — Cypress будет проверять каждые 50 мс

#### Типичная ошибка новичка №1: Использование cy.wait() без причины

Новичок видит, что тест «иногда падает», и пишет:
```javascript
cy.get('button').click()
cy.wait(5000) // Жди 5 секунд
cy.get('.результат').should('be.visible')
```

**Проблемы:**
- Если результат появляется за 1 секунду, тест ждёт ещё 4 секунды зря (медленные тесты)
- Если результат появляется через 6 секунд, тест всё равно упадёт
- Непонятно, ЗАЧЕМ ждать 5 секунд (код неясен)

**Правильно:**
```javascript
cy.get('button').click()
cy.get('.результат').should('be.visible') // Cypress сам подождёт
```

Cypress **автоматически** будет ждать появления `.результат` до 4 секунд. Если появится раньше — пойдёт дальше сразу.

**Когда `cy.wait()` уместен:**
- Ждать завершения API-запроса (об этом на следующей неделе)
- Ждать анимации, которая не меняет DOM
- Ждать внешнего сервиса (например, интеграция с платёжной системой)

Но 99% случаев НЕ требуют `cy.wait(число)`.

#### Мысленное упражнение №2: Найди проблему

**Сценарий:**
Пользователь кликает на кнопку «Показать результат». После клика появляется блок с результатами (через 2 секунды, потому что идёт запрос к серверу).

**Вариант теста 1:**
```javascript
cy.get('[data-test="show-result"]').click()
cy.wait(3000)
cy.get('.result-block').should('be.visible')
```

**Вариант теста 2:**
```javascript
cy.get('[data-test="show-result"]').click()
cy.get('.result-block').should('be.visible')
```

**Вопрос:** Какой вариант лучше и почему?

**Ответ:** Вариант 2.
- Если результат появится за 1 секунду, вариант 1 будет ждать ещё 2 секунды зря
- Если результат появится за 4 секунды, вариант 1 упадёт (ждёт только 3 секунды)
- Вариант 2 подождёт ровно столько, сколько нужно (до 4 секунд)

#### Реальная история: Почему тесты были медленными

На проекте все тесты использовали `cy.wait(3000)` после каждого клика. 
Тесты работали, но очень медленно: 100 тестов выполнялись 30 минут.

**Проблема:** 100 тестов × 10 кликов × 3 секунды = 3000 секунд = 50 минут ЛИШНЕГО ожидания.

После удаления всех `cy.wait(3000)` и замены их на автоматическое ожидание, тесты стали выполняться за 8 минут.

**Вывод:** Доверяй Cypress. Он знает, когда ждать.

#### Цепочка команд: как это работает

Cypress команды можно писать цепочкой:
```javascript
cy.get('[name="email"]')
  .clear()           // Очистить поле
  .type('test@test.com') // Ввести текст
  .should('have.value', 'test@test.com') // Проверить значение
```

**Что происходит:**
1. `cy.get()` находит элемент (ждёт до 4 секунд)
2. `.clear()` очищает найденный элемент
3. `.type()` вводит текст в этот же элемент
4. `.should()` проверяет, что значение правильное

**Важно:** Каждый следующий метод в цепочке работает с результатом предыдущего.

#### Типичная ошибка новичка №2: Неправильное понимание цепочки

**Неправильно:**
```javascript
cy.get('input')
cy.type('текст') // ОШИБКА! type() не знает, в КАКОЙ input вводить
```

**Правильно:**
```javascript
cy.get('input').type('текст')
// или
cy.get('input')
  .type('текст')
```

#### Мысленное упражнение №3: Последовательность действий

Представь тест для входа на сайт:
1. Открой страницу входа
2. Найди поле email
3. Введи email
4. Найди поле пароль
5. Введи пароль
6. Кликни кнопку «Войти»
7. Проверь, что появилась страница профиля

**Где Cypress будет ждать?**
- На шаге 2: ждёт появления поля email
- На шаге 4: ждёт появления поля пароль
- На шаге 6: ждёт, пока кнопка станет кликабельной
- На шаге 7: ждёт появления страницы профиля

**Сколько `cy.wait()` нужно написать?** Ноль! Cypress подождёт автоматически на каждом шаге.

#### Разница между действиями и проверками

**Действия** (commands):
- `cy.visit()` — открыть страницу
- `cy.get().type()` — ввести текст
- `cy.get().click()` — кликнуть
- `cy.get().clear()` — очистить поле
- `cy.get().select()` — выбрать из выпадающего списка

**Проверки** (assertions):
- `.should('be.visible')` — элемент виден
- `.should('have.text', '...')` — элемент содержит текст
- `.should('have.value', '...')` — поле имеет значение

**Ключевая разница:**
- Действия **изменяют** состояние страницы
- Проверки **контролируют** состояние страницы

**Обе группы автоматически ждут!**

#### Мысленное упражнение №4: Найди момент ожидания

Код:
```javascript
cy.visit('https://example.com')
cy.get('[data-test="username"]').type('admin')
cy.get('[data-test="password"]').type('12345')
cy.get('[data-test="login-button"]').click()
cy.url().should('include', '/dashboard')
```

**Где Cypress ждёт?**
1. После `visit()` — ждёт загрузки страницы
2. При первом `cy.get('[data-test="username"]')` — ждёт появления поля
3. При втором `cy.get('[data-test="password"]')` — ждёт появления поля
4. При `cy.get('[data-test="login-button"]')` — ждёт появления кнопки
5. При `.click()` — ждёт, пока кнопка станет кликабельной
6. При `should('include', '/dashboard')` — ждёт изменения URL

**Итого:** 6 точек автоматического ожидания. Никаких `cy.wait()` не нужно!

---

### 3. Практика (со светом) — ~2-3 часа

**Цель:** Написать тест с несколькими последовательными действиями и увидеть автоматическое ожидание в действии.

---

#### Шаг 1: Пройди сценарий руками (обязательно!)
Не пропускай этот шаг. Никогда.

**Сценарий:** Поиск в Google с несколькими действиями.

1. Открой google.com
2. Введи запрос «Cypress testing»
3. Нажми Enter
4. Дождись результатов
5. Кликни на первую ссылку (не обязательно, но для практики)
6. Убедись, что страница открылась

**Запиши время:** сколько секунд заняла загрузка результатов? 1-2 секунды? Больше?

---

#### Шаг 2: Напиши тест с цепочкой действий
Открой файл `cypress/e2e/google.cy.js`.

Напиши или обнови тест:
```javascript
describe('Поиск в Google с действиями', () => {
  it('выполняет поиск и проверяет результаты', () => {
    // Шаг 1: Открыть Google
    cy.visit('https://www.google.com')
    
    // Шаг 2: Найти поле поиска и ввести текст
    cy.get('[name="q"]')
      .type('Cypress testing')
    
    // Шаг 3: Нажать Enter
    cy.get('[name="q"]').type('{enter}')
    
    // Шаг 4: Проверить, что результаты появились
    cy.get('#search').should('be.visible')
    
    // Шаг 5: Проверить, что есть результаты с текстом
    cy.contains('Cypress').should('be.visible')
  })
})
```

**Обрати внимание:**
- Нет ни одного `cy.wait(число)`
- Каждая команда выполняется последовательно
- Cypress автоматически ждёт на каждом шаге

---

#### Шаг 3: Запусти тест и наблюдай за ожиданием
```powershell
npx cypress open
```

Выбери E2E Testing → браузер → google.cy.js

**Что смотреть:**
1. **Левая панель** — список команд с временем выполнения
2. **Зелёные галочки** — команда выполнена успешно
3. **Время выполнения** — сколько заняла каждая команда

**Наведи курсор на любую команду** в левой панели. Cypress покажет:
- Скриншот момента выполнения
- Сколько времени команда выполнялась
- Сколько раз Cypress пытался найти элемент (retry attempts)

**Обрати внимание:**
Если страница грузилась медленно, ты увидишь, что некоторые команды выполнялись дольше (например, 1500 мс вместо 50 мс). Это Cypress ждал!

---

#### Шаг 4: Эксперимент — замедли сеть
**Цель:** Понять, что Cypress справляется с медленными сайтами.

**Действия:**
1. В Cypress Test Runner открой DevTools (F12)
2. Перейди на вкладку **Network**
3. Выбери **Slow 3G** или **Fast 3G** в выпадающем списке throttling
4. Перезапусти тест

**Что происходит:**
- Страница грузится медленнее
- Cypress продолжает ждать
- Тест НЕ падает (если укладывается в 4 секунды)

**Вывод:** Автоматическое ожидание спасает от проблем с медленной сетью.

Верни сеть обратно в **No throttling**.

---

#### Шаг 5: Добавь действие с очисткой поля
Напиши тест, который:
1. Вводит текст в поле
2. Очищает поле
3. Вводит другой текст

```javascript
it('очищает и вводит новый текст', () => {
  cy.visit('https://www.google.com')
  
  // Ввести текст
  cy.get('[name="q"]').type('первый запрос')
  
  // Проверить, что текст введён
  cy.get('[name="q"]').should('have.value', 'первый запрос')
  
  // Очистить поле
  cy.get('[name="q"]').clear()
  
  // Проверить, что поле пустое
  cy.get('[name="q"]').should('have.value', '')
  
  // Ввести новый текст
  cy.get('[name="q"]').type('второй запрос')
  
  // Проверить новый текст
  cy.get('[name="q"]').should('have.value', 'второй запрос')
})
```

**Запусти тест.** Наблюдай, как Cypress выполняет каждое действие последовательно.

---

#### Шаг 6: Эксперимент с неправильным ожиданием
**Цель:** Понять, почему `cy.wait(число)` — плохая идея.

Добавь намеренно плохой код:
```javascript
it('плохой пример с wait', () => {
  cy.visit('https://www.google.com')
  cy.wait(3000) // Ждать 3 секунды без причины
  cy.get('[name="q"]').type('Cypress')
  cy.wait(2000) // Ждать ещё 2 секунды
  cy.get('[name="q"]').type('{enter}')
  cy.wait(3000) // Ждать результатов
  cy.get('#search').should('be.visible')
})
```

**Запусти тест.** Засеки время выполнения.

**Теперь удали все `cy.wait()`:**
```javascript
it('хороший пример без wait', () => {
  cy.visit('https://www.google.com')
  cy.get('[name="q"]').type('Cypress{enter}')
  cy.get('#search').should('be.visible')
})
```

**Запусти тест снова.** Засеки время.

**Сравни:**
- Плохой тест: ~10-12 секунд (3+2+3 = 8 секунд лишнего ожидания)
- Хороший тест: ~2-3 секунды

**Вывод:** Не используй `cy.wait(число)` без веской причины!

---

#### Шаг 7: Напиши тест с несколькими проверками
**Задача:** Проверить несколько вещей после действия.

```javascript
it('проверяет несколько элементов после поиска', () => {
  cy.visit('https://www.google.com')
  cy.get('[name="q"]').type('Cypress E2E{enter}')
  
  // Проверка 1: Блок результатов виден
  cy.get('#search').should('be.visible')
  
  // Проверка 2: Есть хотя бы один результат с текстом Cypress
  cy.contains('Cypress').should('exist')
  
  // Проверка 3: URL изменился (содержит search)
  cy.url().should('include', 'search')
  
  // Проверка 4: Поле поиска всё ещё содержит запрос
  cy.get('[name="q"]').should('have.value', 'Cypress E2E')
})
```

**Запусти тест.** Все проверки должны пройти.

**Обрати внимание:**
Каждая проверка (`should`) тоже автоматически ждёт! Если элемент не виден сразу, Cypress подождёт до 4 секунд.

---

#### Шаг 8: Запиши наблюдения
Создай файл `Мои_заметки_День3.txt`:

```
Что я понял про действия и ожидание:
1. Cypress автоматически ждёт...
2. Команды выполняются...
3. cy.wait(число) нужен только когда...

Эксперименты, которые я провёл:
- Замедлил сеть → тест...
- Убрал cy.wait() → тест стал быстрее на...

Что мне непонятно:
1. ...
2. ...
```

---

### 4. Критерии «день завершён»

✅ **Ты понимаешь на уровне объяснения:**
- Что такое автоматическое ожидание (retry-ability) — можешь объяснить на примере
- Почему команды выполняются последовательно, а не параллельно
- В чём разница между автоматическим ожиданием и `cy.wait(число)`
- Когда тест упадёт, а когда Cypress подождёт

✅ **Ты сделал практически:**
- Написал тест из 4-5 команд (visit, get, type, click, should)
- Запустил тест и увидел время выполнения каждой команды
- Провёл эксперимент с замедлением сети
- Сравнил тест с `cy.wait()` и без него
- Увидел, как Cypress ждёт элемент, который появляется не сразу

✅ **Ты умеешь:**
- Написать цепочку действий без `cy.wait()`
- Использовать базовые действия: `type()`, `click()`, `clear()`
- Добавлять проверки после действий
- Читать логи Cypress и видеть время выполнения команд

✅ **Ты осознаёшь:**
- Что Cypress «терпеливый» — он ждёт, а не требует мгновенной реакции
- Что тесты с `cy.wait(число)` медленнее и менее надёжные
- Что каждая команда и каждая проверка автоматически ждут
- Что 99% случаев НЕ требуют явного ожидания

✅ **Ты НЕ делаешь:**
- Не используешь `cy.wait(число)` «на всякий случай»
- Не пытаешься сделать команды параллельными
- Не пропускаешь ручное прохождение сценария перед написанием теста

**Если хотя бы два пункта НЕ выполнены — перечитай теорию и повтори практику.**

---

### 5. Куда это ведёт дальше

Сегодня ты понял, как Cypress выполняет команды: последовательно и с автоматическим ожиданием.

Завтра ты научишься **проверять результат** действий. Не просто «кликнул и всё», а «кликнул — и убедился, что произошло ТО, что ожидалось».

**Проверки (assertions)** — это сердце любого теста. Без проверок тест бесполезен.

---

### 📝 Домашнее задание (необязательно, но полезно)

**Задание 1: Найди автоматическое ожидание**
Напиши простой тест для любого сайта. Запусти его и посмотри логи.

Найди команды, которые выполнялись дольше 100 мс. Это те места, где Cypress ждал.

**Задание 2: Эксперимент с медленным сайтом**
Найди любой медленный сайт (или замедли сеть в DevTools до Slow 3G).

Напиши тест для него. Убедись, что тест НЕ падает, даже если страница грузится 2-3 секунды.

**Задание 3: Сравни время**
Напиши два одинаковых теста:
1. С тремя `cy.wait(2000)`
2. Без `cy.wait()`

Засеки время выполнения каждого. Запиши разницу.

---



## ДЕНЬ 4. Проверки (как убедиться, что всё работает)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Тест без проверки — бесполезен (он просто кликает, но не контролирует результат)
- Проверка (assertion) — это утверждение: «Я ожидаю, что элемент находится в таком-то состоянии»
- Cypress проверяет автоматически и ждёт, пока условие станет истинным
- Есть разница между «проверить наличие» и «проверить содержимое»
- Проверки — это 50% ценности теста (действия — вторые 50%)
- Проверка должна быть конкретной, а не размытой

**Что должен УМЕТЬ:**
- Написать проверку на видимость элемента (`.should('be.visible')`)
- Проверить текст элемента (`.should('have.text', '...')`)
- Проверить значение поля (`.should('have.value', '...')`)
- Проверить URL страницы (`.should('include', '...')`)
- Понять, когда тест должен упасть (если проверка не прошла)
- Комбинировать несколько проверок для одного элемента

---

### 2. Теория (без света) — ~3-4 часа

#### Мысленный эксперимент №1: Проверка в жизни
Ты попросил друга купить молоко. Он вернулся.

**Два варианта:**
1. **Без проверки:** Ты не спросил, где молоко. Считаешь, что задача выполнена.
2. **С проверкой:** Ты спросил: «Где молоко?» → друг показывает пакет → ты убедился.

**Тест работает так же:**

Тест без проверки:
```javascript
cy.get('[data-test="buy-button"]').click()
// Всё. Кликнул и ушёл.
```
Он кликнул. А что произошло? **Неизвестно.**

Тест с проверкой:
```javascript
cy.get('[data-test="buy-button"]').click()
cy.contains('Товар добавлен в корзину').should('be.visible')
```
Он кликнул **И убедился**, что появилось сообщение.

**Вывод:** Без проверки ты не знаешь, работает ли сценарий.

#### Что такое assertion (утверждение)?

**Assertion** — это команда Cypress: «Я утверждаю, что элемент находится в таком состоянии».

**Примеры утверждений:**
- «Элемент виден на странице»
- «Элемент содержит текст 'Привет'»
- «Поле имеет значение 'test@example.com'»
- «URL содержит '/dashboard'»
- «Кнопка заблокирована (disabled)»

Если утверждение **ложно** — тест упадёт с ошибкой.
Если утверждение **истинно** — тест продолжится.

#### Основные виды проверок

**1. Проверка видимости:**
```javascript
cy.get('[data-test="message"]').should('be.visible')
```
Проверяет, что элемент:
- Существует в DOM
- Не скрыт (`display: none`, `visibility: hidden`)
- Не за пределами экрана

**2. Проверка текста:**
```javascript
cy.get('[data-test="title"]').should('have.text', 'Добро пожаловать')
```
Проверяет, что элемент содержит **точно такой** текст.

**3. Проверка содержания (частичное совпадение):**
```javascript
cy.get('[data-test="title"]').should('contain', 'Добро')
```
Проверяет, что элемент содержит **часть** текста.

**4. Проверка значения поля:**
```javascript
cy.get('[name="email"]').should('have.value', 'test@test.com')
```
Проверяет значение input/textarea.

**5. Проверка URL:**
```javascript
cy.url().should('include', '/profile')
```
Проверяет, что URL содержит определённую часть.

**6. Проверка атрибута:**
```javascript
cy.get('[data-test="link"]').should('have.attr', 'href', '/home')
```
Проверяет, что у элемента есть атрибут с определённым значением.

**7. Проверка класса:**
```javascript
cy.get('[data-test="button"]').should('have.class', 'active')
```
Проверяет, что у элемента есть определённый CSS-класс.

**8. Проверка состояния (disabled/enabled):**
```javascript
cy.get('[data-test="submit"]').should('be.disabled')
cy.get('[data-test="submit"]').should('be.enabled')
```

#### Почему проверки не падают мгновенно?

**Cypress ждёт, пока условие станет истинным** (как с поиском элементов).

Пример:
```javascript
cy.get('[data-test="buy-button"]').click()
cy.get('[data-test="success-message"]').should('be.visible')
```

**Что происходит:**
1. Cypress кликает по кнопке
2. Ищет `.success-message` → **не видит** → ждёт 50 мс
3. Ищет снова → **не видит** → ждёт ещё 50 мс
4. Ищет снова → **ВИДИТ!** → проверка прошла

Если за 4 секунды сообщение не появилось — тест упадёт с ошибкой.

**Это важно!** Проверки автоматически ждут, как и команды поиска элементов.

#### Типичная ошибка новичка №1: Тест без проверок

Новичок пишет тест:
```javascript
cy.visit('https://example.com')
cy.get('[data-test="button"]').click()
// Всё
```

**Проблема:** Тест «проходит», даже если кнопка ничего не сделала.

**Иллюзия работы:**
- Кнопка кликнута ✓
- Но что произошло? Неизвестно.

**Правильно:**
```javascript
cy.visit('https://example.com')
cy.get('[data-test="button"]').click()
cy.contains('Успешно').should('be.visible') // Проверка результата
```

Теперь тест **контролирует**, что после клика появилось ожидаемое сообщение.

#### Мысленное упражнение №2: Найди недостающие проверки

**Сценарий:** Вход на сайт.
1. Открыть страницу входа
2. Ввести email
3. Ввести пароль
4. Кликнуть «Войти»

**Тест новичка:**
```javascript
cy.visit('/login')
cy.get('[name="email"]').type('user@test.com')
cy.get('[name="password"]').type('12345')
cy.get('[data-test="login-button"]').click()
```

**Вопрос:** Что проверить?

**Ответ — минимум 3 вещи:**
1. URL изменился на `/dashboard` или `/profile`
2. Появилось имя пользователя на странице
3. Кнопка «Выйти» видна

```javascript
cy.visit('/login')
cy.get('[name="email"]').type('user@test.com')
cy.get('[name="password"]').type('12345')
cy.get('[data-test="login-button"]').click()

// Проверки:
cy.url().should('include', '/dashboard')
cy.contains('user@test.com').should('be.visible')
cy.get('[data-test="logout-button"]').should('be.visible')
```

**Без проверок ты не знаешь, вошёл ли пользователь.**

#### Разница между `have.text` и `contain`

**`have.text`** — точное совпадение:
```javascript
cy.get('[data-test="title"]').should('have.text', 'Привет, мир!')
```
Если текст элемента: «Привет, мир!» — ✓ проходит
Если текст элемента: «Привет, мир! Добро пожаловать» — ✗ падает

**`contain`** — частичное совпадение:
```javascript
cy.get('[data-test="title"]').should('contain', 'Привет')
```
Если текст элемента: «Привет, мир!» — ✓ проходит
Если текст элемента: «Привет, мир! Добро пожаловать» — ✓ проходит
Если текст элемента: «Добро пожаловать» — ✗ падает (нет слова «Привет»)

**Когда что использовать:**
- `have.text` — когда текст должен быть точно таким
- `contain` — когда важна только часть текста

#### Типичная ошибка новичка №2: Проверка несуществующего элемента

Новичок пишет:
```javascript
cy.get('[data-test="popup"]').should('not.exist')
```

Пытается проверить, что всплывающее окно **не появилось**.

**Проблема:** Если окно появляется с задержкой (например, через 5 секунд), проверка пройдёт, хотя окно потом появится.

**Правильнее:**
Не проверять отсутствие, а проверять присутствие того, что должно быть:
```javascript
cy.contains('Операция завершена').should('be.visible')
```

**Правило:** Проверяй то, что ДОЛЖНО быть, а не то, чего НЕ должно быть (в большинстве случаев).

#### Реальная история: Тест без проверок

На проекте был тест «Добавление товара в корзину». Он кликал кнопку и всё.

Однажды функция сломалась: кнопка кликалась, но товар не добавлялся. Тест продолжал проходить (он же просто кликал!).

О проблеме узнали только когда пользователи начали жаловаться.

**Решение:** Добавили проверку:
```javascript
cy.get('[data-test="add-to-cart"]').click()
cy.get('[data-test="cart-counter"]').should('contain', '1') // Проверка счётчика
```

Теперь тест ловит эту проблему.

#### Мысленное упражнение №3: Сколько проверок нужно?

**Сценарий:** Регистрация на сайте.

**Минималистичный подход (1 проверка):**
```javascript
cy.url().should('include', '/welcome') // Переход на страницу приветствия
```

**Надёжный подход (3-4 проверки):**
```javascript
cy.url().should('include', '/welcome')
cy.contains('Регистрация успешна').should('be.visible')
cy.get('[data-test="username"]').should('contain', 'newuser')
cy.get('[data-test="logout"]').should('be.visible')
```

**Избыточный подход (10+ проверок):**
Проверять каждый элемент на странице — цвет кнопок, размер шрифта, порядок элементов и т.д.

**Золотое правило:** Проверяй **ключевые индикаторы успеха**, но не каждую мелочь.

#### Комбинирование проверок

Можно проверять несколько вещей для одного элемента:
```javascript
cy.get('[data-test="username"]')
  .should('be.visible')
  .and('contain', 'John')
  .and('have.class', 'active')
```

Это цепочка проверок:
1. Элемент виден
2. Содержит текст «John»
3. Имеет класс «active»

**Все проверки должны пройти**, иначе тест упадёт.

#### Мысленное упражнение №4: Проверь сценарий покупки

**Сценарий:**
1. Добавить товар в корзину
2. Перейти в корзину
3. Оформить заказ
4. Увидеть подтверждение

**Какие проверки нужны на каждом шаге?**

Шаг 1:
- Счётчик корзины увеличился
- Появилось уведомление «Товар добавлен»

Шаг 2:
- URL содержит `/cart`
- Товар отображается в списке
- Цена товара правильная

Шаг 3:
- URL изменился на `/checkout` или `/success`
- Появилось сообщение «Заказ оформлен»
- Номер заказа отображается

**Без этих проверок ты не знаешь, работает ли процесс покупки.**

---

### 3. Практика (со светом) — ~1.5 часа

#### Шаг 1: Добавь проверку в существующий тест
Открой [cypress/e2e/google.cy.js](cypress/e2e/google.cy.js).

Найди строку, где Cypress что-то делает (например, ищет). Добавь проверку:
```javascript
cy.get('[name="q"]').type('Cypress testing{enter}')
cy.get('#search').should('be.visible') // проверка: блок результатов виден
```

Запусти тест. Если тест прошёл — проверка сработала.

#### Шаг 2: Проверь текст элемента
Добавь проверку на наличие определённого текста в результатах:
```javascript
cy.contains('Cypress').should('be.visible')
```

Это утверждение: «На странице есть элемент с текстом 'Cypress', и он виден».

#### Шаг 3: Эксперимент с неверной проверкой
Попробуй добавить заведомо ложную проверку:
```javascript
cy.contains('Несуществующий текст').should('be.visible')
```

Запусти тест. Cypress будет ждать 4 секунды, не найдёт элемент и упадёт с ошибкой.

Это правильное поведение — тест должен падать, если ожидание не оправдалось.

#### Шаг 4: Убери ложную проверку
Удали строку с неверной проверкой. Тест должен снова проходить.

---

### 4. Критерии «день завершён»

✅ Ты добавил хотя бы одну проверку `.should()` в тест
✅ Ты понимаешь, что тест без проверки не имеет смысла
✅ Ты видел, как тест падает, если проверка не проходит
✅ Ты не пишешь тесты, которые «просто кликают»

---

### 5. Куда это ведёт дальше

Завтра ты научишься читать ошибки Cypress. Когда тест падает, Cypress показывает, ЧТО пошло не так.

Умение читать ошибки — это 50% навыка тестировщика.

---

## ДЕНЬ 5. Чтение ошибок (как понять, что сломалось)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Ошибка теста — это не катастрофа, а информация
- Cypress показывает, на каком шаге тест упал и почему
- Ошибка может быть в тесте, в сайте или в селекторе

**Что должен УМЕТЬ:**
- Прочитать ошибку и понять, что пошло не так
- Найти строку кода, где упал тест
- Понять разницу между «элемент не найден» и «элемент скрыт»

---

### 2. Теория (без света) — ~2 часа

#### Мысленный эксперимент №1: Инструкция не работает
Ты дал другу инструкцию: «Пойди в комнату, открой шкаф, возьми книгу».

Друг вернулся и сказал: «Не получилось».

Что ты спросишь?
1. Ты нашёл комнату?
2. Ты открыл шкаф?
3. Книга была в шкафу?

**Ошибка Cypress работает так же:** она говорит, НА КАКОМ шаге застрял.

#### Основные типы ошибок

**1. Элемент не найден**
```
Timed out retrying: Expected to find element: '[data-test="кнопка"]', but never found it.
```

Причины:
- Неправильный селектор
- Элемент появляется позже (больше 4 секунд)
- Страница не загрузилась

**2. Элемент скрыт**
```
cy.click() failed because this element is not visible
```

Причины:
- Элемент есть в коде, но скрыт CSS (display: none)
- Элемент за другим элементом
- Элемент вне экрана

**3. Неверный текст**
```
Expected to have text 'Привет', but found 'Hello'
```

Причины:
- Текст на сайте изменился
- Неправильная проверка в тесте

#### Как читать ошибку?
Cypress показывает:
1. Какая команда упала (например, `cy.get()`)
2. Какой селектор не сработал
3. Сколько времени ждал (обычно 4000 мс)

Пример:
```
CypressError: Timed out retrying after 4000ms: Expected to find element: '#login', but never found it.
```

Перевод: «Я ждал 4 секунды, искал элемент с id='login', но не нашёл».

#### Типичная ошибка новичка
Новичок видит ошибку и думает: «Cypress глючит».
Нет. Cypress показывает, что на сайте что-то не так ИЛИ тест написан неправильно.

#### Мысленное упражнение №2
Представь: тест падает с ошибкой «Элемент не найден».

Твои действия:
1. Открой сайт вручную
2. Проверь, есть ли элемент на странице
3. Если есть — проверь селектор в инструментах разработчика
4. Если нет — возможно, сайт сломан

---

### 3. Практика (со светом) — ~1.5 часа

#### Шаг 1: Намеренно сломай тест
Открой [cypress/e2e/google.cy.js](cypress/e2e/google.cy.js).

Измени селектор на неправильный:
```javascript
cy.get('[name="несуществующееполе"]').type('текст')
```

Запусти тест. Посмотри на ошибку в Cypress.

#### Шаг 2: Прочитай ошибку
Cypress покажет:
- Команду, которая упала
- Селектор, который не сработал
- Время ожидания

Найди в ошибке фразу: `Expected to find element`.

Это значит: Cypress не нашёл элемент. Проблема в селекторе.

#### Шаг 3: Исправь селектор
Верни правильный селектор:
```javascript
cy.get('[name="q"]').type('текст')
```

Запусти тест. Ошибка исчезла.

#### Шаг 4: Эксперимент с неверной проверкой
Добавь проверку с неправильным текстом:
```javascript
cy.contains('Cypress').should('have.text', 'НЕВЕРНЫЙТЕКСТ')
```

Запусти тест. Посмотри на ошибку:
```
Expected 'Cypress - JavaScript End to End Testing Framework' to have text 'НЕВЕРНЫЙТЕКСТ'
```

Cypress показывает, какой текст ожидал и какой нашёл.

#### Шаг 5: Убери неверную проверку
Удали строку. Тест должен проходить.

---

### 4. Критерии «день завершён»

✅ Ты намеренно сломал тест и прочитал ошибку
✅ Ты понимаешь, что ошибка — это подсказка, а не враг
✅ Ты нашёл в ошибке: какая команда упала, какой селектор, сколько ждал
✅ Ты исправил ошибку и тест снова прошёл

---

### 5. Куда это ведёт дальше

Завтра ты узнаешь, как сделать тесты **стабильными**. Иногда тест «иногда падает» — это проблема не сайта, а теста.

Ты научишься избегать таких ситуаций.

---

## ДЕНЬ 6. Стабильность тестов (почему тест не должен «иногда падать»)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Тест должен быть предсказуемым: либо ВСЕГДА падает, либо ВСЕГДА проходит
- «Иногда падает» — признак плохого теста (flaky test)
- Причины нестабильности: внешние зависимости, случайные данные, плохие селекторы

**Что должен УМЕТЬ:**
- Написать тест, который не зависит от случайных факторов
- Использовать фикстуры вместо реальных данных
- Понять, когда тест нужно исправить, а когда проблема в сайте

---

### 2. Теория (без света) — ~2.5 часа

#### Мысленный эксперимент №1: Ненадёжная инструкция
Ты написал инструкцию: «Возьми ЛЮБУЮ книгу с полки».

Проблема: каждый раз будут брать разные книги. Если одна книга — рецепты, а другая — фантастика, результат непредсказуем.

**Нестабильный тест работает так же.**

Пример:
```javascript
cy.get('button').first().click()
```

Если на странице кнопки появляются в случайном порядке, каждый раз будет кликаться разная кнопка.

#### Причины нестабильности

**1. Зависимость от порядка элементов**
```javascript
cy.get('li').eq(2).click() // клик на третий элемент списка
```

Если список динамический (элементы добавляются/удаляются), тест сломается.

**2. Зависимость от реальных данных**
Тест регистрирует пользователя с email `test@test.com`.
Запустили тест второй раз → email уже занят → тест упал.

**3. Зависимость от времени**
```javascript
cy.wait(3000) // жёсткое ожидание
```

Если страница загрузилась за 4 секунды, тест упадёт.

**4. Зависимость от внешних сервисов**
Тест проверяет погоду на реальном API. Если API недоступен, тест упадёт.

#### Как сделать тест стабильным?

**1. Используй точные селекторы**
Вместо: `cy.get('button').first()`
Лучше: `cy.get('[data-test="submit-button"]')`

**2. Используй фикстуры (заготовленные данные)**
Вместо реального API с погодой — используй заранее подготовленный JSON с ответом.

**3. Избегай жёстких ожиданий**
Вместо: `cy.wait(3000)`
Лучше: `cy.get('элемент').should('be.visible')` — Cypress сам подождёт.

**4. Очищай данные перед тестом**
Если тест создаёт пользователя, добавь в начало теста удаление этого пользователя (если он существует).

#### Типичная ошибка новичка
Новичок запускает тест 10 раз. 9 раз прошёл, 1 раз упал.
Новичок думает: «Ну, иногда бывает».

Нет. Это проблема. Тест должен быть надёжным.

#### Мысленное упражнение №2
Представь тест для интернет-магазина: «Добавить товар в корзину».

Нестабильный вариант:
- Тест кликает на первый товар в списке
- Но список товаров зависит от акций (меняется каждый день)

Стабильный вариант:
- Тест ищет товар по конкретному data-атрибуту: `[data-test="product-123"]`
- Товар не меняется

---

### 3. Практика (со светом) — ~1.5 часа

#### Шаг 1: Создай нестабильный тест
Открой [cypress/e2e/google.cy.js](cypress/e2e/google.cy.js).

Напиши тест, который кликает на первый результат поиска:
```javascript
cy.visit('https://www.google.com')
cy.get('[name="q"]').type('Cypress{enter}')
cy.get('#search a').first().click()
```

Запусти тест несколько раз. Каждый раз будет кликаться первая ссылка в результатах.

Но результаты могут меняться (зависят от региона, истории поиска). Тест нестабилен.

#### Шаг 2: Сделай тест стабильным
Вместо клика на первую ссылку, проверь конкретный текст:
```javascript
cy.visit('https://www.google.com')
cy.get('[name="q"]').type('Cypress{enter}')
cy.contains('Cypress.io').should('be.visible')
```

Теперь тест просто проверяет, что в результатах есть упоминание Cypress.io. Это предсказуемо.

#### Шаг 3: Эксперимент с жёстким ожиданием
Добавь в тест:
```javascript
cy.wait(5000) // жёсткая задержка
```

Запусти тест. Он будет ждать 5 секунд, даже если страница загрузилась за 1 секунду.

Это замедляет тест и не делает его надёжнее.

#### Шаг 4: Убери жёсткое ожидание
Удали `cy.wait(5000)`. Тест должен работать быстрее и так же стабильно.

---

### 4. Критерии «день завершён»

✅ Ты понимаешь, что такое flaky test (нестабильный тест)
✅ Ты написал тест, который не зависит от порядка элементов
✅ Ты НЕ используешь `cy.wait(время)` без необходимости
✅ Ты используешь точные селекторы (data-атрибуты)

---

### 5. Куда это ведёт дальше

Завтра — последний день первой недели. Ты узнаешь, когда тесты НЕ нужны, и как не тестировать лишнее.

Это важно: плохой тест хуже, чем отсутствие теста.

---

## ДЕНЬ 7. Когда НЕ писать тесты (и что делать с тем, что узнал)

### 1. Фиксация цели дня
**Что должен ПОНЯТЬ:**
- Не нужно тестировать ВСЁ — это бессмысленно
- Тесты должны проверять критичные сценарии, а не каждую кнопку
- Плохой тест требует больше времени на поддержку, чем приносит пользы

**Что должен УМЕТЬ:**
- Решить, стоит ли писать тест для конкретного сценария
- Понять, когда достаточно ручного тестирования
- Подвести итоги недели и понять, что изучать дальше

---

### 2. Теория (без света) — ~3 часа

#### Мысленный эксперимент №1: Проверять ли всё?
Представь: ты управляешь автомобилем. Перед каждой поездкой ты можешь:
1. Проверить масло
2. Проверить шины
3. Проверить фары
4. Проверить тормоза
5. Проверить дворники
6. Проверить радио

Что критично? Тормоза и шины.
Что не критично? Радио.

**Тесты работают так же:** проверяй важное, не трать время на мелочи.

#### Когда НЕ писать E2E-тест?

**1. Функция редко используется**
Если кнопка «Экспорт в PDF» используется 1% пользователей, E2E-тест для неё избыточен.

**2. Функция быстро меняется**
Если дизайн страницы переделывают каждую неделю, тест будет постоянно ломаться. Дешевле проверить руками.

**3. Функция простая и очевидная**
Проверять, что кнопка «Закрыть» закрывает окно, — излишне. Это слишком просто, чтобы сломаться.

**4. Есть unit-тесты**
Если функция уже проверена на уровне кода (unit-тесты), E2E-тест дублирует проверку.

#### Когда писать E2E-тест?

**1. Критичный сценарий**
Вход на сайт, оплата, регистрация — если это сломается, бизнес пострадает.

**2. Часто используемая функция**
Поиск, добавление в корзину, отправка сообщения — если это сломается, пользователи уйдут.

**3. Сложный сценарий**
Многошаговый процесс (например, оформление заказа в 5 шагов) легко сломать изменением одного элемента.

#### Правило 80/20
80% проблем ловятся 20% тестов.

Лучше иметь 10 стабильных тестов на критичные сценарии, чем 100 тестов на всё подряд.

#### Типичная ошибка новичка
Новичок пишет тесты на всё: каждую кнопку, каждое поле, каждую страницу.
Результат: 100 тестов, половина падают после каждого обновления дизайна.

Лучше: 10 тестов, которые проверяют то, без чего сайт мёртв.

#### Мысленное упражнение №2
Представь сайт с функциями:
1. Вход в аккаунт
2. Поиск товаров
3. Добавление в корзину
4. Оплата
5. Изменение цвета темы (светлая/тёмная)
6. Подписка на рассылку

Какие сценарии критичны?
- 1, 2, 3, 4 — без них сайт бесполезен.
- 5, 6 — приятные, но не критичные.

Пиши тесты на 1-4. На 5-6 можно проверить руками.

---

### 3. Практика (со светом) — ~1.5 часа

#### Шаг 1: Оцени существующий тест
Открой [cypress/e2e/google.cy.js](cypress/e2e/google.cy.js).

Задай себе вопросы:
1. Проверяет ли тест критичный сценарий? (Да: поиск — основная функция Google)
2. Стабилен ли тест? (Если селекторы правильные — да)
3. Приносит ли тест пользу? (Да: если поиск сломается, тест упадёт)

Если на все три вопроса «да» — тест имеет смысл.

#### Шаг 2: Придумай сценарий, который НЕ стоит тестировать
Напиши комментарий в файле:
```javascript
// Сценарий, который НЕ стоит тестировать:
// - Изменение языка интерфейса (не критично)
// - Проверка цвета кнопки (визуальное тестирование, не E2E)
```

Это помогает осознать границы: что важно, а что — нет.

#### Шаг 3: Подведи итоги недели
Создай файл в корне проекта: `Что я узнал.md`

Напиши кратко:
- Что я понял про E2E-тесты?
- Какие команды Cypress я использовал?
- Какие ошибки я видел и исправил?
- Что мне ещё непонятно?

Это саморефлексия — важный навык тестировщика.

---

### 4. Критерии «день завершён»

✅ Ты понимаешь, что НЕ всё нужно тестировать
✅ Ты можешь решить: стоит ли писать тест для конкретного сценария
✅ Ты создал файл с итогами недели
✅ Ты готов к следующему этапу обучения

---

### 5. Что дальше?

**Ты прошёл фундамент.**

Теперь ты понимаешь:
- Что такое E2E-тест и зачем он нужен
- Как Cypress выполняет команды и ждёт элементы
- Как находить элементы (селекторы)
- Как проверять результат (assertions)
- Как читать ошибки
- Как делать тесты стабильными
- Когда тесты НЕ нужны

**На следующей неделе** (если продолжишь):
- Работа с формами (ввод, выбор, загрузка файлов)
- Перехваты запросов (fixtures, cy.intercept)
- Организация тестов (beforeEach, переиспользование кода)
- Работа с данными (переменные, алиасы)

Но это уже следующий уровень. Пока — закрепи то, что узнал на этой неделе.

---

## ЗАМЕТКИ ДЛЯ ПРОДОЛЖЕНИЯ

Если ученик запросит **продолжение**, двигаться в таких направлениях:

**Неделя 2: Углубление**
- Работа с формами (checkbox, radio, select, file upload)
- Алиасы и переменные
- beforeEach / afterEach
- Группировка тестов (describe)

**Неделя 3: Реальность**
- Работа с API (cy.request, cy.intercept)
- Фикстуры и моки
- Переиспользование кода (custom commands)
- Работа с куками и localStorage

**Неделя 4: Стратегия**
- Page Object Model
- Стратегии организации тестов
- CI/CD (запуск тестов автоматически)
- Отчёты и скриншоты при падении

---

## НАПОМИНАНИЕ НАСТАВНИКУ

- Не перегружать терминами
- Объяснять СМЫСЛОМ, а не синтаксисом
- Поддерживать, а не поучать
- Двигаться от простого к сложному
- Всегда давать возможность закрепить практикой

**Успехов ученику в обучении!**
